# 오디오 설계 (R1) — BlockCastle

정본 문서. 코드의 접점은 `Main.gd`의 `===== 오디오 =====` 블록 한 곳이고, 감사는 `tools/audio_probe.gd`,
음색 청취는 `tools/audio_bake.gd`가 한다. 형제 문서 = `HAPTIC_PLAN.md`(같은 이음새를 공유한다).

## 1. 왜 지금

- **`sound_on`·`bgm_on`이 소비되지 않는 선호로 오래 남아 있었다.** 설정 모달에 토글이 있고 세이브에
  바이트가 있는데 소리를 내는 코드는 0줄이었다 = 유저에게는 "꺼도 켜도 똑같은 스위치".
- **콤보의 연속 스케일을 낼 수 있는 채널이 오디오뿐이다.** 시각 칭찬어는 이산 등급(GOOD→UNREAL),
  햅틱은 amp 분기가 불가능해 실질 1박/2박이 전부다. 음정은 8단을 **연속으로** 올릴 수 있다.
- **이름값을 소리가 갚는다.** 게임 이름의 '연쇄'는 규칙에서 빠졌고(창작자 확인) 연출 순서로 채우기로
  했다(L1). 링이 시간차로 터지는 그 시퀀스에 **상승 음계**를 얹으면 연쇄가 처음으로 귀에 들린다.

동결(ROADMAP §5, 2026-07-27)과의 관계: 형식상 폴리싱이지만 **무음 빌드로 사람 플테를 재면 코어를
과소평가한다**는 햅틱과 같은 근거로 진행(유저 결정, 2026-07-31). 소리는 진동보다 이 논거가 강하다 —
무음 퍼즐게임은 실제로 덜 재밌게 느껴진다.

## 2. 원칙 넷

1. **두 번째 채널이지 유일한 채널이 아니다.** 모바일은 상당수가 무음 플레이다. 소리가 없으면
   못 읽히는 정보는 만들지 않는다 — 오디오는 전부 **이미 시각으로 말한 것의 보강**이다.
2. **적은 뭉갬이 아니라 진흙(mud)이다.** 햅틱은 액추에이터가 하나라 승자독식이었지만 오디오는
   폴리포니가 있다. 그래서 위험이 다르다: 동시에 열 개가 울리면 드롭이 아니라 **탁하게 뭉친 소음**이
   된다. 방어는 보이스 상한 + 단어별 최소 간격 + 초당 발화 예산.
3. **잦을수록 작고 짧게.** `place`는 판당 수십 번 울린다 → 가장 조용하고 가장 짧다. 긴 소리는
   판당 1회(`fanfare`)뿐이다.
4. **음정이 의미를 나른다.** 세기가 아니라 **음높이**로 크기를 말한다. 그래야 볼륨을 낮춰 들어도
   구분이 살아남고, 5음계라 몇 개가 겹쳐도 불협이 안 난다.

## 3. 두 믹서, 한 접점

햅틱과 오디오는 **호출부를 공유하고 믹서를 공유하지 않는다.**

- 공유하는 이유: 호출부가 갈리면 6757줄짜리 `Main.gd`의 같은 줄을 두 시스템이 각각 고쳐 병합마다
  충돌하고, 더 나쁘게는 "무엇이 중요한가"를 두 곳이 따로 판단해 어긋난다.
- 공유하지 **않는** 이유: 물리가 다르다. 진동은 승자독식·불응기 드롭이 맞고, 소리는 겹쳐도 되며
  삭제음을 진동 불응기 때문에 삼키면 **보상이 사라진다**. 한 믹서로 합치면 이 둘 중 하나가 망가진다.

그래서 `_fb(kind, intensity)` 하나가 접점이고, 내부에서 `_haptic()`과 `_sfx()`로 **독립 라우팅**한다.
매핑표(`FB_MAP`)가 "이 사건은 진동 무엇 · 소리 무엇"을 한자리에 적어두는 유일한 정본이다.

## 4. 어휘 — 다섯

| 단어 | 물리 | 언제 | 간격 · 비고 |
|---|---|---|---|
| **place** | 0.09s, 190Hz 나무 톡 + 3ms 트랜지언트 | 조각 착지 | 0.04s · 가장 조용(−16dB) |
| **clear** | 0.32s, 523Hz 종(배음 4개) | 줄 삭제 다운비트(섬광과 같은 프레임) | 간격 없음 — **절대 안 드롭**(보상이다). 콤보 → 5음계 음정 |
| **chain** | 0.18s, 짧은 마림바 | 순차 피격·처치·보석 획득 **하나마다** | 0.03s · 판 안에서 **한 계단씩 상승**, 상한 8단 |
| **fanfare** | `clear`를 0/+4/+7/+12로 4연타 | 스테이지 클리어 · 무한 PB 돌파 | **판당 1회** |
| **tap** | 0.05s, 330Hz 부드러운 딸깍 | UI 버튼 | 0.05s |

**5음계 사다리** = `[0,2,4,7,9,12,14,16]` 반음(장5음계). 크로매틱을 쓰면 연쇄 중간중간이 불협으로
들리는데, 5음계는 **어떤 조합으로 겹쳐도 협화**라 순서가 흐트러져도 음악으로 들린다. 상한 16반음
(=2.52×)에서 멈춘다 — 더 올리면 개 호루라기가 되고 `pitch_scale` 상한에도 가까워진다.

**손실에는 아무 소리도 없다.** 누수·거점사·막힘사 전부 무음 = 햅틱과 같은 결정("축하만, 손실은 눈으로",
유저 재확인 2026-07-31). 코지 톤과 싸우지 않게 하려는 것이고, 없다가 넣는 게 넣었다 빼는 것보다 쉽다.

**기각**: 배치 실패 버즈(탐색을 벌하면 '안전하게 배우는 재미'와 정면 충돌) · 적 전진음(매 턴 = 진흙) ·
호버/드래그음 · 별도 처치음(`chain`이 이미 그 자리다).

## 5. 에셋 — 없다 (런타임 합성)

**`.wav` 파일을 하나도 안 만든다.** `_ready`에서 GDScript가 PCM을 계산해 `AudioStreamWAV`를 메모리에
띄운다(전부 합쳐 22.05kHz·16bit 모노 ~0.6초 = **약 27KB**).

| | 합성 | CC0 팩 | 외주 |
|---|---|---|---|
| 번들 | ~0KB | 수백 KB~ | 수백 KB~ |
| 라이선스 표면 | **0** (RELEASE.md·심사 무변화) | 출처 관리 필요 | 계약 |
| 튜닝 반복 | 상수 한 줄 | 파일 교체 | 왕복 |
| 톤 | 미니멀·저아트와 일치 | 남의 게임 톤 | 맞출 수 있음 |

커스텀-draw로 픽셀 하나까지 코드가 그리는 프로젝트라 소리도 코드가 만드는 쪽이 톤이 맞는다.
⚠**한계: 합성음은 값싸게 들릴 수 있다.** 그래서 엔벨로프(빠른 어택·지수 감쇠)와 배음 구조에 공을
들였고, **판정은 사람 귀 몫**이다 — `tools/audio_bake.gd`가 각 단어를 `build/audio_preview/*.wav`로
구워 주니 그걸 듣고 상수를 고친다. BGM은 합성으로 못 만든다(§7).

**난수를 안 쓴다.** 미세 디튠은 `randf()`가 아니라 발화 카운터 기반 결정적 순환이다 — 회귀 골든이
전역 RNG 스트림을 공유하므로, 연출이 난수를 더 뽑으면 하류가 시프트할 여지가 생긴다. 안 뽑으면 그
질문 자체가 없어지고 프로브도 결정적으로 재현된다.

## 6. 이음새

- **접점**: `_fb(kind, intensity)` → `_haptic()` / `_sfx()`. `_sfx_step(delta)`는 `_hap_step` 바로 뒤
  (조기 반환보다 먼저 — 예약된 fanfare 음이 메뉴 전환에서 끊기면 안 된다).
- **호출부 6곳**: `_place_piece`(place) · `_burst_lines`(clear, 콤보) · `_apply_hit`의 처치·보석 분기
  (chain) · `_check_win`(fanfare) · PB 돌파 분기(fanfare) · 설정 토글(tap).
- **보이스 풀**: `AudioStreamPlayer` 8개(Master 버스 그대로 — 버스 레이아웃 파일을 안 만든다 =
  project.godot 무변화). 가득 차면 가장 오래된 것을 뺏는다.
- **관찰만**: 게임 값도 RNG도 안 건드린다 → `tools/regress.gd` 골든 불변.
- **플랫폼**: 데스크톱·모바일 동일 동작. 헤드리스는 오디오 드라이버가 더미라 자동 no-op.

## 7. BGM — R1에서 만들지 않는다

합성으로 못 만들고, 기본값이 이미 OFF이며, 모바일 다수는 무음 플레이다. `bgm_on` 토글은 **오늘과
똑같이 아무 일도 안 하는 상태로 남는다** — R1이 고치는 건 `sound_on`뿐이다.
⚠**미결**: 죽은 토글을 실배선까지 숨길지는 유저 판단으로 남긴다(숨기면 UI가 정직해지고,
두면 나중에 되살릴 때 자리 이동이 없다).

## 8. 검증 상태

`tools/audio_probe.gd` (시드 20250123 고정, 창 모드) — **11검사 PASS**:

| 실측 | 값 |
|---|---|
| 사람 템포(1.8s/수) | 27발 / 29.0초 = **초당 0.93발**, 드롭 0 |
| 스트레스(최고속) | 초당 3.46발, `place` 간격 드롭 7건(불응기 작동) |
| **동시 보이스** | 사람·스트레스 **양쪽 최대 3** (상한 8 대비 여유) |
| 롤링 1초 | 최대 6발 (예산 14 대비 여유) |
| 연쇄 사다리 | 판당 3회, 최장 런 `[0,2,4,7,9]` 5음, 역행 0회·상한 초과 0회 |
| fanfare | `[0,4,7,12]` ×2판, 같은 판 두 번째는 `once` 드롭 |
| 결정성 | 같은 시드 두 패스 로그 완전 동일(RNG 미사용) |

`tools/haptic_probe.gd` — 11검사 PASS(리팩터 후에도 불변). `tools/regress.gd` — 골든 **byte-identical**.

`tools/audio_bake.gd` 실측 — 뱅크 총 **29.3KB**, 전 파일 클리핑 0, 시작·끝 샘플 0(불연속 잡음 없음).
⚠튜닝 이력: `clear` amp를 0.62→0.45로 낮췄다 — 배음 4개가 −0.9dBFS까지 붙어서 `fanfare`가 그걸
4연타로 겹치자 프리뷰 파일에 클립 5샘플이 났다(합성은 헤드룸을 직접 챙겨야 한다).

⚠**미검증 둘**: ①**음색** — 합성음이 값싸게 들리는지는 사람 귀로만 답이 난다(구운 파일 청취가 다음 할 일)
②**기기** — 실제 볼륨 밸런스·스피커 톤·안드로이드 지연·오디오 포커스는 실기기 몫.

> 곁다리 발견: `tools/settings_shot.gd`는 **이미 깨져 있다**(없어진 `SETTINGS_GEAR` 상수를 참조 —
> main·track/haptic 양쪽에 없음). 이번 변경과 무관한 선재 결함이라 손대지 않았다.

## 9. R2 — 사운드 디렉터 검토 반영 (2026-07-31)

R1을 계측했더니 **데스크톱에서 믹스한 티가 그대로 나왔다.** 네 결함과 조치:

| | 진단(R1 실측) | 조치 | 결과(R2 실측) |
|---|---|---|---|
| **P0** | `place` 에너지의 97%가 300Hz 아래(기음 190Hz) → 폰 시뮬 **−18.4dB** = 가장 잦은 소리가 사라짐 | 몸통 190→**520Hz**, 2.8kHz 딸깍 추가. 235Hz는 남기되 **정보를 안 실음** | 폰 시뮬 **−6.7dB** (11.7dB 회복) |
| **P1** | 네 단어 중심이 196~591Hz 한 옥타브에 몰림, 2kHz 위 0.0%. `clear`(591)와 `chain`(537)이 캐스케이드에서 서로 가림 | 단어마다 자기 대역: place 507 · clear 660 · **chain 903** · tap 1215Hz. clear 배음을 3.5kHz까지 | 종·연쇄 분리 확보, 어택창 2~5kHz 3.5→**8.7%**(place) |
| **P2** | 디튠 ±0.6%(≈10센트) = 사실상 안 들려 반복 피로를 못 막음 | 단어별 폭(`det`)으로 분리 — place·tap ±1.2%, **음정이 의미를 나르는** clear·chain은 ±0.4%로 좁게(사다리 계단 보존) | — |
| **P3** | 실효 −24~−34dBFS로 판단했으나 **이 진단은 과했다**(아래) | 위계만 재조정 + 런타임 리미터 | clear 0 / chain −5.5 / place −7.6 / tap −7.4 dB |

**⚠P3는 과잉 진단이었다(기록으로 남긴다).** "실효 RMS −34dBFS"는 *소리 길이 기준 RMS*인데 이걸
*프로그램 통합 라우드니스* 목표와 비교해 "너무 조용하다"고 했다. 크레스트가 12~17dB라 실제
보이스 피크는 −7~−18dBFS로 정상 범위였다. 진짜 문제는 절대 레벨이 아니라 **위계**였다 —
`chain`이 `clear` 바로 아래(−2.4dB)에 붙어 있었는데, 한 연쇄에 5~9발이 나가므로 종을 덮는다.

**리미터**: `AudioServer`로 **런타임에** "SFX" 버스 + `AudioEffectHardLimiter`(ceiling −0.5dB)를
만든다. 버스 레이아웃 리소스를 안 만들므로 §5의 '파일 0개'가 유지된다. 캐스케이드 합산 피크의
보험이지 상시 압축이 아니다.

**계측 도구가 두 번 틀렸다 — 이게 이번의 진짜 교훈.**
① 첫 FFT는 파일 앞 4096샘플에 해닝창 하나만 씌웠다 → 전 구간 Welch 평균으로 교체.
② 그 다음 어택창 분석에서 **같은 실수를 반복**했다 — 264샘플 구간에 해닝을 씌웠는데 해닝은
sample 0에서 0이라 **재려던 트랜지언트를 창이 지웠다**. "딸깍이 아예 없다"는 결론이 여기서
나왔고 틀렸다(사각창으로 재니 존재했다). → **트랜지언트를 잴 땐 사각창.** 그리고 계측이 이상한
결론을 내면 대상이 아니라 **자를 먼저 의심할 것**([[verify-reference-claims-on-raw-frames]]와 같은 종류의 함정).

검증: `audio_probe` 11검사 PASS · `haptic_probe` 11검사 PASS · regress 골든 **byte-identical** ·
전 파일 클리핑 0 · 뱅크 26.9KB. **여전히 음색은 사람 귀 미판정**(`*_PHONE.wav` A/B 포함).

## 10. R3 — 레퍼런스(Block Blast) 실측 반영 (2026-07-31)

52초 화면녹화를 분석했다. **⚠먼저 함정: 녹화의 18.4~24.3초·44.6~47.2초는 동영상 광고다.**
광고 오디오는 피크가 0dBFS에 붙어 있어, 안 걷어내면 "레퍼런스 콤보음이 배치음보다 8dB 크다"는
정반대 결론이 나온다. 프레임을 안 봤으면 그대로 믿었을 것이다([[verify-reference-claims-on-raw-frames]]).
아래는 광고 8.5초 제외, 게임플레이 43.6초 · 이벤트 42개 기준.

**확인된 것**
- **BGM이 없다.** 프레임 52.5%가 최대 대비 −45dB 이하, 바닥은 디지털 무음. 통합 −19.1 LUFS.
  → §7의 "R1에 BGM 없음" 판단이 레퍼런스와 일치(이제 추측이 아니라 실측).
- **저역을 안 쓴다.** 300Hz 아래 에너지: 집기 1.1% · 착지 0.6% · 삭제 0.1%. → §9 P0가 옳았다.
- **제스처 방향이 동작을 말한다** — 집기는 **상승**(345→560Hz, +8.4반음), 착지는 **하강**
  (904→388Hz). 삭제는 904→1550Hz 상승 2음(+9.3반음) + 3531Hz 광택층.
- **보석/카운터 도착 = 517→646→775→1550Hz = C–E–G–G′ 상승 아르페지오.**
  우리 `fanfare`(0/+4/+7/+12)와 사실상 같은 음형에 독립적으로 도달했다.
- **레벨 위계가 거의 없다** — 전 SFX가 −8.3~−9.6dBFS 한 줌. 삭제음이 착지음보다 **안 크다**.
  볼륨이 아니라 **음색·제스처·층수**로 차별화한다.

**반영한 것(R3)**
1. **`grab` 신설** — 우리엔 드래그 시작이 무음이었다. 레퍼런스에서 가장 잦은 소리(14회)가 이거다.
   480→780Hz 상승 글라이드(+8.4반음, 폭은 레퍼런스 그대로). 접점 = `_pick_up`.
   ⚠시작음을 420Hz로 뒀더니 폰 시뮬 −8.0dB → 480Hz로 올려 −6.2dB.
2. **`place`를 하강 제스처로** — 884→520Hz(도착 주파수는 R2 그대로, 위에서 미끄러져 온다).
   집기 상승과 짝이 된다. 부수 효과로 폰 통과가 −6.7→**−4.0dB**로 더 좋아졌다.
3. `_sfx_render`에 **글라이드**(위상 누적) 추가. `sin(TAU·f(t)·t)`로 하면 f가 변할 때 위상이 튀어
   잡음이 낀다 → 샘플마다 위상을 적분한다.
4. `grab`·`place`는 **한 쌍이라 실효 레벨을 맞췄다**(−8.0 / −7.4 dB rel). 레퍼런스도 둘이 같다.
   ⚠db 상수는 크게 다르다(−19 vs −10) — 피크 정규화 기준이라 크레스트 차(10.4 vs 18.9dB)를
   상쇄해야 하기 때문. **숫자가 아니라 실효 RMS를 맞출 것.**

**미결로 남긴 것**
- **콤보 음정 상승**: 이 녹화엔 큰 콤보가 없다. 삭제 4회가 매번 **정확히 같은 음정**(904→1550Hz)
  이지만, **표본에 콤보가 안 나왔으니 "레퍼런스에 사다리가 없다"고는 말할 수 없다.**
  판단하려면 연쇄가 크게 터지는 녹화가 필요하다. 우리 사다리는 일단 유지(연쇄가 이름값이다).
- **레벨 위계를 좁힐지**: 레퍼런스는 전 SFX가 1dB 안인데 우리는 `clear`가 `place`보다 +7.4dB다.
  우리는 삭제가 더 드물어서 강조가 맞을 수도 있다 — 유저 판단 대기(권장은 3~4dB로 축소).

검증: `audio_probe` **12검사 PASS**(⑧ 집기 배선 신설 — 봇은 `_place_piece`를 직접 불러서
grab이 패스 A~C에 안 나오므로, 실제 `_pick_up` 경로를 타는 검사를 따로 뒀다. 안 그러면 배선이
끊겨도 초록이다) · `haptic_probe` 11검사 PASS · regress 골든 byte-identical · 뱅크 30.1KB.
글라이드 실측: 집기 474→689Hz 상승, 착지 818→517Hz 하강.

## 11. R4 — 사람 귀 판정 1차 반영 (2026-07-31)

첫 청취 피드백 셋: ①**전자음 같고 은은하다** ②**타격감이 없고 삭제음이 비어 보인다**(레퍼런스는
점수 오르는 소리도 있다) ③**실패 시점에 짧은 소리를 원한다**.

| 지적 | 원인(실측) | 조치 |
|---|---|---|
| 은은함 | 레퍼런스는 전 SFX가 **−8.3~−9.6dBFS 한 줌**에 납작한데 우리는 훨씬 조용하고 위계만 넓었다(집기는 **11.5dB 아래**) | 전체 5~8dB 상향 + 위계 축소. 합산 피크는 리미터가 받는다 |
| 전자음 | 순수 사인 합이라 배음이 정확히 부분음뿐 | **tanh 소프트 포화** 추가(clear 0.35 · fail 0.20) — 배음을 채워 두께를 만든다 |
| 타격감·비어 있음 | 삭제음이 **단층 종소리** 하나. 레퍼런스는 904Hz 타격 + 40ms 뒤 1550Hz 광택의 **2단 사건** | `clear`를 2층으로 재작성 + 3.4kHz 잡음 트랜지언트. 크레스트 15.5→**11.4dB**(밀도↑), 중심 660→**779Hz** |
| 점수 소리 없음 | 롤업에 소리가 안 붙어 있었다 | **`score` 신설** — 1.76kHz 카운터 틱. ⚠캠페인은 `clear_score()`가 0이라 롤업이 없다 → **무한 모드에서만** 울린다(화면에 구르는 숫자가 있는 바로 그때) |
| 실패 무음 | — | **`fail` 신설** — 840→521Hz 하강 2층. **"손실엔 무음" 결정을 유저 요청으로 뒤집은 것**(오디오만. 햅틱은 그대로 무진동 — 실패에 진동까지 얹으면 벌주는 느낌이 된다) |

**⚠같은 병이 세 번째로 재발했다**: `fail`을 620→380Hz로 잡았더니 에너지의 29%가 300Hz 아래로
깔려 폰 시뮬 −8.9dB. 840→521Hz로 올려 −4.7dB. **새 단어를 만들 때마다 폰 시뮬을 돌릴 것.**

**죽은 값 하나 발견**: 아르페지오가 `clear` 이름으로 발화돼서 `SFX_WORDS["fanfare"].db`가 아무 데도
안 쓰이고 있었다. 4음이 겹쳐 리미터를 세게 때리던 원인이기도 하다(프리뷰 클립 8샘플).
→ 큐 항목에 `db`를 실어 넘기고 `_sfx_fire`가 오버라이드를 받게 했다. fanfare = −9dB.

**프로브가 진짜 결함을 잡았다**: `fail` 배선 검사를 처음엔 `pending_core_dead = true`로 세워
`_end_turn`을 태웠는데 **0발**이 나왔다 — `_end_turn` 안의 `advance_step`이
`pending_core_dead = core_hp <= 0`으로 **재계산**해 플래그를 덮기 때문. `core_hp = 0`으로 고쳐야
실제 경로를 탄다. ⚠**grab·score·fail 셋 다 봇 패스(A~C)에선 한 발도 안 울린다** — 배선 검사를
따로 두지 않으면 호출부가 통째로 빠져 있어도 프로브가 조용히 초록이 된다.

검증: `audio_probe` **13검사 PASS**(⑨점수·⑩실패 배선 신설) · `haptic_probe` 11검사 PASS ·
regress 골든 byte-identical · 전 파일 클리핑 0 · 뱅크 50.6KB. 폰 시뮬 전 단어 경고 없음.

## 12. R5 — "메인 플레이가 안 바뀐 것 같다" (2026-07-31)

R4에서 레벨을 3~6dB 올렸는데 유저 판정은 **"실패음만 확인되고 메인 플레이는 모르겠다"**.
버스 피크를 재보니 적용은 확실했다(place −11.4→−6.9 · chain −18.6→−12.6 · clear −7.4→−4.4).
그러면 **레벨은 올랐는데 성격이 안 바뀐 것**이다. 원인을 포락선에서 찾았다.

**몸통/어택 에너지비**(30~120ms 에너지 ÷ 0~30ms 에너지):

| | 레퍼런스 | R4 우리 | R5 우리 |
|---|---|---|---|
| 착지 | **1.76** | **0.08** | **0.86** |
| 집기 | 3.64 | 0.15 | 0.51 |

**R4까지 `place`는 30ms 뒤 에너지가 어택의 8%뿐이었다 = 몸통 없는 딸깍.** 그래서 볼륨을 아무리
올려도 "틱"으로만 들렸다 — **타격감은 세기가 아니라 몸통에서 온다.** 메인 플레이에서 가장 많이
들리는 소리가 `place`(14수당 14회)라 이게 전체 인상을 지배했다.

조치 = **울림 층 추가**. 1층은 하강/상승 제스처(짧게), 2층은 착지 주파수에서 **길게 우는 울림**
(글라이드 없음, τ 0.15~0.20). `place` 85→420ms, `grab` 75→340ms.
⚠레퍼런스는 크레스트 18.1dB(뾰족함)를 **유지하면서** 몸통이 크다 = 울림은 **길되 조용해야** 한다.
그래서 진폭이 아니라 τ를 늘렸다(진폭을 올리면 크레스트가 무너져 펀치가 죽는다).

동시 보이스는 사람 템포에서 최대 2, 스트레스에서 3 — 꼬리가 길어져도 진흙 위험 없음(사람 템포
1.8초/수라 꼬리가 안 겹친다). 뱅크 50.6→76.4KB.

검증: `audio_probe` 13검사 PASS · regress 골든 byte-identical · 클리핑 0 · 폰 시뮬 경고 없음.

## 13. R7 — 합성을 버리고 CC0 샘플로 (2026-07-31)

R1~R6은 사인 부분음을 런타임 합성했고 6라운드를 튜닝했지만 유저 판정은 계속 **"전자음 같다"**였다.
CC0 팩 391개를 우리 기준(폰 통과·대역·밀도)으로 심사해 후보를 추렸고, 유저가 **`pop3`·`pop9`**를
골랐다. **§5의 '에셋 파일 0개'는 여기서 폐기된다.**

**⚠지표의 교훈 — '몸통'과 '밀도'를 혼동했다.** R5에서 나는 "타격감 = 긴 꼬리(몸통비)"라 결론냈는데,
유저가 고른 pop은 **몸통비 0.00**이다. 대신 크레스트가 낮다(9~11dB) = **짧지만 꽉 참**. 즉 타격감은
길이가 아니라 밀도였다. 지표는 후보를 좁힐 뿐 **판정은 귀가 한다**.

**한 가족에서 전 어휘를 뽑는다** — 음원은 파형 **둘뿐**(합계 19KB):

| | 파형 | 쓰이는 곳 |
|---|---|---|
| `pop_low` (775Hz, 크레스트 10.8dB) | 따뜻·꽉 참 | place · clear · fail |
| `pop_high` (1034Hz, 크레스트 14.6dB) | 밝고 가벼움 | grab · chain · clear2 · score · tap |

음정(`pitch_scale`)·레벨(`db`)·층(지연 발화)으로만 갈리므로 **통일감이 공짜로 생긴다.**
삭제음은 low 타격 + 40ms 뒤 high(+9반음) **2단**(레퍼런스 구조 그대로).

**유지된 것 — 배관은 하나도 안 바뀌었다**: `_fb` 접점 · FB_MAP · 두 믹서 분리 · 5음계 사다리 ·
보이스 8 · 간격/예산 · 런타임 SFX 버스+리미터 · 프로브 13검사 · 폰 시뮬. 바뀐 건
`_sfx_build_bank()`가 계산 대신 `load()`를 하는 것뿐이다.
**합성 코드(`_sfx_render`/`_sfx_layer`/`_sfx_lcg`, 약 200줄)는 지웠다** — git C104~C109에 남아 있고,
"나중에 쓸지 몰라서" 남기는 죽은 코드가 제일 잘 썩는다.

**⚠Godot 함정 — WAV는 기본 QOA 압축으로 임포트된다.** 게임 재생은 멀쩡하지만 `AudioStreamWAV.data`가
PCM이 아니게 되어 bake 도구가 **쓰레기 파일을 만들었다**(경고: "Saving IMA_ADPCM and QOA samples is
not supported yet"). `.import`의 `compress/mode=0`으로 무압축 고정. 파형은 19KB뿐이라 손해가 없다.

**⚠프로브가 또 잡았다**: 삭제음 둘째 층을 `chain` 이름으로 발화시켰더니 사다리 분석이 그걸 계단으로
세어 **역행 2회**로 읽었다(9 → 0). 내부 전용 어휘 `clear2`로 분리해 해소.

**라이선스**: CC0 1.0(퍼블릭 도메인) — 상업 사용 가능, **표기 의무 없음** → RELEASE.md·심사 표면 무변화.
출처는 `sfx/CREDITS.txt`에 기록한다(의무가 아니라 **추적용** — 나중에 교체·재라이선스를 판단하려면
어디서 왔는지 알아야 한다).

검증: `audio_probe` 13검사 PASS · regress 골든 **byte-identical** · 클리핑 0 · 폰 시뮬 경고 없음
(low −3.8dB · high −2.3dB) · 버스 실측 전 어휘 발화(clear는 보이스 2개 = 2단 확인).
⚠검사 ④의 이번 표본은 연쇄 런이 짧아(최장 `[0]`) 사다리 검증력이 약하다 — 사다리를 크게 바꿀 땐
콤보가 큰 판으로 다시 볼 것.

## 14. R8 — 어휘별 기본 음정(base) (2026-07-31)

유저: **"놓기를 더 높은 소리로 하는 건 어때?"**

**파형을 맞바꾸지 않았다.** `pop_high`는 크레스트가 14.6dB로 `pop_low`(10.8dB)보다 얇아서, 가장 잦은
`place`를 거기 얹으면 §12에서 고친 "타격감 없음"으로 되돌아간다. **밀도는 low로 지키고 음정만 올린다.**

그래서 `SFX_WORDS`에 **`base`(단어별 기본 음정, 반음)**를 새로 뒀다. 사다리(`semi`)와 분리한 이유:
`clear`·`chain`은 `semi`가 콤보/계단을 나르므로 거기에 기본값을 섞으면 사다리 시작점이 밀린다.
**`base` = 이 단어의 자리, `semi` = 지금 몇 번째.**

실측 결과(재생 음정 기준):

| 어휘 | 파형 | base | 지배 F | 길이 |
|---|---|---|---|---|
| **place** | low | **+7** | **1163Hz** | 59ms |
| chain | high | 0 | 1034Hz | 130ms |
| **grab** | high | **−2** | **904Hz** | 146ms |
| clear(다운비트) | low | 0 | 775Hz | 89ms |

놓기가 집기보다 **4반음 위**, 그리고 삭제 다운비트가 가장 낮아 무게를 받친다.
⚠**음을 올리면 샘플이 그만큼 짧아진다** — place는 89→59ms(+7반음 = 길이 67%). 유저가 고른
`pop3`가 58ms였으니 같은 범위다.

검증: `audio_probe` 13검사 PASS · regress 골든 byte-identical · 폰 통과 전 어휘 −0.8~−2.9dB.

## 15. R9 — 보상음을 '팡'으로 (2026-07-31)

유저: **"블록 터지고 적 죽이는 사운드가 너무 잔잔해. 더 팡 터지고 기분 좋았으면."**

**pop은 본질적으로 얌전한 소리다.** 놓기·집기처럼 작고 잦은 동작엔 맞지만 보상엔 부족하다.
→ **동작 계열(pop)과 보상 계열을 갈랐다.** 파형 넷:

| 파형 | 특성 | 쓰이는 곳 |
|---|---|---|
| `pop_low` 775Hz | 따뜻·꽉 참 | place · fail |
| `pop_high` 1034Hz | 밝고 가벼움 | grab · score · tap |
| **`burst`** 3146Hz | 어택 22dB · **2~5kHz에 58%** | **clear**(줄 삭제 타격) |
| **`sparkle`** 5156Hz | 2~5kHz에 52% | **chain**(적 처치) · clear2 · fanfare |

'팡'의 음향 조건 넷으로 391개를 전수 심사했다: ①강한 어택 ②밝음(1.5~5kHz = 폰이 제일 잘 내는 대역)
③광대역(순음이면 '터진다'로 안 들림) ④폰 통과. `slam`·`shot` 계열도 통과했지만 **"기분 좋은"이
아니라서** 뺐다 — 보상은 터지기만 하면 안 되고 **반짝여야** 한다. 그래서 2층(타격+반짝임)이다.

**아르페지오도 바꿨다** — `clear`(타격) 4연타는 거칠어서 `clear2`(반짝임)로. 판을 닫는 소리는
기분 좋게 울려야 한다.

**⚠레벨은 실효 RMS가 아니라 '보이스 피크'로 잡았다.** `burst`는 크레스트가 25dB라 RMS를 다른
단어와 맞추려면 +8.7dB가 필요한데 그러면 피크가 **+8dBFS**가 되어 리미터를 뭉갠다. 짧고 뾰족한
소리는 peak이 체감 세기를 지배한다 → clear −2.7 / clear2 −9 / chain −12 dBFS.
동작 계열(pop)은 여전히 실효 RMS 기준이다. **두 기준이 한 표에 섞여 있으니 주석을 볼 것.**

버스 실측: `clear` −9.2 → **−5.9dB**(보이스 2개 = 타격+반짝임).

검증: `audio_probe` 13검사 PASS · regress 골든 byte-identical · 클리핑 0 · 동시 보이스 최대 2.
⚠프로브 검사 ⑤가 아르페지오를 `clear` 이름으로 세고 있어 한 번 FAIL했다(어휘를 바꾸면 **검사도
같이 갱신**해야 한다 — 배선 검사의 이름 의존은 이런 식으로 깨진다).

## 16. R10 — 레퍼런스 스펙트럼 매칭 (2026-07-31)

유저: **"부족해. 그리고 사운드가 좀 거슬려. 레퍼런스 분석해서 최대한 비슷한 걸로 찾아줘."**

**방법을 바꿨다.** R9까지는 내가 정한 임계값(밝기·어택·폰통과)으로 걸렀는데 그 기준이 유저 귀와
어긋났다. 이번엔 **레퍼런스 클립 자체를 목표 벡터로** 삼았다:
녹화에서 소리 넷을 잘라내(`REF_grab/place/clear/gem`) 특징 벡터(1/3옥타브 밴드 로그 에너지 +
길이 + 크레스트, RMS 정규화)를 만들고, **CC0 후보 391개를 그 벡터와의 거리로 줄 세웠다.**

**거슬림의 정체가 수치로 나왔다:**

| | 스펙트럼 중심 | 2~5kHz 비중 | REF_clear와의 거리 순위 |
|---|---|---|---|
| **REF_clear(레퍼런스)** | 1479Hz | 22.2% | — |
| R9 `burst` | 3146Hz | **58.3%** | 26 / 380 |
| R9 `sparkle` | 5156Hz | **52.0%** | **258 / 380** ← 꼴찌권 |

우리 보상음이 레퍼런스보다 **2~3배 밝았다.** `sparkle`은 네 레퍼런스 전부에서 하위권이었다.

**교체(매칭 1위):**

| 어휘 | 파형 | 중심 | 근거 |
|---|---|---|---|
| `clear` | **`hit`**(OGA wooden_02) | **1464Hz** | REF_clear 1479Hz와 **거의 일치** |
| `clear2` | **`shine`**(Kenney switch_01) | 1758Hz | 40ms 뒤 광택(2~5kHz 33%) |
| `chain` | `pop_high`로 **되돌림** | 964Hz | 연쇄는 판당 5~9발 — 밝은 파형이 가장 먼저 귀를 피곤하게 한다. 964Hz는 REF 삭제음 1층(904Hz)과 같은 자리 |

⚠**둘째 층의 +9반음을 뺐다.** R9까지는 두 층이 같은 파형이라 음정으로 올렸는데, 이제 둘째 층
파형이 **이미** 더 높다(1758 > 1464). 그대로 올리면 2462Hz로 다시 날카로워진다.

검증: `audio_probe` 13검사 PASS · regress 골든 byte-identical · 동시 보이스 최대 2 ·
버스 실측 `clear` −5.1dB(보이스 2개). ⚠프리뷰 `shine__clear2.wav`에 클립 1샘플이 있는데 이건
**원본 소스가 0.0dBFS**여서다(Kenney 파일) — 게임에선 −11dB 트림이라 무해.

**남은 판단**: 레퍼런스 클립(`REF_*.wav`)을 프리뷰 폴더에 같이 뒀다. 우리 것과 **직접 A/B**해서
아직 먼 게 있으면 그 이름을 알려주면 된다 — 매칭 표에서 다음 후보를 바로 꺼낼 수 있다.

## 17. R11 — 유리/크리스털 + 60ms 지연 결함 (2026-07-31)

유저: **"라인 지울 때 나는 소리가 나무 소리 같아서 방향성에 안 맞아. 소리 나는 빈도도 레퍼런스에
비해 훨씬 적어."** 두 지적이 성질이 다른 문제로 갈렸다 — 하나는 파일, 하나는 배선.

### ① 나무 — 매칭 벡터가 재질을 안 담는다

R10이 고른 1위는 **문자 그대로 나무 샘플**(OGA `wooden_02`)이었다. 1/3옥타브 밴드·길이·크레스트로
스펙트럼 봉투는 맞췄지만 **재질(material)은 그 벡터에 없다.** 유저 귀는 정확했다.

→ 방법을 한 칸 고쳤다: **재질은 사람이 먼저 정하고**(유저 = 유리/크리스털 → 후보 41개),
**그 안에서 레퍼런스 거리로 기계가 고른다.** 결과가 거리도 더 가깝다:

| | 1층 | 2층 | 중심 | 2~5kHz | REF_clear 거리 |
|---|---|---|---|---|---|
| REF(Block Blast) | — | — | 2029Hz | 28% | — |
| R10 (나무) | `hit` wooden_02 | `shine` switch_01 | 1325Hz | 5% | 10.87 |
| **R11 (유리)** | **`glass`** impactGlass_light_002 | **`chime`** pluck_001 | **2327Hz** | **27%** | **8.14** |

⚠**R9의 함정을 다시 밟지 않았다**: 유리 후보 중엔 2~5kHz가 88~100%인 것들이 상위에 있는데
(`glass_02`·`glass_005`) 그게 R9 sparkle(52%)이 "거슬린다"로 기각된 바로 그 대역이다. 27%로 잡았다.

### ② 60ms 지연 — 진짜 결함은 음색이 아니었다

파일 봉투를 5ms 프레임으로 뜨니 **`hit.wav` 앞에 60ms 무음, `shine.wav` 앞에 25ms**가 있었다.

- 삭제음이 **섬광보다 3.6프레임 늦게** 울렸다 — §4의 "clear = 섬광과 같은 프레임"이 안 지켜지고 있었다.
- 2층 구조가 통째로 뭉개져 있었다: 1층 실어택 60ms vs 2층(예약 40ms + 자체 리드 25ms = 65ms)
  → **5ms 차**. "타격 + 40ms 뒤 광택"은 코드엔 있었지만 귀엔 없었다.

지금 둘 다 리드 0ms이고, 구운 `clear_2layer.wav` 봉투에서 2층이 **정확히 40ms 지점**에 보인다.
→ **교훈: 샘플을 새로 넣을 땐 리드를 재고 자른다.** 스펙트럼 매칭은 시간축 정렬을 안 본다.

### ③ 빈도 — 총량이 아니라 분포다

임시 프로브로 블라스트 연출 창(`resolving`) 안의 발화를 떴다(시드 20250123, 사람 템포):

| 창 길이 | 발화 | 창 시작 기준 시각 |
|---|---|---|
| 0.85초 | 3발 | .17 .22 .52 |
| 1.48초 | 7발 | .27 .32 .62 .70 .98 1.07 1.15 |
| 1.48초 | 5발 | .37 .42 .72 .80 1.08 |
| **1.70초** | **2발** | .47 .52 → 이후 **1.18초 무음** |

**초당 발화는 우리가 더 많다** — 사람 템포 1.07/s(봇은 `grab`을 안 태우니 실플레이 ~1.5/s),
레퍼런스는 42이벤트/43.6초 = 0.96/s. 그러니 "소리를 늘려라"가 답이 아니다.

진짜 차이는 **연출 길이**다. 레퍼런스 삭제음은 320ms짜리 사건이고 그 안이 꽉 찼는데, 우리 삭제
연출은 **0.85~1.70초**로 4~5배 길고 **양 끝이 비어 있다**(시작 0.17~0.47초 무음, 끝 0.3~1.2초 무음).

**화면은 움직이는데 소리가 아예 없는 사건들**(다음 라운드 작업 목록):

| 사건 | 위치 | 비고 |
|---|---|---|
| 로켓 순차 발사 | `BLAST_RING_DELAY` 0.26s 간격 | '연쇄'의 몸통인데 무음. `chain`은 **적이 죽을 때만** 운다 |
| 보석 카운터 도착 | `Main.gd` `gem_flights` 만료 | **레퍼런스에서 가장 밀도 높은 소리**(REF_gem 500ms 연속 열차)가 우리는 무음 |
| 전멸 클라이맥스 | `_fire_climax` | `_fb` 호출 0개 — 게임에서 제일 큰 순간이 무음 |
| 비살상 피격 · 넉백 · 칭찬 단어 팝인 | — | 전부 무음 |

→ **연출을 먼저 고칠 필요는 없다**(유저 질문에 대한 답). 연출이 빈약한 게 아니라 이미 있는 beat에
소리가 안 붙어 있다. 붙일 자리는 다 있고, 그러고도 늘어지면 그때 `BLAST_RING_DELAY`를 본다.

검증: `audio_probe` **13검사 PASS** · 폰 시뮬 −1.5dB(경고 없음) · 뱅크 55KB→**21KB**(리드·꼬리 절단) ·
`clear_2layer` 봉투에서 2층 40ms 확인. regress 골든은 R12에서 한꺼번에 확인했다(§18).

A/B용으로 `build/audio_preview/CLEAR_{A_glass,B_crystal,C_deep}.wav`를 구워 뒀다(각 3연타,
게임과 같은 2층·레벨). A가 채택안이고 B는 더 맑은 쪽, C는 더 낮고 차분한 쪽이다.

## 18. R12 — 카지노 칩으로 확정 + 선정 방식을 바꿈 (2026-08-01)

R11(유리/크리스털)에 대한 유저 판정: **"약간 에러 사운드처럼 느껴져. 샘플 몇 개 출력해줘,
내가 골라볼게."** 그리고 **`chip-lay-3`(카지노 칩 놓는 소리)를 골랐다.**

**"에러 사운드 같다"는 근거가 있었다.** R11의 2층 `pluck_001`은 Kenney **Interface Sounds** 팩
소속이고, 그 팩엔 `error_001~008`이 같은 신스 보이스로 들어 있다. UI 피드백용으로 디자인된
음색이라 "무언가 잘못됐다"는 관용구를 그대로 갖고 온다. 1층 유리 '깡'도 파손 쪽으로 읽힌다.
→ **보상음은 출신 팩의 용도까지 봐야 한다.** 스펙트럼이 맞아도 관용구가 어긋나면 진다.

### 선정 방식을 바꿨다 — 이게 이번의 본론

| 라운드 | 방식 | 결과 |
|---|---|---|
| R9 | 내가 정한 임계값(밝기·어택·폰통과)으로 거름 | "거슬린다" |
| R10 | 레퍼런스 벡터 거리 **1위 채택** | "나무 소리다" |
| R11 | 재질을 유저가 정하고 그 안에서 **거리 1위 채택** | "에러 사운드 같다" |
| **R12** | 기계는 **명백히 못 쓰는 것만** 걷어내고 재질마다 2개씩 **넓게 구워 사람이 고름** | **채택** |

거리 1위는 세 번 다 틀렸다. 지금 남은 기계 필터는 둘뿐이다 — 폰에서 사라짐(−6dB 초과) ·
2~5kHz 55% 초과(R9 '거슬림' 대역). 나머지는 전부 귀로 넘긴다. 도구가 `build/audio_pick/`에
`SNN_<재질>.wav` 20개 + `S00_현재` + `S99_레퍼런스`를 게임과 같은 레벨로 3연타씩 굽는다.

외부에서 CC0 팩 셋을 새로 받아왔다: Kenney **Casino Audio**(칩) · OGA **100 CC0 SFX #2** ·
OGA **Bell dings/chimes**. Kenney Digital Audio도 받았지만 8비트 전자음이라 뺐다(기각된 방향).

⚠**조용한 탈락을 하나 잡았다.** 후보 스캔이 `.wav`를 ffmpeg 없이 바로 읽는데 스테레오·24bit
파일에서 `struct` 예외가 났고, 그게 `try/except`에 삼켜져 **종·차임 계열이 통째로 목록에서
사라지고 있었다.** 전부 ffmpeg를 통과시키게 고쳤다. → **필터링 파이프라인의 `except: continue`는
"탈락"과 "못 읽음"을 구분해야 한다.**

### 채택안

| | 파형 | 중심 | 길이 | 비고 |
|---|---|---|---|---|
| `clear` 1층 | **`chip_low`** chip-lay-3 | 748Hz | 157ms | 유저 선택 |
| `clear2` 2층 | **`chip_high`** chips-collide-1 | 4994Hz | 104ms | 같은 팩 = 통일감(§13) |

2층은 같은 카지노 팩 12개를 재서 골랐다. 단독으론 5142Hz로 밝지만 **−11dB로 1층 아래 깔리면
합이 중심 2013Hz**가 되어 REF_clear(2029Hz)와 거의 겹친다. 2~5kHz 13% · 5kHz 위 17%.

⚠**아르페지오(fanfare)를 `clear2`에서 낮은 파형으로 되돌렸다.** `chip_high`를 +12반음 올리면
아르페지오 중심이 **9355Hz · 5kHz 위 93%** — R9에서 기각된 바로 그 소리가 된다. 낮은 파형으로
4연타하면 1329Hz · 5kHz 위 2%다. **파형을 바꾸면 아르페지오를 반드시 다시 재라.**
같이 고친 것: 아르페지오가 **`fanfare` 자기 이름으로 로그에 남는다**(뱅크에 자기 항목을 줬다).
전엔 `clear2` 이름을 빌려 써서 삭제 광택층과 구분이 안 됐고, 이름에 의존한 프로브 검사가
R9에서 실제로 한 번 조용히 깨졌다. 프로브 ⑤·⑦·`WORD_DUR`도 같이 갱신했다.

⚠**감수한 값: 칩의 저역.** `chip_low`는 에너지의 **30%가 300Hz 아래**라 폰 시뮬 **−4.9dB**로
어휘 중 손실이 가장 크다(다른 단어는 −1~−2dB). §9 P0와 같은 위험 신호지만, 음정을 +7반음까지
올려도 −3.5dB뿐이고 길이가 157→105ms로 짧아져 **이득이 작다.** 유저가 고른 음색을 지키는 쪽을
택했다. `chip_low__clear_PHONE.wav`로 폰에서 어떻게 들리는지 확인할 수 있고, 얇으면 하이패스로
다듬는 게 다음 수다.

검증: `audio_probe` **13검사 PASS**(main 병합 후 재실행 포함) · 전 프리뷰 클리핑 0 ·
동시 보이스 최대 2 · 뱅크 48KB · `tools/regress.gd` 골든 **byte-identical**.

⚠**regress를 안전하게 돌리는 법**(그동안 못 돌리고 미뤄 온 것): 하네스에 `persist_enabled=false`
가드가 없어 14판 전승 결과가 실유저 세이브에 각인된다([[campaign-save-reset-gotchas]]).
**세이브를 복사해 두고 돌린 뒤 되돌리면 된다** — 이번에 그렇게 확인했고, 실제로 각인이
일어났다(`campaign.save` 0x00000000 → 0x00000001). 경로는
`~/Library/Application Support/Godot/app_userdata/BlockCastle/{campaign,endless}.save`.
근본 수정(가드 추가)은 하네스 소유 트랙 몫으로 남긴다.

## 19. R13 — UI 탭 전면 배선 (2026-08-01)

유저: **"우선 버튼 클릭음은 모두 필요해."**

**`tap`은 어휘표에도 FB_MAP에도 있었지만 호출부가 딱 하나였다** — 설정에서 소리를 *켤 때* 확인용 한 발.
허브·진행화면·결과팝업·설정 모달·기어·안드로이드 뒤로가기 전부 무음이었다. 어휘가 있으니 표만
보면 배선된 줄 알았다 = **§11이 경고한 '조용한 초록'이 UI에서 그대로 일어나 있었다**(봇 패스는
버튼을 한 번도 안 누른다).

### 어휘 넷 — 파형은 안 늘렸다

| 어휘 | base | 중심 | 쓰이는 곳 |
|---|---|---|---|
| `tap_go` | **+7** | 1471Hz | 진입·확정 — Adventure/Classic·PLAY·재도전·광고 이어하기·발사 |
| `tap` | 0 | 982Hz | 중립 — 기어·토글·트로피·조준 |
| `tap_back` | **−5** | 735Hz | 뒤로·닫기·홈 — ESC·안드로이드 뒤로가기 포함 |
| `tap_off` | **−8** | 618Hz | 잠김·무반응 — 잠긴 Classic, 표적 없는 발사 |

전부 `pop_high` **한 파형**이고 `base`(§14)로만 갈린다. UI는 판 안의 사건이 아니므로 새 음색을
주면 세계에 없던 물건이 하나 더 생긴다. 대신 **방향이 뜻을 나른다**(§10 레퍼런스: 집기=상승·
착지=하강) — 진입은 위, 뒤로는 아래.

⚠**음정은 5음계 가족 안에서 골랐다**(+7 5도 위 · −5 4도 아래 · −8 6도 아래). 연쇄 사다리와 같은
음률이라 UI 소리가 판의 소리와 협화한다. 크로매틱으로 잡으면 UI만 조성 밖에서 논다.

### 세 가지 판단

1. **잠긴 버튼(`tap_off`)** — 지금까지 완전 무음이라 '고장'으로 읽혔다. 경고음을 주면 §4가 기각한
   '탐색을 벌하는 소리'가 되므로, **낮고 조용한 중립음** 하나로 "눌리긴 했고, 여긴 아직 아니다"만
   말한다. 화면 동작은 그대로 무반응이다.
2. **키·하드웨어 경로도 같이 태웠다** — SPACE/ESC/E/L과 **안드로이드 뒤로가기**(`_notification`).
   버튼에만 달면 같은 행동이 입력 방식에 따라 다르게 들리고, 특히 모바일에선 뒤로가기가 화면
   버튼보다 자주 눌려서 **폰에서만 UI가 무음**이 된다.
3. **사운드 토글을 끌 때도 한 발** — `_sfx`가 `sound_on`을 보고 조기 반환하므로 **플래그를 뒤집기
   전에** 울려야 한다. 켤 때만 소리가 나던 게 R12까지의 상태였고, 끄는 탭만 응답이 없었다.

### ⚠계측 도구가 또 틀렸다 — 네 번째

`tap_off`를 처음엔 −9반음으로 잡고 폰 시뮬을 돌렸는데, 그 시뮬 자체가 틀려 있었다.
1/12옥타브 **로그 간격** Goertzel로 스펙트럼을 떴는데 로그 간격은 대역폭 보정이 없다(옥타브당
빈 개수는 같은데 실제 대역폭은 두 배씩 넓어진다) → 저역을 체계적으로 과대평가한다.
`chip_high`를 **535Hz**로 읽었다 — 실제는 5147Hz다(§18의 실측 4994Hz와 일치).

→ **선형 간격 FFT로 교체**하고 나서야 자가 맞았다. §9의 해닝창 사고, §16의 벡터 거리와 같은 종류다:
**계측이 이상한 결론을 내면 대상이 아니라 자를 먼저 의심할 것.**

교정된 자로 음정 사다리를 재니 **무릎이 −7~−8반음**이었다(−9에서 −3.6dB, −12에서 −6.0dB).
가장 조용한 단어가 폰에서 가장 많이 깎이면 '무음'으로 되돌아가므로 `tap_off`를 −8로 올리고
레벨로 절반쯤 되갚았다(−12 → −10.5dB).

### 도구를 저장소에 넣었다

§11이 "새 어휘를 만들 때마다 폰 시뮬을 돌릴 것"이라 못박는데, **그 도구가 저장소에 없었다** —
R1~R12 동안 매번 임시로 만들어 쓰고 버려졌고, 그래서 같은 병이 세 번 재발했다.
→ `tools/phone_sim.py`(stdlib만, 의존성 0). **어휘 표는 `Main.gd`에서 파싱한다** — 하드코딩하면
상수를 고칠 때마다 조용히 어긋난다.

실측(전 12어휘): 탭 넷 −0.1 / −0.5 / −1.6 / −3.0dB로 경고선(−6) 안쪽.
⚠이 자는 폐기된 옛 도구와 **눈금이 같지 않다** — `chip_low`를 −7.6dB로 읽는다(옛 도구 −4.9dB).
§18에서 **유저가 알고 감수한 칩 저역**이라 새 결함이 아니다. 절대값이 아니라 어휘 간 상대 비교로 쓸 것.

검증: `audio_probe` **17검사 PASS**(⑪~⑭ 신설 — 어휘를 직접 때리지 않고 **진짜 입력 이벤트를
`_input`에 먹여** 기어·닫기·진입·잠김을 재고, 소리와 화면 전환이 일치하는지까지 본다) ·
`tools/regress.gd` 골든 **byte-identical**(521줄) · 폰 시뮬 경고 없음 · 프리뷰 `ui_taps.wav`.

> 곁다리 발견 둘(이번 변경과 무관, 손대지 않았다):
> ① **허브에 설정 진입점이 없다.** `_input`의 주석은 "허브에서도 기어로 열린다(C80)"라고 하는데
>    기어를 그리는 건 `_draw_hud`(플레이 화면)뿐이고, 허브 입력 분기엔 `gear_rect` 히트테스트가
>    없다. 안드로이드 뒤로가기로도 허브에선 설정이 아니라 종료로 간다.
> ② **비행기 픽업만 `_fb`가 없다.** 보석 낚아채기는 `chain`을 울리는데(`_apply_hit`) 같은 문법인
>    비행기 픽업 분기는 무음이다 — §20 B-8.

## 20. R14 — 블라스트 창 채우기 + 누수음 (2026-08-01)

유저: **"[누수음] 넣어줘. 그리고 시작해줘."** 순서는 R13에서 합의한 대로 — **기존 파형으로 되는
자리부터 배선하고, 그러고도 비면 새 파형(whoosh)을 굽는다.**

### 붙인 것 넷

| 어휘 | 파형 | 자리 | 설계 |
|---|---|---|---|
| **`climax`** | chip_low(0) **3층** | `_fire_climax` | 전멸 = clear의 '더 큰 판' |
| **`praise`** | chip_high(**−5**) | 칭찬 단어 팝인 | 창 앞머리 3박의 셋째 |
| **`leak`** | pop_low(**+3**) | `_reveal_leaks` | 누수 −1 |
| — | (기존 `chain`) | 비행기 픽업 | R13 곁다리 발견의 한 줄 |

**⚠전멸을 음정으로 키우지 않았다.** `chip_low`는 **내릴수록 폰에서 죽는다** — 폰 시뮬로 재니
0반음 −7.6dB인데 −5반음이면 **−9.2dB**다. "크다 = 낮다"로 잡으면 폰에서 제일 큰 순간이 제일
많이 사라진다. 레퍼런스도 레벨 위계가 거의 없고 **음색·제스처·층수**로 차별화한다(§10)
→ climax는 clear와 **같은 음정에 3층**(타격 + 45ms 광택 + 135ms **상승** 광택)으로 커진다.
⚠둘째 광택을 +12로 올리면 안 된다 — chip_high가 10.3kHz로 가서 폰 −6.8dB, R9에서 기각된
그 대역으로 되돌아간다. **+5까지가 안전선**이다.

**누수(`leak`) = "손실엔 무음"을 뒤집은 두 번째 자리**(첫째는 `fail`, §11). 벌이 아니라 알림이
되도록 `fail`과 **같은 파형에 더 높고(+3, 덜 최종적) 더 조용하게**(−13dB) 잡았다. 햅틱은
그대로 무진동이다. ⚠**한 스텝에 여러 열이 새도 한 발만 운다** — 호출부를 루프 밖에 뒀다.

### ⚠자가 또 거짓 합격을 냈다 — 이번엔 내가 방금 만든 자

창 분포를 재는 계측을 붙이고 돌렸더니 **앞침묵이 전부 0.00초**로 나왔다. "고쳤다"고 쓸 뻔했다.
원인: `_place_piece`와 `_settle` 사이에 프레임이 안 흘러서 **배치음이 창 시작과 정확히 같은
시각에 찍힌다.** 그걸 창 안의 첫 발로 세면 앞침묵이 항상 0이 된다.
경계의 배치음을 빼고 다시 재니 **0.18~0.48초** — §17③의 0.17~0.47과 **사실상 그대로**였다.

> 이번 라운드에서 자를 두 번 의심했고 두 번 다 자가 틀렸다(R13의 로그 간격 FFT, 여기의 경계 표본).
> **계측을 새로 만들면 그 계측부터 검증할 것.** 특히 "고쳐졌다"가 나올 때.

### 실측 — 무엇이 고쳐졌고 무엇이 아닌가

| | R13 이전(§17③) | R14 | |
|---|---|---|---|
| 창당 발화 | 2~7발 | **3~12발** | ✅ 채워짐 |
| 초당 발화(봇) | 1.07/s | **1.66/s** | 레퍼런스 0.96/s를 넘어섬 |
| **앞침묵** | 0.17~0.47s | **0.18~0.48s** | ❌ **안 고쳐짐** |
| 뒤침묵 | 0.3~1.2s | 0.32~0.98s | △ 소폭 |
| 동시 보이스 | 2 | 3 (상한 8) | 여유 |
| 롤링 1초 | 8발 | 12발 (예산 15) | ⚠여유가 줄었다 |

**앞침묵의 정체가 드러났다 — 충전 구간이다.** 조각을 놓고 `charge_dur`(콤보 비례) 동안 화면은
차오르는데 소리가 없다. 내가 붙인 셋은 전부 파괴 **이후**에 울려서 중간만 채웠다.
**이건 B-목록에 아예 없던 자리다**(B-19로 추가). 뒤침묵은 로켓(B-1) 자리 그대로다.

검증: `audio_probe` **22검사 PASS**(⑮~⑱ 신설 — 전멸은 플테 '8'키와 같은 강제 경로로, 픽업은
`_apply_hit`으로, 누수는 **3열 동시**로 세워 한 발만 나는지까지 본다) · regress 골든
**byte-identical** · 폰 시뮬 신규 셋 −7.6 / −1.6 / −1.0dB(climax는 clear와 같은 값 = §18에서
유저가 감수한 칩 저역) · 프리뷰 `climax_3layer.wav` · `leak_and_praise.wav`.

## 21. R15 — 삭제음 재작업 시도, **전부 기각**(2026-08-01~02)

유저: "줄 터지는 사운드 타격감 너무 약해" → 여러 라운드 → **"별로야"**로 종료.
**코드는 통째로 되돌렸다**(C113 상태 유지). 하지만 이번에 나온 **측정 결과는 유효하고, 다음 시도의
출발점**이므로 남긴다. 실험 전체는 `scratchpad/R15_보존/R15_R16_experiment.patch`에 있다.

### 알아낸 것 — 이건 계속 쓸 수 있다

**① 지금 삭제음은 '아령 모양'이다(밴드 구멍).**

| 대역 | <300 | 0.8–1.5k | 1.5–2.5k | 2.5–5k | >5k |
|---|---|---|---|---|---|
| REF_clear(블블) | 2.3 | **26.4** | **38.5** | 23.0 | 3.6 |
| 우리(chip 2층) | **30.6** | 1.0 | 1.0 | 11.8 | **45.0** |

타격이 사는 0.8~2.5kHz가 **2%**다. 30%는 300Hz 아래(폰이 못 냄), 45%는 5kHz 위(얇은 치찰음).
`chip_low` 단독으로는 46.8%가 300Hz 아래고 폰 통과 −7.6dB — 어휘 중 최악이다(§18에서 감수한 값).

**② R9~R10의 모순이 풀렸다.** 그때도 "팡 터지게"였는데 5kHz 위로 올렸다가 "거슬린다"로 기각됐다.
**레퍼런스의 펀치는 밝은 데가 아니라 가운데(1.5~2.5kHz)에 있다.** 밝기 축이 아니라 중역 축이었다.

**③ ⚠레퍼런스에 '리듬'은 없다**(52초 녹화의 **시간 구조**를 처음 뜯어봤다. 그동안 스펙트럼만 봤다):
- 사건 35개 중 **28개(80%)가 한 발**
- 여러 발인 7개도 간격이 **40~45ms** = 리듬이 아니라 **한 방의 두 층**(우리 clear2가 정확히 40ms다)
- 초당 발화는 **레퍼런스 0.99 vs 우리 1.66** — 우리가 이미 더 자주 운다

**④ 대신 5배 길다.** REF 삭제음 **320ms** vs R15의 burst 58ms. 유저의 "짧게 한번 탁 나오고 끝난다"는
비유가 아니라 실측 그대로였다. ⚠**감쇠 봉투로는 길이를 못 늘린다** — 원본이 짧으면 늘릴 꼬리가 없다.
길이는 **울리는 소재를 뒤에 붙여야** 나온다.

**⑤ 라이브러리에 답이 없다.** CC0 팩 1040개를 (짧고·뾰족하고·음정 있고·하강·중역) 기준으로 훑으니
**15개만 통과**했고 전부 이미 기각된 계열(유리·뜯음·금속)이었다. 레퍼런스처럼 0.8k~5kHz에 고르게
퍼진 소리는 단일 녹음이 아니라 **층을 쌓아 설계된 소리**다. 층으로 만들면 분포는 맞출 수 있다
(유리+금속 2층 = 29.6/40.7/27.6, REF 26.4/38.5/23.0) — **분포를 맞춰도 유저 귀는 아니었다.**

**⑥ 레벨 여유는 4dB뿐이다.** clear 실피크 −4.5dBFS, 리미터 천장 −0.5dB. 그 이상 '크게'는
절대 레벨이 아니라 **위계**(나머지를 낮춤)로만 가능하다 — §9 P3와 같은 결론.

**⑦ 전멸은 층을 시간에 퍼뜨리면 안 된다.** R14의 climax는 45·135ms 3층이었는데 화면은
`_fire_climax`가 "확장 링 없이 전부 **동시** 폭발"이다(C90). 유저 지적("5콤보는 한번에 팡")이 정확했다.
⚠`_sfx_queue` 예약은 다음 프레임(최대 16ms 뒤)에 나가 '한 방'이 두 발로 갈라진다 — 같은 프레임에
합치려면 `_sfx_fire` 직행이어야 한다(실측 0.82/0.83 → 0.82/0.82).

### 기각된 것(다시 시도하지 말 것)

`burst`(딸깍+몸통 합성) · 진짜 폭발음(1초+·저역·폰에서 −8~−20dB) · Kenney Sci-fi/OGA Pack2의
텔레포트·레이저 계열 · 유리/금속/나무 꼬리 · 리듬 figure(double·triple·wide tail) · 로켓 착탄 꼬리
(⚠단 이건 **"전보다 좋아졌어"**를 한 번 받았다 — 완전 기각은 아니고 나머지가 별로여서 같이 묻혔다).

### 방법론 교훈

- **자를 두 번 의심했고 두 번 다 자가 틀렸다**: 로그 간격 FFT(저역 과대평가, chip_high를 535Hz로
  읽음 — 실제 5147Hz) · 창 분포의 경계 표본(배치음이 창 시작과 같은 시각에 찍혀 '앞침묵 0.00'이라는
  가짜 합격). **새 계측을 만들면 그 계측부터 검증할 것. 특히 "고쳐졌다"가 나올 때.**
- **플테 A/B 도구는 세 축을 따로 둬야 한다**(음색·길이·리듬·레벨을 섞으면 판정이 안 된다).
  ⚠그리고 **모든 축에 미리듣기를 달 것** — '7'키만 미리듣기가 없어서 유저에게 "아예 소리가 안 난다"로
  읽혔다. 또 모드가 **드문 상황(2줄 이상 동시 삭제)에만 걸려 있어** 1줄 클리어에선 한 발도 안 달라졌다
  ("안 바뀌는 것 같다" = 정확한 관찰이었다). **실험은 흔한 경우에서 달라져야 실험이다.**
- 파일 A/B로는 끝내 판정이 안 났다("감이 안와"). **게임 안에서 키로 바꿔가며 듣는 방식**이 유일하게
  피드백을 뽑아냈다 — 다음 라운드도 그 방식으로 시작할 것.

### 다음에 시도할 것(순서 제안)

1. **꼬리 소재 교체** — R15가 도달한 마지막 후보는 `pluck`(Kenney *Interface* 팩)인데, 그 팩은
   §18에서 "에러 사운드 같다"로 기각된 바로 그 신스 보이스다. 앞부분에 가려 안 드러났을 뿐일 수 있다.
2. **길이를 320ms까지** — 울리는 소재로만 가능(④).
3. **위계로 키우기** — clear +4dB가 한계이므로 place·grab·chain·tap을 −3~6dB 낮추는 쪽(⑥).
4. **외부 소스 확장** — 지금 라이브러리(게임용 UI 팩)엔 답이 없었다(⑤). Sonniss GDC 번들 등
   **실제 녹음 기반** 라이브러리가 남은 미개척지다.

## 22. 남은 것

### A. 무음 beat에 소리 붙이기 — 코드를 전수로 훑은 목록(2026-08-01)

§17③이 셋만 적어 뒀는데, 호출부를 전부 훑으니 더 나왔다. 우선순위순:

| # | 사건 | 위치 | 왜 |
|---|---|---|---|
| **B-0** | **삭제음 재작업** | `SFX_BURST`/`clear` | ⚠**R15 전체 기각**(§21). 유저 판정 "타격감 약함·부딪히는 소리 같음". 측정은 §21에 남아 있다 |
| **B-19** | **충전(charge) 구간** | `charge_dur` · `_begin_resolve` | ⚠**R14에서 새로 드러난 자리.** 창 앞침묵 0.18~0.48초의 정체가 이것이다. 화면은 차오르는데 소리가 없다. 콤보 비례라 길이가 의미를 나른다 |
| B-1 | **로켓 순차 발사**(링마다 0.26s) | `_spawn_muzzle` | '연쇄'의 몸통인데 무음. `chain`은 *적이 죽을 때만* 운다. 뒤침묵 0.98초가 여기 |
| B-2 | **보석 카운터 도착** | `gem_flights` 만료 | 레퍼런스에서 **가장 밀도 높은 소리**(REF_gem)가 우리는 무음 |
| ~~B-3~~ | ~~전멸 클라이맥스~~ | — | ✅ R14 완료(`climax` 3층) |
| B-4 | **도둑 3결과** — 훔침·탈출·회수 | `advance_step` · `_apply_hit` | Protect 동사의 손실·만회가 안 들리면 동사가 안 읽힌다 |
| B-5 | **폭탄 폭발 + 하트 토큰 착지** | `_spawn_boom` · `_reveal_detonations` | Defuse의 데드라인 벌. 연쇄 hop 시차도 무음 |
| ~~B-6~~ | ~~누수(거점 −1)~~ | — | ✅ R14 완료(`leak`, 유저 결정으로 "손실 무음" 두 번째 예외) |
| ~~B-7~~ | ~~클리어 축하 무대 2.8초~~ | — | ✅ R17 완료(§24 — 스윕·글자·정점·폭죽) |
| ~~B-8~~ | ~~비행기 픽업~~ | — | ✅ R14 완료(`chain` 한 줄) |
| B-9 | 비살상 피격 · 넉백 · 탱크 BLOCK | `_apply_hit` | "안 죽었다"가 처치와 구분이 안 된다 |
| B-10 | 보스 머리 피격 | `_burst_lines` | 보스 판의 유일한 진척 신호가 무음 |
| B-11 | 비행기 발사(호밍 0.34초) | `_fire_plane` | 지금은 탭 소리(`tap_go`)뿐 — 나는 소리가 없다 |
| ~~B-12~~ | ~~칭찬 단어 팝인~~ | — | ✅ R14 완료(`praise`) |
| B-13 | 첫 등장 콜아웃 배너 | `_set_callout` | 판에 몇 번 없는 사건 |
| B-14 | 트레이 리필 | `_refill_tray` | ⚠판당 수십 회 — **진흙 위험**. 붙일지부터 판단 |
| B-15 | 결과 팝업 개봉 · 인트로 카드 도킹 | `RESULT_*` · `intro_t` | 화면이 움직이는데 무음 |
| B-16 | 막힘사 죽음 연출(행 순차 점등) | `_stuck_total` | `fail` 1발뿐 |
| — | 보석·비행기 놓침(회색 퍼프) | `advance_step` | **무음 유지** — 벌이 아니라 조용한 지연이다 |
| — | 적 스폰·전진 | — | **기각 유지**(매 턴 = 진흙, §4) |

남은 신설 어휘는 넷: `charge`(B-19) · `whoosh`(B-1·11, **새 파형 필요**) · `collect`(B-2) ·
`thud`(B-9·10) · `alert`(B-4·5·13).
**⚠새 단어마다 `tools/phone_sim.py`를 같이 돌릴 것**(§19) — 그리고 배선 검사를 프로브에 따로 둘 것.

⚠**다음 라운드는 예산을 봐야 한다.** R14로 롤링 1초가 8 → **12발**이 됐다(예산 15). 창을 더
채우려면 남은 여유가 3발뿐이라, 여기서부터는 "붙인다"가 아니라 **"무엇을 빼고 넣나"**가 된다.
동시 보이스는 3(상한 8)이라 아직 여유가 있다 — 병목은 겹침이 아니라 **초당 발화**다.

### B. 그 밖

1. **2층 확정** — `build/audio_pick/P1~P3`로 광택층 A/B(현재 P2 = 칩 부딪힘 채택 중).
2. **믹스 밸런스** — 단어 간 상대 볼륨은 실기기 스피커에서 다시 본다.
3. **BGM 판정**(§7) + 죽은 토글 처리.
4. **오디오 포커스**(전화·타 앱 재생 중 덕킹)와 iOS 무음 스위치 정책 — 실기기 단계.
5. 선택: 애널리틱스에 `sound_on` 1필드(`run_started`의 필드로, 새 이벤트 아님).

## 23. R16 — 삭제음 B안 채택 + 로켓 발사음 (2026-08-02)

R15 전량 기각 직후의 재시도. **§21의 교훈 두 개를 지켜서 이번엔 판정이 났다.**

### 무엇이 달라졌나

**① 진짜 원인은 삭제음이 아니라 무음 구간이었다.** 유저 관찰 — *"줄지움 이펙트 나가는데 그 사운드가
없어서 아닐까"*. 실측이 맞다: 삭제음은 **250ms**에 끝나는데 링 간 발사 텀이 **260ms**다. 로켓 발사
(`_spawn_muzzle`)와 유도탄 발사엔 `_fb` 호출이 **아예 없었다** → 소리가 끝난 뒤부터 화면만 움직였고,
**콤보가 클수록 무음이 길어졌다**(클수록 조용해지는 구조). §19가 이미 첫 항목으로 지목한 자리다.

**② "조용하다"는 레벨이 아니라 크레스트였다.** 실측 clear 크레스트 **22.0dB** · 피크 −3.6dBFS로
리미터까지 여유 3dB뿐. R4에서 +5~8dB 올리고도 안 잡힌 이유다. 새추레이션으로 눌러 넣었다
(hit 21.6 → 10.2dB). §21⑥과 같은 결론에 처방만 다르다.

**③ "얇다"는 대역이 하나여서였다.** 동작 어휘 넷(place·grab·chain·tap)이 **같은 파형 둘**을 음정만
바꿔 쓰고 **300~2kHz에 96.9%**가 몰려 있다. 저역 3% · 고역 0.2%.

### 채택안

| 층 | 파형 | 대역 | 역할 |
|---|---|---|---|
| 삭제 1층 | `clear_hit`(금속 55ms) | 0.8~2.5k **89.8%** | 타격 — 줄 수와 무관하게 한 발 |
| 삭제 2층 | `clear_note`(유리 106ms) | 0.8~2.5k **99.7%** | **도·레·미** — 40ms 뒤부터 55ms 간격 |
| 로켓 | `rocket`(신스 150ms) | 300~800 **78.7%** | 무음이던 창을 채움. 링 번호 → 음정 |

**음 개수 = 동시에 지운 줄 수 · 시작 음높이 = 연속 콤보.** 지금까지 줄 수는 소리에 전혀 안 쓰였고
콤보만 음정 하나를 정했다 — **화면에서 가장 큰 사건을 소리가 모르고 있었다.**

⚠**레퍼런스에는 이런 상승 런이 없다**(§21③: 사건의 80%가 한 발). 레퍼런스 추종이 아니라 창작자
결정이다(유저 요청). 되돌릴 땐 그 사실부터 기억할 것.

### 방법론 — §21이 옳았다

- **파일 A/B는 이번에도 실패했다.** 폴더 4개(24 + 12 + 16 + 10개)를 구웠고 유저 반응은 "잘 모르겠어".
  **게임 안 '7'/'6'키 전환으로 바꾸자 즉시 판정이 나왔다.** 다음에도 파일 폴더부터 만들지 말 것.
- **지표는 재질을 못 담는다**(§21과 동일). 스펙트럼 조건만으로 거르니 보상음 후보로 `chop`(도끼질)·
  `bookPlace`(책 놓기)가 올라왔고, 조건을 좁히면 신스 삐 소리만 남았다. **재질은 이름으로 먼저 좁히고
  지표는 '못 쓰는 것 걷어내기'에만** 쓰는 게 맞다.
- **크레스트를 후보 조건으로 걸면 안 된다.** 자연 녹음은 전부 뾰족하다(casino 중앙값 27.2dB · 최소
  17.6dB). 크레스트로 거르면 신스만 남는다 → **선별이 아니라 굽는 단계의 처리**다.
- **동전은 음정을 못 나른다.** "그나마 통쾌하다"로 먼저 뽑혔지만 노이즈성이라 `pitch_scale`을 올려도
  도레미가 안 읽힌다(유저 확인). **음정이 의미를 나르는 자리엔 음정 있는 재질을 쓸 것.**

### 남은 것

1. **`SFX_WORDS` 위계 재계산** — 파형이 바뀌면 dB 표를 다시 잡아야 한다(크레스트가 단어마다 달라
   같은 숫자여도 체감이 다르다). 지금 값은 굽기에서 유저가 고른 비율(−7dB 차)을 옮긴 것뿐이다.
2. **실기기 확인** — 로켓의 300~800Hz가 폰 스피커에서 얼마나 남는지 미검증(내 시뮬은 300Hz 아래만
   깎는다). 얇으면 재질을 다시 봐야 한다.
3. **'7'키 제거 + A 경로 삭제** — 출고본엔 A/B가 없어야 한다. 프로브의 ④⑦⑮를 B 기준으로 다시 쓸 것
   (지금은 `tools/audio_probe.gd`가 `clear_ab=0`으로 고정해 A를 잰다).
4. 동작 어휘의 '얇음'(③)은 **아직 안 고쳤다.** 삭제·로켓만 손봤다.

## 24. R17 — 클리어 축하 무대(§22 B-7) (2026-08-03)

유저: **"스테이지 클리어 사운드 작업 진행하자."**

무대는 `finish` 아르페지오(0.3초) 하나로 시작해 **3.4초를 통째로 침묵**했다. 판에서 가장 긴 무음이
하필 가장 큰 순간에 있었던 셈이다 — §17③이 블라스트 창에서 진단한 것과 **같은 병이고, 이번엔 4배 길다.**
화면엔 스윕 8행 · 글자 11개 · 색종이 · 폭죽 7발이 이미 다 있었다. 연출을 새로 만들 필요가 없었다.

### 붙인 것 — 새 파형 0개

| 무대시각 | 화면 | 소리 | 파형 |
|---|---|---|---|
| −0.64 | 승리 | `finish` 아르페지오(기존) | chip_low |
| −0.64…−0.22 | 피니시 스윕(행마다 0.06s) | **`sweep`** — 행 높이 = 사다리 계단 | clear_note(유리) |
| 스윕 끝 | 맨 윗행 섬광+흔들림 | `clear_hit` 한 발(기존 어휘) | 금속 |
| 0.02…0.34 | BLOCK 5글자 팝인 | **`letter`** — 글자 순서 = 계단 | pop_low(=place) |
| 0.56 | CASTLE 등장 | `letter` 상승선의 6번째 계단 | pop_low |
| 0.96 | 오버슛 정점 | **`logo`** — 2층(타격+45ms 광택) | chip_low+chip_high |
| 1.20…2.05 | 폭죽 발사 | **`fw_rise`** | rocket(신스) |
| 1.60…2.45 | 폭죽 터짐 | **`fw_pop`** | chip_high(−5) |

**설계 셋**
1. **음정은 전부 호출부가 정한다.** `chain`처럼 내부 카운터를 쓰면 화면 순서와 어긋나고 무대엔
   되돌릴 다운비트도 없다 → 행 높이·글자 순서·폭죽 번호를 그대로 계단 번호로 넘긴다.
   **연출 상수를 고치면 소리가 자동으로 따라온다**(정점 시각도 `CLEAR_L2_PUNCH` 상수로 뺐다).
2. **폭죽은 발사·터짐의 대역을 갈랐다** — 로켓 파형 300~800Hz(78.7%) vs 칩 높은음 3.9kHz.
   §23③이 지적한 "동작 어휘가 300~2kHz에 96.9%"의 반대 처방이다.
   ⚠터짐을 2층으로 하면 롤링 1초가 **17발**이 되어 예산(15)을 넘는다 → 층 대신 **음정을 흩었다**
   (`CLEAR_FW_STEPS = [0,4,2,6,1,5,3]`. 오름차순으로 주면 음계 연습처럼 들린다).
3. **색종이(1.00초)는 의도적 무음.** 나풀나풀 떨어지는 데 붙일 어택이 없고, 앞(정점 0.96)과
   뒤(폭죽 1.20)가 이미 꽉 차 있다. 하드컷(2.80)도 무음이다 — 폭죽이 컷을 넘어 계속 탄다.
4. **햅틱은 전부 무진동.** 이 창엔 이미 `finish`의 roll이 나가 있고, 2.8초에 진동을 열 몇 번
   더 얹으면 승자독식 액추에이터가 하나의 긴 웅웅거림으로 뭉갠다(§3).

### 곁다리로 잡은 선재 결함 — **맨 아랫행 스윕이 통째로 빠져 있었다**

행 발화 조건이 `el_was < tr`인데 맨 아랫행은 `tr = 0`이고 타이머가 정확히 `-CLEAR_HOLD`에서
시작한다 → **첫 프레임부터 거짓** = 팝도 파편도 한 번도 안 났다. 지우기는 `_draw_clear_wipe`가
따로 그려서 **눈으로는 안 드러났고**, 소리를 붙이자 사다리의 첫 계단(0)이 안 울려서 드러났다.
`<=`/`>`로 고쳤다. → **연출에 소리를 붙이면 연출의 구멍이 드러난다**(§11 ②와 같은 종류).

### 게임 안 '5'키 = 축하 무대 즉시 재생

§21의 방법론 교훈("파일 A/B는 판정이 안 난다, 게임 안 키 전환만 통한다")을 지키려면 **들을 수
있어야** 하는데, 이 연출은 스테이지를 실제로 깨야 나온다 = 한 번 듣는 데 몇 분. 그래서 넣었다.
⚠**입력 관문 셋보다 위**에 둬야 한다 — 무대가 끝나면 결과 팝업 분기가 먼저 `return`해서
**두 번째 재생이 안 먹었다**(실측). 진행도(`cleared`·`_save_campaign`)는 안 건드린다.

### ⚠프로브 설정이 유저 세이브에 물려 있었다

패스 G가 `_start_stage(0)` 뒤의 판을 그대로 쓰려다 실패했다 — 튜토리얼이 살아 있으면
`_tut_setup_beat1`이 십자를 미리 깔아서 **앞 패스와 세이브 상태에 따라 세는 행 수가 달라진다.**
보드를 명시적으로 비우고 채우게 고쳤다. → **합/불을 내는 프로브는 시드뿐 아니라 판도 고정할 것.**

또 하나 정정: 옛 주석의 "봇은 14수 안에 스테이지를 못 깬다"는 **이제 틀렸다**(밸런스 변경 후
봇이 실제로 깬다) → 축하 어휘가 패스 A 로그에 정상적으로 나타난다. 검사 ⑦의 허용 목록을 넓혔다.

검증: `audio_probe` **24검사 PASS**(⑲~㉒ 신설 — 스윕 배선·상승, 로고 조립·2층, 폭죽 14발,
그리고 **무대 안 최대 무음 ≤ 0.55s**) · `haptic_probe` 11검사 PASS · `tools/regress.gd` 골든
**byte-identical**(세이브 백업→복원) · 폰 시뮬 신규 다섯 −0.1 / −2.0 / −7.6 / −4.6 / −1.6dB
(`logo`의 −7.6은 §18에서 유저가 감수한 칩 저역 그대로 = climax와 같은 값) ·
프리뷰 `build/audio_preview/CLEAR_STAGE.wav`(3.64초 타임라인 통째, 합산 피크 −1.4dBFS = 리미터 여유 안).

**실측 — 무엇이 고쳐졌나**

| | R16 이전 | R17 |
|---|---|---|
| 무대 발화 | 4발(아르페지오뿐) | **31발** |
| 무대 안 최대 무음 | **3.4초** | **0.43초**(스윕 끝 → 암전 → 첫 글자) |
| 동시 보이스 | 1 | 4 (상한 8) |
| 롤링 1초 | 4 | 12 (예산 15) |

### 남은 것

1. **귀 판정** — 다섯 어휘의 레벨·음정은 전부 첫 안이다. '5'키로 반복 재생해 고칠 것.
   특히 ①`logo` 강펀치가 정말 '한 방'으로 들리는지 ②폭죽 14발이 진흙인지 ③스윕이 아르페지오와
   싸우는지(둘 다 상승이라 겹친다 — 대역은 다르다: 칩 485Hz vs 유리 1569Hz).
2. **예산이 거의 찼다** — 롤링 1초 12/15. 여기서 더 붙이려면 무엇을 뺄지부터 정해야 한다(§22 경고).
3. 남은 무음 beat: B-19(충전 구간) · B-2(보석 카운터) · B-4·5(도둑·폭탄) · B-15(결과 팝업 개봉).

## 25. R18 — 폭죽을 진짜 폭죽 녹음으로 (2026-08-03)

유저: **"좀더 폭죽 터지는 사운드였으면 좋겠어. 외부에서 찾아도 좋아."**

R17의 `fw_pop`은 **카지노 칩 부딪힘을 −5반음 내린 것**이었다 — 밝은 크랙이지 폭죽이 아니다.
있는 파형으로 되는 자리부터 배선한다는 순서(R13 합의)를 지킨 결과였고, 그 순서가 여기서 끝났다.

**폭죽은 층으로 못 만든다.** 크랙 + 몸통 + 크래클 꼬리가 **한 사건**이라 세 층으로 쌓아야 하는데,
축하 무대는 롤링 1초가 이미 12/15다(§24) — 7발 × 3층은 예산이 못 받는다.
→ **그런 녹음을 구해 오는 게 유일한 길이었다.**

### 음원 — OpenGameArt "25 CC0 bang / firework SFX"(rubberduck, CC0 1.0)

**진짜 불꽃놀이 녹음** 25개(bang 10 · cannon 5 · fw 6 · shot 3 · loop 1). §21④가 "라이브러리에
답이 없다"고 한 건 **삭제음**(짧고 뾰족하고 음정 있는) 얘기였고, 폭죽은 정반대 물건이라 있었다.

기계 필터는 **둘만** 걸었다(§18 선정 방식 그대로) — 폰 통과 −6dB 초과 · 2~5kHz 55% 초과.
25개 중 다섯이 폰에서 탈락했고(전부 저역 덩어리, 최악 `bang_03`은 **−17.6dB**에 300Hz 아래 86%),
남은 것 중 **성격이 갈리는 여섯**을 후보로 남겼다. 고르는 건 사람이다.

| 후보 | 길이 | 중심 | 0.8~2.5k | 폰 | 성격 |
|---|---|---|---|---|---|
| **B `cannon_04`**(기본값) | 0.53s | 1819Hz | **41%** | −1.6 | 레퍼런스 타격 대역과 가장 가깝다 |
| C `fw_03` | 1.03s | 2372Hz | 32% | −1.0 | 진짜 불꽃놀이, 꼬리에 크래클 |
| A `fw_02` | 1.00s | 3178Hz | 34% | −0.3 | 반짝이는 쪽 끝(2~5k 46%) |
| D `cannon_05` | 0.42s | 1664Hz | 33% | −2.1 | 짧은 팡 — 겹침이 걱정될 때의 안전패 |
| E `bang_05` | 0.38s | 909Hz | 36% | −2.8 | 낮은 팡 |
| F `fw_01` | 0.59s | 730Hz | 19% | −4.0 | 가장 무겁다 |

**'6'키로 게임 안에서 순환한다**(미리듣기 = 실제 쇼 간격 0.14초로 **3발** — 폭죽은 겹쳐야 폭죽이다).
'5'키로 무대 전체를 다시 들으면 맥락 안에서 판정이 된다.

### 두 가지 판단

1. **터짐은 사다리를 안 탄다.** 폭죽 녹음은 노이즈성이라 음정이 안 읽히고(R16의 동전과 같은 성질),
   5음계 상한 +16반음까지 올리면 **0.53초가 0.21초로 줄어 재질 자체가 바뀐다.** 여기서 음정은
   음악이 아니라 **크기**(큰 폭죽/작은 폭죽)를 말한다 → `CLEAR_FW_SEMI = [0,4,−3,2,−5,5,−1]` ±5반음.
   발사(`fw_rise`)는 신스 글라이드라 그대로 사다리를 탄다 — **같은 사건인데 음정 규칙이 다르다.**
2. **레벨을 −10 → −13dB로 내렸다.** 길이가 0.10초에서 0.4~1.0초로 4~10배가 됐고 7발이 겹친다.

**겹침 실측(후보 전부 · 판이 가득 찬 최악 조건)**: 최대 동시 보이스 4~7(상한 8) · 롤링 1초 15발 ·
**드롭 0**. 1초짜리(A·C)가 7까지 가고 짧은 것(D·E)은 4다 — 여유가 없진 않지만 **여기가 천장이다.**

### 곁다리 셋

- **`tools/sfx_prep.py` 신설** — 리드/꼬리 절단 · tanh 새추레이션 · 피크 정규화. 이 처리는 규칙으로만
  적혀 있었고(§13·§17②) 매번 손으로 했다. 그래서 두 번 샜다(R10 hit.wav의 60ms 리드, R16의 크레스트).
  **규칙만 적어 두고 도구를 안 남기면 그 규칙은 안 지켜진다** — `phone_sim.py`와 같은 이유로 저장소에.
  후보 여섯은 crest 24~30 → **13~18dB**로 눌러 넣었다.
- **⚠임포트 압축이 세 파일에서 안 지켜져 있었다.** R16에 들어온 `clear_hit`·`clear_note`·`rocket`이
  `compress/mode=2`(QOA)로 남아 있었다 — 게임 재생은 멀쩡하지만 `AudioStreamWAV.data`가 PCM이
  아니라 **bake가 쓰레기 프리뷰를 만든다**(§13이 경고한 그 함정). §24의 `CLEAR_STAGE.wav`도 그래서
  세 층이 잡음이었다. 전부 0으로 고치고 다시 구웠다.
- **⚠계측 도구가 또 조용히 어긋났다.** `fw_pop`의 파형이 후보 목록에서 오게 되자 `phone_sim.py`의
  파서가 그 어휘를 **표에서 통째로 빠뜨렸다** — "새 단어마다 폰 시뮬"이 조용히 안 돌아간 것이다.
  후보 목록도 읽게 고치고, **못 찾은 어휘는 경고로 띄우게** 했다(빠지는 것보다 시끄러운 게 낫다).
  같은 이유로 `audio_probe`의 손으로 적던 `WORD_DUR` 표도 버렸다 — 이제 **뱅크에서 실제 스트림
  길이를 잰다**(파형이 바뀌면 길이가 5배씩 달라지는데 표는 안 따라온다).

검증: `audio_probe` **24검사 PASS** · `haptic_probe` 11검사 PASS · regress 골든 **byte-identical** ·
폰 시뮬 후보 전원 −0.3~−4.0dB(경고선 안) · `CLEAR_STAGE.wav` 합산 피크 −2.0dBFS.

**남은 것**: ①유저가 여섯 중 고르면 진 후보와 `sfx/pick/`을 지우고 이긴 것만 `sfx/`로 옮긴다
②발사음(`fw_rise`)은 아직 신스다 — 터짐이 진짜 녹음이 되면 짝이 안 맞을 수 있다(그때 판단)
③'5'·'6'·'7'키는 출고 전에 같이 걷어낸다.
