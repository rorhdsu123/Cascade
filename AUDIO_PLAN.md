# 오디오 설계 (R1) — BlockCastle

정본 문서. 코드의 접점은 `Main.gd`의 `===== 오디오 =====` 블록 한 곳이고, 감사는 `tools/audio_probe.gd`,
음색 청취는 `tools/audio_bake.gd`가 한다. 형제 문서 = `HAPTIC_PLAN.md`(같은 이음새를 공유한다).

## 1. 왜 지금

- **`sound_on`·`bgm_on`이 소비되지 않는 선호로 오래 남아 있었다.** 설정 모달에 토글이 있고 세이브에
  바이트가 있는데 소리를 내는 코드는 0줄이었다 = 유저에게는 "꺼도 켜도 똑같은 스위치".
- **콤보의 연속 스케일을 낼 수 있는 채널이 오디오뿐이다.** 시각 칭찬어는 이산 등급(GOOD→UNREAL),
  햅틱은 amp 분기가 불가능해 실질 1박/2박이 전부다. 음정은 8단을 **연속으로** 올릴 수 있다.
- **이름값을 소리가 갚는다.** 게임 이름의 '연쇄'는 규칙에서 빠졌고(창작자 확인) 연출 순서로 채우기로
  했다(L1). 링이 시간차로 터지는 그 시퀀스에 **상승 음계**를 얹으면 연쇄가 처음으로 귀에 들린다.

동결(ROADMAP §5, 2026-07-27)과의 관계: 형식상 폴리싱이지만 **무음 빌드로 사람 플테를 재면 코어를
과소평가한다**는 햅틱과 같은 근거로 진행(유저 결정, 2026-07-31). 소리는 진동보다 이 논거가 강하다 —
무음 퍼즐게임은 실제로 덜 재밌게 느껴진다.

## 2. 원칙 넷

1. **두 번째 채널이지 유일한 채널이 아니다.** 모바일은 상당수가 무음 플레이다. 소리가 없으면
   못 읽히는 정보는 만들지 않는다 — 오디오는 전부 **이미 시각으로 말한 것의 보강**이다.
2. **적은 뭉갬이 아니라 진흙(mud)이다.** 햅틱은 액추에이터가 하나라 승자독식이었지만 오디오는
   폴리포니가 있다. 그래서 위험이 다르다: 동시에 열 개가 울리면 드롭이 아니라 **탁하게 뭉친 소음**이
   된다. 방어는 보이스 상한 + 단어별 최소 간격 + 초당 발화 예산.
3. **잦을수록 작고 짧게.** `place`는 판당 수십 번 울린다 → 가장 조용하고 가장 짧다. 긴 소리는
   판당 1회(`fanfare`)뿐이다.
4. **음정이 의미를 나른다.** 세기가 아니라 **음높이**로 크기를 말한다. 그래야 볼륨을 낮춰 들어도
   구분이 살아남고, 5음계라 몇 개가 겹쳐도 불협이 안 난다.

## 3. 두 믹서, 한 접점

햅틱과 오디오는 **호출부를 공유하고 믹서를 공유하지 않는다.**

- 공유하는 이유: 호출부가 갈리면 6757줄짜리 `Main.gd`의 같은 줄을 두 시스템이 각각 고쳐 병합마다
  충돌하고, 더 나쁘게는 "무엇이 중요한가"를 두 곳이 따로 판단해 어긋난다.
- 공유하지 **않는** 이유: 물리가 다르다. 진동은 승자독식·불응기 드롭이 맞고, 소리는 겹쳐도 되며
  삭제음을 진동 불응기 때문에 삼키면 **보상이 사라진다**. 한 믹서로 합치면 이 둘 중 하나가 망가진다.

그래서 `_fb(kind, intensity)` 하나가 접점이고, 내부에서 `_haptic()`과 `_sfx()`로 **독립 라우팅**한다.
매핑표(`FB_MAP`)가 "이 사건은 진동 무엇 · 소리 무엇"을 한자리에 적어두는 유일한 정본이다.

## 4. 어휘 — 다섯

| 단어 | 물리 | 언제 | 간격 · 비고 |
|---|---|---|---|
| **place** | 0.09s, 190Hz 나무 톡 + 3ms 트랜지언트 | 조각 착지 | 0.04s · 가장 조용(−16dB) |
| **clear** | 0.32s, 523Hz 종(배음 4개) | 줄 삭제 다운비트(섬광과 같은 프레임) | 간격 없음 — **절대 안 드롭**(보상이다). 콤보 → 5음계 음정 |
| **chain** | 0.18s, 짧은 마림바 | 순차 피격·처치·보석 획득 **하나마다** | 0.03s · 판 안에서 **한 계단씩 상승**, 상한 8단 |
| **fanfare** | `clear`를 0/+4/+7/+12로 4연타 | 스테이지 클리어 · 무한 PB 돌파 | **판당 1회** |
| **tap** | 0.05s, 330Hz 부드러운 딸깍 | UI 버튼 | 0.05s |

**5음계 사다리** = `[0,2,4,7,9,12,14,16]` 반음(장5음계). 크로매틱을 쓰면 연쇄 중간중간이 불협으로
들리는데, 5음계는 **어떤 조합으로 겹쳐도 협화**라 순서가 흐트러져도 음악으로 들린다. 상한 16반음
(=2.52×)에서 멈춘다 — 더 올리면 개 호루라기가 되고 `pitch_scale` 상한에도 가까워진다.

**손실에는 아무 소리도 없다.** 누수·거점사·막힘사 전부 무음 = 햅틱과 같은 결정("축하만, 손실은 눈으로",
유저 재확인 2026-07-31). 코지 톤과 싸우지 않게 하려는 것이고, 없다가 넣는 게 넣었다 빼는 것보다 쉽다.

**기각**: 배치 실패 버즈(탐색을 벌하면 '안전하게 배우는 재미'와 정면 충돌) · 적 전진음(매 턴 = 진흙) ·
호버/드래그음 · 별도 처치음(`chain`이 이미 그 자리다).

## 5. 에셋 — 없다 (런타임 합성)

**`.wav` 파일을 하나도 안 만든다.** `_ready`에서 GDScript가 PCM을 계산해 `AudioStreamWAV`를 메모리에
띄운다(전부 합쳐 22.05kHz·16bit 모노 ~0.6초 = **약 27KB**).

| | 합성 | CC0 팩 | 외주 |
|---|---|---|---|
| 번들 | ~0KB | 수백 KB~ | 수백 KB~ |
| 라이선스 표면 | **0** (RELEASE.md·심사 무변화) | 출처 관리 필요 | 계약 |
| 튜닝 반복 | 상수 한 줄 | 파일 교체 | 왕복 |
| 톤 | 미니멀·저아트와 일치 | 남의 게임 톤 | 맞출 수 있음 |

커스텀-draw로 픽셀 하나까지 코드가 그리는 프로젝트라 소리도 코드가 만드는 쪽이 톤이 맞는다.
⚠**한계: 합성음은 값싸게 들릴 수 있다.** 그래서 엔벨로프(빠른 어택·지수 감쇠)와 배음 구조에 공을
들였고, **판정은 사람 귀 몫**이다 — `tools/audio_bake.gd`가 각 단어를 `build/audio_preview/*.wav`로
구워 주니 그걸 듣고 상수를 고친다. BGM은 합성으로 못 만든다(§7).

**난수를 안 쓴다.** 미세 디튠은 `randf()`가 아니라 발화 카운터 기반 결정적 순환이다 — 회귀 골든이
전역 RNG 스트림을 공유하므로, 연출이 난수를 더 뽑으면 하류가 시프트할 여지가 생긴다. 안 뽑으면 그
질문 자체가 없어지고 프로브도 결정적으로 재현된다.

## 6. 이음새

- **접점**: `_fb(kind, intensity)` → `_haptic()` / `_sfx()`. `_sfx_step(delta)`는 `_hap_step` 바로 뒤
  (조기 반환보다 먼저 — 예약된 fanfare 음이 메뉴 전환에서 끊기면 안 된다).
- **호출부 6곳**: `_place_piece`(place) · `_burst_lines`(clear, 콤보) · `_apply_hit`의 처치·보석 분기
  (chain) · `_check_win`(fanfare) · PB 돌파 분기(fanfare) · 설정 토글(tap).
- **보이스 풀**: `AudioStreamPlayer` 8개(Master 버스 그대로 — 버스 레이아웃 파일을 안 만든다 =
  project.godot 무변화). 가득 차면 가장 오래된 것을 뺏는다.
- **관찰만**: 게임 값도 RNG도 안 건드린다 → `tools/regress.gd` 골든 불변.
- **플랫폼**: 데스크톱·모바일 동일 동작. 헤드리스는 오디오 드라이버가 더미라 자동 no-op.

## 7. BGM — R1에서 만들지 않는다

합성으로 못 만들고, 기본값이 이미 OFF이며, 모바일 다수는 무음 플레이다. `bgm_on` 토글은 **오늘과
똑같이 아무 일도 안 하는 상태로 남는다** — R1이 고치는 건 `sound_on`뿐이다.
⚠**미결**: 죽은 토글을 실배선까지 숨길지는 유저 판단으로 남긴다(숨기면 UI가 정직해지고,
두면 나중에 되살릴 때 자리 이동이 없다).

## 8. 검증 상태

`tools/audio_probe.gd` (시드 20250123 고정, 창 모드) — **11검사 PASS**:

| 실측 | 값 |
|---|---|
| 사람 템포(1.8s/수) | 27발 / 29.0초 = **초당 0.93발**, 드롭 0 |
| 스트레스(최고속) | 초당 3.46발, `place` 간격 드롭 7건(불응기 작동) |
| **동시 보이스** | 사람·스트레스 **양쪽 최대 3** (상한 8 대비 여유) |
| 롤링 1초 | 최대 6발 (예산 14 대비 여유) |
| 연쇄 사다리 | 판당 3회, 최장 런 `[0,2,4,7,9]` 5음, 역행 0회·상한 초과 0회 |
| fanfare | `[0,4,7,12]` ×2판, 같은 판 두 번째는 `once` 드롭 |
| 결정성 | 같은 시드 두 패스 로그 완전 동일(RNG 미사용) |

`tools/haptic_probe.gd` — 11검사 PASS(리팩터 후에도 불변). `tools/regress.gd` — 골든 **byte-identical**.

`tools/audio_bake.gd` 실측 — 뱅크 총 **29.3KB**, 전 파일 클리핑 0, 시작·끝 샘플 0(불연속 잡음 없음).
⚠튜닝 이력: `clear` amp를 0.62→0.45로 낮췄다 — 배음 4개가 −0.9dBFS까지 붙어서 `fanfare`가 그걸
4연타로 겹치자 프리뷰 파일에 클립 5샘플이 났다(합성은 헤드룸을 직접 챙겨야 한다).

⚠**미검증 둘**: ①**음색** — 합성음이 값싸게 들리는지는 사람 귀로만 답이 난다(구운 파일 청취가 다음 할 일)
②**기기** — 실제 볼륨 밸런스·스피커 톤·안드로이드 지연·오디오 포커스는 실기기 몫.

> 곁다리 발견: `tools/settings_shot.gd`는 **이미 깨져 있다**(없어진 `SETTINGS_GEAR` 상수를 참조 —
> main·track/haptic 양쪽에 없음). 이번 변경과 무관한 선재 결함이라 손대지 않았다.

## 9. R2 — 사운드 디렉터 검토 반영 (2026-07-31)

R1을 계측했더니 **데스크톱에서 믹스한 티가 그대로 나왔다.** 네 결함과 조치:

| | 진단(R1 실측) | 조치 | 결과(R2 실측) |
|---|---|---|---|
| **P0** | `place` 에너지의 97%가 300Hz 아래(기음 190Hz) → 폰 시뮬 **−18.4dB** = 가장 잦은 소리가 사라짐 | 몸통 190→**520Hz**, 2.8kHz 딸깍 추가. 235Hz는 남기되 **정보를 안 실음** | 폰 시뮬 **−6.7dB** (11.7dB 회복) |
| **P1** | 네 단어 중심이 196~591Hz 한 옥타브에 몰림, 2kHz 위 0.0%. `clear`(591)와 `chain`(537)이 캐스케이드에서 서로 가림 | 단어마다 자기 대역: place 507 · clear 660 · **chain 903** · tap 1215Hz. clear 배음을 3.5kHz까지 | 종·연쇄 분리 확보, 어택창 2~5kHz 3.5→**8.7%**(place) |
| **P2** | 디튠 ±0.6%(≈10센트) = 사실상 안 들려 반복 피로를 못 막음 | 단어별 폭(`det`)으로 분리 — place·tap ±1.2%, **음정이 의미를 나르는** clear·chain은 ±0.4%로 좁게(사다리 계단 보존) | — |
| **P3** | 실효 −24~−34dBFS로 판단했으나 **이 진단은 과했다**(아래) | 위계만 재조정 + 런타임 리미터 | clear 0 / chain −5.5 / place −7.6 / tap −7.4 dB |

**⚠P3는 과잉 진단이었다(기록으로 남긴다).** "실효 RMS −34dBFS"는 *소리 길이 기준 RMS*인데 이걸
*프로그램 통합 라우드니스* 목표와 비교해 "너무 조용하다"고 했다. 크레스트가 12~17dB라 실제
보이스 피크는 −7~−18dBFS로 정상 범위였다. 진짜 문제는 절대 레벨이 아니라 **위계**였다 —
`chain`이 `clear` 바로 아래(−2.4dB)에 붙어 있었는데, 한 연쇄에 5~9발이 나가므로 종을 덮는다.

**리미터**: `AudioServer`로 **런타임에** "SFX" 버스 + `AudioEffectHardLimiter`(ceiling −0.5dB)를
만든다. 버스 레이아웃 리소스를 안 만들므로 §5의 '파일 0개'가 유지된다. 캐스케이드 합산 피크의
보험이지 상시 압축이 아니다.

**계측 도구가 두 번 틀렸다 — 이게 이번의 진짜 교훈.**
① 첫 FFT는 파일 앞 4096샘플에 해닝창 하나만 씌웠다 → 전 구간 Welch 평균으로 교체.
② 그 다음 어택창 분석에서 **같은 실수를 반복**했다 — 264샘플 구간에 해닝을 씌웠는데 해닝은
sample 0에서 0이라 **재려던 트랜지언트를 창이 지웠다**. "딸깍이 아예 없다"는 결론이 여기서
나왔고 틀렸다(사각창으로 재니 존재했다). → **트랜지언트를 잴 땐 사각창.** 그리고 계측이 이상한
결론을 내면 대상이 아니라 **자를 먼저 의심할 것**([[verify-reference-claims-on-raw-frames]]와 같은 종류의 함정).

검증: `audio_probe` 11검사 PASS · `haptic_probe` 11검사 PASS · regress 골든 **byte-identical** ·
전 파일 클리핑 0 · 뱅크 26.9KB. **여전히 음색은 사람 귀 미판정**(`*_PHONE.wav` A/B 포함).

## 10. R3 — 레퍼런스(Block Blast) 실측 반영 (2026-07-31)

52초 화면녹화를 분석했다. **⚠먼저 함정: 녹화의 18.4~24.3초·44.6~47.2초는 동영상 광고다.**
광고 오디오는 피크가 0dBFS에 붙어 있어, 안 걷어내면 "레퍼런스 콤보음이 배치음보다 8dB 크다"는
정반대 결론이 나온다. 프레임을 안 봤으면 그대로 믿었을 것이다([[verify-reference-claims-on-raw-frames]]).
아래는 광고 8.5초 제외, 게임플레이 43.6초 · 이벤트 42개 기준.

**확인된 것**
- **BGM이 없다.** 프레임 52.5%가 최대 대비 −45dB 이하, 바닥은 디지털 무음. 통합 −19.1 LUFS.
  → §7의 "R1에 BGM 없음" 판단이 레퍼런스와 일치(이제 추측이 아니라 실측).
- **저역을 안 쓴다.** 300Hz 아래 에너지: 집기 1.1% · 착지 0.6% · 삭제 0.1%. → §9 P0가 옳았다.
- **제스처 방향이 동작을 말한다** — 집기는 **상승**(345→560Hz, +8.4반음), 착지는 **하강**
  (904→388Hz). 삭제는 904→1550Hz 상승 2음(+9.3반음) + 3531Hz 광택층.
- **보석/카운터 도착 = 517→646→775→1550Hz = C–E–G–G′ 상승 아르페지오.**
  우리 `fanfare`(0/+4/+7/+12)와 사실상 같은 음형에 독립적으로 도달했다.
- **레벨 위계가 거의 없다** — 전 SFX가 −8.3~−9.6dBFS 한 줌. 삭제음이 착지음보다 **안 크다**.
  볼륨이 아니라 **음색·제스처·층수**로 차별화한다.

**반영한 것(R3)**
1. **`grab` 신설** — 우리엔 드래그 시작이 무음이었다. 레퍼런스에서 가장 잦은 소리(14회)가 이거다.
   480→780Hz 상승 글라이드(+8.4반음, 폭은 레퍼런스 그대로). 접점 = `_pick_up`.
   ⚠시작음을 420Hz로 뒀더니 폰 시뮬 −8.0dB → 480Hz로 올려 −6.2dB.
2. **`place`를 하강 제스처로** — 884→520Hz(도착 주파수는 R2 그대로, 위에서 미끄러져 온다).
   집기 상승과 짝이 된다. 부수 효과로 폰 통과가 −6.7→**−4.0dB**로 더 좋아졌다.
3. `_sfx_render`에 **글라이드**(위상 누적) 추가. `sin(TAU·f(t)·t)`로 하면 f가 변할 때 위상이 튀어
   잡음이 낀다 → 샘플마다 위상을 적분한다.
4. `grab`·`place`는 **한 쌍이라 실효 레벨을 맞췄다**(−8.0 / −7.4 dB rel). 레퍼런스도 둘이 같다.
   ⚠db 상수는 크게 다르다(−19 vs −10) — 피크 정규화 기준이라 크레스트 차(10.4 vs 18.9dB)를
   상쇄해야 하기 때문. **숫자가 아니라 실효 RMS를 맞출 것.**

**미결로 남긴 것**
- **콤보 음정 상승**: 이 녹화엔 큰 콤보가 없다. 삭제 4회가 매번 **정확히 같은 음정**(904→1550Hz)
  이지만, **표본에 콤보가 안 나왔으니 "레퍼런스에 사다리가 없다"고는 말할 수 없다.**
  판단하려면 연쇄가 크게 터지는 녹화가 필요하다. 우리 사다리는 일단 유지(연쇄가 이름값이다).
- **레벨 위계를 좁힐지**: 레퍼런스는 전 SFX가 1dB 안인데 우리는 `clear`가 `place`보다 +7.4dB다.
  우리는 삭제가 더 드물어서 강조가 맞을 수도 있다 — 유저 판단 대기(권장은 3~4dB로 축소).

검증: `audio_probe` **12검사 PASS**(⑧ 집기 배선 신설 — 봇은 `_place_piece`를 직접 불러서
grab이 패스 A~C에 안 나오므로, 실제 `_pick_up` 경로를 타는 검사를 따로 뒀다. 안 그러면 배선이
끊겨도 초록이다) · `haptic_probe` 11검사 PASS · regress 골든 byte-identical · 뱅크 30.1KB.
글라이드 실측: 집기 474→689Hz 상승, 착지 818→517Hz 하강.

## 11. R4 — 사람 귀 판정 1차 반영 (2026-07-31)

첫 청취 피드백 셋: ①**전자음 같고 은은하다** ②**타격감이 없고 삭제음이 비어 보인다**(레퍼런스는
점수 오르는 소리도 있다) ③**실패 시점에 짧은 소리를 원한다**.

| 지적 | 원인(실측) | 조치 |
|---|---|---|
| 은은함 | 레퍼런스는 전 SFX가 **−8.3~−9.6dBFS 한 줌**에 납작한데 우리는 훨씬 조용하고 위계만 넓었다(집기는 **11.5dB 아래**) | 전체 5~8dB 상향 + 위계 축소. 합산 피크는 리미터가 받는다 |
| 전자음 | 순수 사인 합이라 배음이 정확히 부분음뿐 | **tanh 소프트 포화** 추가(clear 0.35 · fail 0.20) — 배음을 채워 두께를 만든다 |
| 타격감·비어 있음 | 삭제음이 **단층 종소리** 하나. 레퍼런스는 904Hz 타격 + 40ms 뒤 1550Hz 광택의 **2단 사건** | `clear`를 2층으로 재작성 + 3.4kHz 잡음 트랜지언트. 크레스트 15.5→**11.4dB**(밀도↑), 중심 660→**779Hz** |
| 점수 소리 없음 | 롤업에 소리가 안 붙어 있었다 | **`score` 신설** — 1.76kHz 카운터 틱. ⚠캠페인은 `clear_score()`가 0이라 롤업이 없다 → **무한 모드에서만** 울린다(화면에 구르는 숫자가 있는 바로 그때) |
| 실패 무음 | — | **`fail` 신설** — 840→521Hz 하강 2층. **"손실엔 무음" 결정을 유저 요청으로 뒤집은 것**(오디오만. 햅틱은 그대로 무진동 — 실패에 진동까지 얹으면 벌주는 느낌이 된다) |

**⚠같은 병이 세 번째로 재발했다**: `fail`을 620→380Hz로 잡았더니 에너지의 29%가 300Hz 아래로
깔려 폰 시뮬 −8.9dB. 840→521Hz로 올려 −4.7dB. **새 단어를 만들 때마다 폰 시뮬을 돌릴 것.**

**죽은 값 하나 발견**: 아르페지오가 `clear` 이름으로 발화돼서 `SFX_WORDS["fanfare"].db`가 아무 데도
안 쓰이고 있었다. 4음이 겹쳐 리미터를 세게 때리던 원인이기도 하다(프리뷰 클립 8샘플).
→ 큐 항목에 `db`를 실어 넘기고 `_sfx_fire`가 오버라이드를 받게 했다. fanfare = −9dB.

**프로브가 진짜 결함을 잡았다**: `fail` 배선 검사를 처음엔 `pending_core_dead = true`로 세워
`_end_turn`을 태웠는데 **0발**이 나왔다 — `_end_turn` 안의 `advance_step`이
`pending_core_dead = core_hp <= 0`으로 **재계산**해 플래그를 덮기 때문. `core_hp = 0`으로 고쳐야
실제 경로를 탄다. ⚠**grab·score·fail 셋 다 봇 패스(A~C)에선 한 발도 안 울린다** — 배선 검사를
따로 두지 않으면 호출부가 통째로 빠져 있어도 프로브가 조용히 초록이 된다.

검증: `audio_probe` **13검사 PASS**(⑨점수·⑩실패 배선 신설) · `haptic_probe` 11검사 PASS ·
regress 골든 byte-identical · 전 파일 클리핑 0 · 뱅크 50.6KB. 폰 시뮬 전 단어 경고 없음.

## 12. R5 — "메인 플레이가 안 바뀐 것 같다" (2026-07-31)

R4에서 레벨을 3~6dB 올렸는데 유저 판정은 **"실패음만 확인되고 메인 플레이는 모르겠다"**.
버스 피크를 재보니 적용은 확실했다(place −11.4→−6.9 · chain −18.6→−12.6 · clear −7.4→−4.4).
그러면 **레벨은 올랐는데 성격이 안 바뀐 것**이다. 원인을 포락선에서 찾았다.

**몸통/어택 에너지비**(30~120ms 에너지 ÷ 0~30ms 에너지):

| | 레퍼런스 | R4 우리 | R5 우리 |
|---|---|---|---|
| 착지 | **1.76** | **0.08** | **0.86** |
| 집기 | 3.64 | 0.15 | 0.51 |

**R4까지 `place`는 30ms 뒤 에너지가 어택의 8%뿐이었다 = 몸통 없는 딸깍.** 그래서 볼륨을 아무리
올려도 "틱"으로만 들렸다 — **타격감은 세기가 아니라 몸통에서 온다.** 메인 플레이에서 가장 많이
들리는 소리가 `place`(14수당 14회)라 이게 전체 인상을 지배했다.

조치 = **울림 층 추가**. 1층은 하강/상승 제스처(짧게), 2층은 착지 주파수에서 **길게 우는 울림**
(글라이드 없음, τ 0.15~0.20). `place` 85→420ms, `grab` 75→340ms.
⚠레퍼런스는 크레스트 18.1dB(뾰족함)를 **유지하면서** 몸통이 크다 = 울림은 **길되 조용해야** 한다.
그래서 진폭이 아니라 τ를 늘렸다(진폭을 올리면 크레스트가 무너져 펀치가 죽는다).

동시 보이스는 사람 템포에서 최대 2, 스트레스에서 3 — 꼬리가 길어져도 진흙 위험 없음(사람 템포
1.8초/수라 꼬리가 안 겹친다). 뱅크 50.6→76.4KB.

검증: `audio_probe` 13검사 PASS · regress 골든 byte-identical · 클리핑 0 · 폰 시뮬 경고 없음.

## 13. R7 — 합성을 버리고 CC0 샘플로 (2026-07-31)

R1~R6은 사인 부분음을 런타임 합성했고 6라운드를 튜닝했지만 유저 판정은 계속 **"전자음 같다"**였다.
CC0 팩 391개를 우리 기준(폰 통과·대역·밀도)으로 심사해 후보를 추렸고, 유저가 **`pop3`·`pop9`**를
골랐다. **§5의 '에셋 파일 0개'는 여기서 폐기된다.**

**⚠지표의 교훈 — '몸통'과 '밀도'를 혼동했다.** R5에서 나는 "타격감 = 긴 꼬리(몸통비)"라 결론냈는데,
유저가 고른 pop은 **몸통비 0.00**이다. 대신 크레스트가 낮다(9~11dB) = **짧지만 꽉 참**. 즉 타격감은
길이가 아니라 밀도였다. 지표는 후보를 좁힐 뿐 **판정은 귀가 한다**.

**한 가족에서 전 어휘를 뽑는다** — 음원은 파형 **둘뿐**(합계 19KB):

| | 파형 | 쓰이는 곳 |
|---|---|---|
| `pop_low` (775Hz, 크레스트 10.8dB) | 따뜻·꽉 참 | place · clear · fail |
| `pop_high` (1034Hz, 크레스트 14.6dB) | 밝고 가벼움 | grab · chain · clear2 · score · tap |

음정(`pitch_scale`)·레벨(`db`)·층(지연 발화)으로만 갈리므로 **통일감이 공짜로 생긴다.**
삭제음은 low 타격 + 40ms 뒤 high(+9반음) **2단**(레퍼런스 구조 그대로).

**유지된 것 — 배관은 하나도 안 바뀌었다**: `_fb` 접점 · FB_MAP · 두 믹서 분리 · 5음계 사다리 ·
보이스 8 · 간격/예산 · 런타임 SFX 버스+리미터 · 프로브 13검사 · 폰 시뮬. 바뀐 건
`_sfx_build_bank()`가 계산 대신 `load()`를 하는 것뿐이다.
**합성 코드(`_sfx_render`/`_sfx_layer`/`_sfx_lcg`, 약 200줄)는 지웠다** — git C104~C109에 남아 있고,
"나중에 쓸지 몰라서" 남기는 죽은 코드가 제일 잘 썩는다.

**⚠Godot 함정 — WAV는 기본 QOA 압축으로 임포트된다.** 게임 재생은 멀쩡하지만 `AudioStreamWAV.data`가
PCM이 아니게 되어 bake 도구가 **쓰레기 파일을 만들었다**(경고: "Saving IMA_ADPCM and QOA samples is
not supported yet"). `.import`의 `compress/mode=0`으로 무압축 고정. 파형은 19KB뿐이라 손해가 없다.

**⚠프로브가 또 잡았다**: 삭제음 둘째 층을 `chain` 이름으로 발화시켰더니 사다리 분석이 그걸 계단으로
세어 **역행 2회**로 읽었다(9 → 0). 내부 전용 어휘 `clear2`로 분리해 해소.

**라이선스**: CC0 1.0(퍼블릭 도메인) — 상업 사용 가능, **표기 의무 없음** → RELEASE.md·심사 표면 무변화.
출처는 `sfx/CREDITS.txt`에 기록한다(의무가 아니라 **추적용** — 나중에 교체·재라이선스를 판단하려면
어디서 왔는지 알아야 한다).

검증: `audio_probe` 13검사 PASS · regress 골든 **byte-identical** · 클리핑 0 · 폰 시뮬 경고 없음
(low −3.8dB · high −2.3dB) · 버스 실측 전 어휘 발화(clear는 보이스 2개 = 2단 확인).
⚠검사 ④의 이번 표본은 연쇄 런이 짧아(최장 `[0]`) 사다리 검증력이 약하다 — 사다리를 크게 바꿀 땐
콤보가 큰 판으로 다시 볼 것.

## 14. R8 — 어휘별 기본 음정(base) (2026-07-31)

유저: **"놓기를 더 높은 소리로 하는 건 어때?"**

**파형을 맞바꾸지 않았다.** `pop_high`는 크레스트가 14.6dB로 `pop_low`(10.8dB)보다 얇아서, 가장 잦은
`place`를 거기 얹으면 §12에서 고친 "타격감 없음"으로 되돌아간다. **밀도는 low로 지키고 음정만 올린다.**

그래서 `SFX_WORDS`에 **`base`(단어별 기본 음정, 반음)**를 새로 뒀다. 사다리(`semi`)와 분리한 이유:
`clear`·`chain`은 `semi`가 콤보/계단을 나르므로 거기에 기본값을 섞으면 사다리 시작점이 밀린다.
**`base` = 이 단어의 자리, `semi` = 지금 몇 번째.**

실측 결과(재생 음정 기준):

| 어휘 | 파형 | base | 지배 F | 길이 |
|---|---|---|---|---|
| **place** | low | **+7** | **1163Hz** | 59ms |
| chain | high | 0 | 1034Hz | 130ms |
| **grab** | high | **−2** | **904Hz** | 146ms |
| clear(다운비트) | low | 0 | 775Hz | 89ms |

놓기가 집기보다 **4반음 위**, 그리고 삭제 다운비트가 가장 낮아 무게를 받친다.
⚠**음을 올리면 샘플이 그만큼 짧아진다** — place는 89→59ms(+7반음 = 길이 67%). 유저가 고른
`pop3`가 58ms였으니 같은 범위다.

검증: `audio_probe` 13검사 PASS · regress 골든 byte-identical · 폰 통과 전 어휘 −0.8~−2.9dB.

## 15. R9 — 보상음을 '팡'으로 (2026-07-31)

유저: **"블록 터지고 적 죽이는 사운드가 너무 잔잔해. 더 팡 터지고 기분 좋았으면."**

**pop은 본질적으로 얌전한 소리다.** 놓기·집기처럼 작고 잦은 동작엔 맞지만 보상엔 부족하다.
→ **동작 계열(pop)과 보상 계열을 갈랐다.** 파형 넷:

| 파형 | 특성 | 쓰이는 곳 |
|---|---|---|
| `pop_low` 775Hz | 따뜻·꽉 참 | place · fail |
| `pop_high` 1034Hz | 밝고 가벼움 | grab · score · tap |
| **`burst`** 3146Hz | 어택 22dB · **2~5kHz에 58%** | **clear**(줄 삭제 타격) |
| **`sparkle`** 5156Hz | 2~5kHz에 52% | **chain**(적 처치) · clear2 · fanfare |

'팡'의 음향 조건 넷으로 391개를 전수 심사했다: ①강한 어택 ②밝음(1.5~5kHz = 폰이 제일 잘 내는 대역)
③광대역(순음이면 '터진다'로 안 들림) ④폰 통과. `slam`·`shot` 계열도 통과했지만 **"기분 좋은"이
아니라서** 뺐다 — 보상은 터지기만 하면 안 되고 **반짝여야** 한다. 그래서 2층(타격+반짝임)이다.

**아르페지오도 바꿨다** — `clear`(타격) 4연타는 거칠어서 `clear2`(반짝임)로. 판을 닫는 소리는
기분 좋게 울려야 한다.

**⚠레벨은 실효 RMS가 아니라 '보이스 피크'로 잡았다.** `burst`는 크레스트가 25dB라 RMS를 다른
단어와 맞추려면 +8.7dB가 필요한데 그러면 피크가 **+8dBFS**가 되어 리미터를 뭉갠다. 짧고 뾰족한
소리는 peak이 체감 세기를 지배한다 → clear −2.7 / clear2 −9 / chain −12 dBFS.
동작 계열(pop)은 여전히 실효 RMS 기준이다. **두 기준이 한 표에 섞여 있으니 주석을 볼 것.**

버스 실측: `clear` −9.2 → **−5.9dB**(보이스 2개 = 타격+반짝임).

검증: `audio_probe` 13검사 PASS · regress 골든 byte-identical · 클리핑 0 · 동시 보이스 최대 2.
⚠프로브 검사 ⑤가 아르페지오를 `clear` 이름으로 세고 있어 한 번 FAIL했다(어휘를 바꾸면 **검사도
같이 갱신**해야 한다 — 배선 검사의 이름 의존은 이런 식으로 깨진다).

## 16. 남은 것

1. **음색 튜닝** — 구운 파일을 듣고 상수 조정(가장 먼저).
2. **믹스 밸런스** — 단어 간 상대 볼륨은 실기기 스피커에서 다시 본다.
3. **BGM 판정**(§7) + 죽은 토글 처리.
4. **오디오 포커스**(전화·타 앱 재생 중 덕킹)와 iOS 무음 스위치 정책 — 실기기 단계.
5. 선택: 애널리틱스에 `sound_on` 1필드(`run_started`의 필드로, 새 이벤트 아님).
