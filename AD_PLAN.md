# Cascade — 광고 배관 계획 (Phase V W2)

> 확정일 2026-07-28. **정본 관계**: 광고 포맷·슬롯 정책의 정본은 `SPEC.md` §8.1(결정 로그 C55), 계측 스키마 정본은 `ANALYTICS_TAXONOMY.md` §3-7. 이 문서는 그 위에 **"광고가 제대로 안 나올 때 게임이 무엇을 하는가"**를 확정하고, 배선 순서를 못 박는다.
>
> 왜 이게 이번 기획의 전부인가: 포맷(리워드 부활 + 캡 인터스티셜)과 슬롯은 C55에서 이미 닫혔다. 남은 미정은 **실패 경로**뿐인데, 광고는 자주 실패한다 — 재고 없음(no-fill), 로드 지연, 유저 중도 이탈. 신생 앱·소규모 지역일수록 흔하다. 이 경로를 안 정하고 SDK를 붙이면, 결정이 SDK 기본 동작에 떠밀려 정해진다.

---

## 1. 이번에 확정한 것 (2026-07-28)

| # | 항목 | 확정 | 근거 |
|---|---|---|---|
| 1 | **부활 횟수** | **판당 1회** — 광고 부활이든 공짜 부활이든 **합산 1회** | 세션 늘어짐·광고 피로를 먼저 막는다. ⚠SPEC §8.1 A의 "리워드로 무제한"을 **정정**(코드는 처음부터 판당 1회였고 문서가 못 따라온 상태였음) |
| 2 | **no-fill 폴백** | **공짜로 이어준다** (버튼 숨김·회색 아님) | 광고 재고가 없는 건 유저 잘못이 아니다. 죽은 직후 이어하기가 사라진 화면 = C60 '억울한 죽음' 재발 → 잃는 건 광고 단가가 아니라 그 유저의 다음 세션 |
| 3 | **광고 중도 이탈** | **부활 기회 소진 아님** — 팝업 유지, 다시 누를 수 있음 | 보상 미지급은 AdMob 규칙이라 어쩔 수 없지만, 실수로 스킵한 유저를 죽이지 않는다 |
| 4 | **인터스티셜** | **배관만 깔고 노출은 off** (슬롯·캡은 2026-07-30 개정 → §3) | 캡 수치(첫 2판 면제 / 3판·3분 중 늦은 쪽 / 온보딩 5판 면제)는 사람 플테 0회 상태서 정한 값 = 근거 없음. 지금 켜서 얻을 데이터도 없다(5~10명서 광고 피로는 안 나옴) → W4 UA 코호트에서 on/off 리텐션 비교 |
| 5 | **배선 순서** | **R1(코드·계정 0) → R2(SDK·빌드) → R3(출시)** | W1 애널리틱스가 같은 형태로 성공(이음새+로컬 백엔드 먼저, Firebase 뒤). UX 결정은 전부 R1에 있고 계정·네이티브와 독립 |

**공짜 폴백에 별도 카운터를 만들지 않는다** — 부활 제한이 `revive_used` 스위치 하나로 걸려 있고 이게 켜지는 시점은 *실제 부활 성사 순간*뿐이라, 판당 1회 총량 안에 공짜 폴백이 그대로 흡수된다. 무부활 최고점(`clean_best`, 광고 안 쓰고 낸 순수 기록) 판정도 같은 스위치를 보므로 **공짜 부활도 자동으로 "부활 쓴 판"**으로 잡힌다 — 정확하다.

**⚠데이터 후 재검토 대상**: 판당 1회는 광고 수익 상한을 스스로 거는 선택이다. SPEC이 무제한이라 했던 근거는 "무한 모드는 죽을수록 광고를 더 보니 주력 수익 엔진"이었다. 값 하나(`revive_used` → 카운터)라 언제든 2~3회로 풀 수 있으니, 소프트런치 데이터에서 **부활 전환율이 높고 세션 길이가 안 늘어지면** 푸는 쪽을 검토한다.

---

## 2. 부활 흐름 (확정 상태 기계)

```
실패(거점사·막힘·금고털림)
  └→ 결과 팝업: 'AD 이어하기' 버튼 (revive_used==false 일 때만)
        │
        │ 누름
        ▼
     광고 요청 ──── 준비됨 ──→ 시청 ──┬── 끝까지 봄 ──→ 부활 (method=ad_reward)
        │                            └── 중간에 끔 ──→ ✗아무 일 없음. 팝업 유지, 다시 누를 수 있음
        │
        └── 재고 없음(no-fill) / 로드 실패 / SDK 오류 ──→ 부활 (method=free_fallback)
```

- **부활의 내용은 안 바뀐다** — 기존 세컨드 윈드(실패 원인에 맞춰 하단 3줄만 손대고 진행도는 이어받음, C48) 그대로. 광고는 그 앞에 붙는 게이트일 뿐이다.
- **대기 중엔 팝업의 다른 버튼(재도전·홈)을 막는다.** 광고 뜨는 사이 잘못 눌러 판이 날아가는 사고 방지.
- **소프트락 안전망 유지** — 부활 후 놓을 자리 보장(트레이 리필 + 최악엔 하단부터 한 줄씩 더 비움, d6689ad)은 두 경로 모두에 그대로 적용된다.

---

## 3. 인터스티셜 정책 (배관만, 노출 off)

### 3.1 빈도 캡 (`ad_service`)
- 세션 **첫 2판은 면제**(첫인상 보호)
- 이후 **"3판마다 AND 3분마다"** 1회 — 둘 중 늦은 쪽
- 캡 카운터는 **띄우기로 결정한 순간** 리셋한다(로드 실패가 빈도를 올리는 역설 방지)

### 3.2 온보딩 면제 (`Main.gd: AD_ONBOARD_CLEARS = 5`)
빈도 캡은 **세션** 단위라 신규 설치 첫 세션에도 3판째부터 열린다 — 거기가 온보딩이고 이탈 위험이 가장 큰 자리다.
그래서 **누적 클리어 5판 전까지는 인터스티셜을 아예 안 띄운다**. 판단 입력은 이미 영속되는 `cleared` 진행도라
새 저장 필드가 없다. 역할 분담 = **빈도는 광고 서비스 / "아직 배우는 중인가"는 게임**.

### 3.3 이음매 = 슬롯은 한 곳뿐 (2026-07-30 개정)
**`_result_advance()` — 결과 화면을 닫고 다음 판을 열기 직전.** 그 시점은 축하 무대·팝업 개봉이 다 끝나
보상 피드백을 자른 게 없고, 다음 판의 진입 연출은 광고가 닫힌 뒤에 **처음부터** 재생된다.
근거 = 레퍼런스(Block Blast) 원본 프레임 실측: 광고는 축하가 완전히 끝난 뒤 씬 경계에서 들어오고,
복귀 후 진입 연출(로고 조립·보상 입금)을 온전히 다시 보여준다. 그래서 8초 광고인데도 손해감이 적다.

**규칙 4개:**
1. **이긴 판 뒤에만.** 실패 직후 재도전 앞에 끼우면 '벌' 위에 '대기'를 얹는 것.
2. **판 시작(`_init_game`)에서는 절대 안 띄운다.** 바로 뒤에 켜지는 인트로 배너(목표 선언 → HUD 도킹)가
   광고 뒤에서 흘러가 버린다. ⚠2026-07-30까지 실제로 여기 있었다(노출 off라 안 드러났을 뿐).
3. **다음 판 시작을 광고 콜백까지 미룬다.** 단 **안전밸브 `AD_SEAM_TIMEOUT=6s`** — 인터스티셜 플랫폼
   경로엔 SDK 타임아웃이 없어 콜백이 침묵하면 진행이 인질로 잡힌다.
4. **무한·부활 직후는 구조적으로 제외.** 무한 런은 `game_clear`로 끝나지 않고, 그 자리는 리워드 부활이 이미 쓴다.

*(구 문장 "슬롯은 새 런 시작 시점뿐, 스테이지 전환엔 절대 안 뜬다"를 이 절이 대체한다. 그 규칙대로면
세션당 1회 수준이라 수익을 검증할 표본조차 안 생기고, 레퍼런스와도 어긋난다.)*

### 3.4 실측 케이던스 (`tools/ad_cadence_probe.gd`)
신규 설치에서 16판을 이어 플레이한 시뮬레이션 — 판 소요시간을 60/120/240초로 바꿔도 결과 동일:

```
광고 4회 @ 5, 8, 11, 14번째 판   [····A··A··A··A··]
실패 섞으면(2승1패) 4회 @ 7, 10, 13, 16   [··F··FA·FA·FA·FA]
```

**첫 광고는 5번째 판(=스테이지 5 클리어) 뒤, 이후 3판마다.** 시간 게이트(180초)는 판 60초짜리에서도
안 걸린다(3판이면 이미 초과) → 실질 규칙은 "3판마다" 하나이고, 시간 게이트는 초고속 플레이용 브레이크다.
⚠이 수치는 **사람 플테 0회 상태의 보수적 초기값**이다. 이탈 지표를 보고 조정할 것.

R1에선 정책 함수와 카운터를 만들고 프로브로 검증만 한다. 게임 실행 중엔 스위치가 꺼져 있어 아무것도 안 뜬다.

---

## 4. 계측 (`ANALYTICS_TAXONOMY` §3-7 → P2 승격)

R1에서 실배선: `ad_requested` · `ad_filled` / `ad_no_fill` · `ad_shown` · `ad_rewarded` · `ad_closed`.
발화 주체는 **광고 서비스 자신**(게임 코드는 광고 지표를 몰라도 된다).

기존 부활 이벤트 2건 보강:
- `revive_offered.is_ad_ready` — 하드코딩 `false` → **실값**(프리로드 성공 여부)
- `revive_taken.method` — `"stub"` → **`ad_reward` / `free_fallback`** 2값

이 라벨 분리가 있어야 "부활 전환율"과 "광고 수익"이 안 섞인다. 공짜 폴백이 많다 = 재고 문제지 유저가 광고를 본 게 아니다.

---

## 5. 배선 3단계

### R1 — 코드만 (계정 0, 실광고 0)
`ad_service.gd` 이음새(`leaderboard.gd`·`analytics.gd`와 동형) + **페이크 백엔드**. 페이크는 fill / no-fill / 유저취소 / SDK오류를 토글로 재현하므로, 실광고 없이 위 상태 기계 전부를 데스크톱에서 검증할 수 있다. 헤드리스(회귀·시뮬·프로브)에선 서비스가 스스로 꺼져 하네스를 오염시키지 않는다.

### R2 — SDK + 빌드 ✅코드·빌드 완료(2026-07-28) / 실기기 관측만 남음

[Poing Studios 플러그인](https://github.com/poingstudios/godot-admob-plugin) **v5.0.0** 배선 완료. **AdMob 계정은 결국 필요 없었다** — 구글 공개 테스트 유닛(앱 ID `…~3347511713`, 리워드 `…/5224354917`)으로 실기기 검증까지 간다. 계정은 R3(실 유닛) 몫.

배선 요약:

| 무엇 | 어디 |
|---|---|
| 플러그인 | `addons/admob/` (v5.0.0). 샘플 앱은 뺐다 — 샘플 번역이 `project.godot`에 등록되면 우리 커스텀 `_t()` i18n과 무관한 로케일 파일이 딸려 온다 |
| 안드로이드 네이티브 바이너리 | `addons/admob/android/bin/` — **일부러 커밋한다**(업스트림 기본은 무시). 안 하면 헤드리스 export가 매번 GitHub에서 aar을 내려받는다(`binary_installer.gd`는 *헤드리스일 때만* 자동 설치 = 정확히 우리 CLI 빌드 경로) |
| 활성화 | `project.godot` `[editor_plugins]`. ⚠에디터가 이 파일을 다시 쓰면 **주석이 통째로 날아간다**(값은 유지) — 에디터/창 모드 실행 뒤엔 `git diff project.godot`로 주석 소실을 확인할 것 |
| App ID | 프로젝트 설정 기본값(= 구글 테스트 앱 ID)이라 `project.godot`에 안 적힌다(값이 기본값과 같으면 Godot이 저장을 생략). R3에서 실 ID로 바꾸면 그때 파일에 남는다 |
| 유닛 ID | `ad_service.gd` 상단 상수 4개 |
| 실 백엔드 스위치 | `_platform_on` — 안드로이드/iOS면 자동, 데스크톱은 `--ad-mock`을 줬을 때만 |

**⚠지뢰 3개(밟은 순서대로)**

1. **gradle 커스텀 빌드 전환** — 네이티브 플러그인의 유일한 전제. 선행 완료(de6d2c3). APK 경로가 프리빌트→로컬 컴파일로 한 번 갈아엎어졌다.
2. **compileSdk 35 → 36** — v5.0.0의 aar이 Google Mobile Ads **Next-Gen SDK v25**라 `compileSdk ≥ 36`을 요구하는데 Godot 4.6.2가 깔아주는 템플릿은 35다. 빌드가 `requires ... compile against version 36 or later`로 거절된다. compileSdk는 export preset이 노출하지 않는 값(min/target만)이라 **템플릿을 직접 고쳐야 한다** → `tools/android_template_patch.py`. `android/`는 .gitignore라 저장소가 이 수정을 기억 못 하므로 **템플릿을 재설치할 때마다 스크립트를 다시 돌린다.** 선행 = `sdkmanager "platforms;android-36" "build-tools;36.0.0"`.
3. **플러그인 부재가 페이크로 위장되는 것** — 실기기에서 네이티브 플러그인이 안 붙었는데 페이크 백엔드로 흘러가면 광고가 *도는 것처럼* 보인다. 수익은 0인데 사고는 안 드러나고 소프트런치 데이터가 통째로 거짓이 된다. 그래서 **기기에서 플러그인이 없으면 서비스를 끈다**(유저는 공짜 부활로 안 다치고, `ad_*` 지표가 통째로 비어 즉시 눈에 띈다).

**부수 결정**: APK에 안 넣는 것 = 목 광고 UI·샘플 자산·C#·iOS 바이너리(`export_presets.cfg` `exclude_filter`). 안 빼면 목 광고용 `music.ogg` 1.8MB가 그대로 실린다.

**관측 현황(2026-07-29, 에뮬레이터 Pixel 6 / Android 15 / arm64)**

| # | 무엇 | 상태 |
|---|---|---|
| 1 | 테스트 광고 시청 → **부활 성사** | ✅**관측됨**(2026-07-29) |
| 2 | 네트워크 끊고 → **실 SDK 실패가 우리 폴백 경로로 번역** | ✅**관측됨**(2026-07-30, 아래) |
| 2b | 그 상태에서 '이어하기' 눌러 **공짜 부활 성사** | ⬜미관측 — 죽을 때까지 플레이해야 해서 사람 몫 |
| 3 | 광고 중도 이탈 → 부활 안 됨 + **다시 누를 수 있음** | ⬜미관측 — 영상을 중간에 끄는 건 사람 몫 |

①이 통과했다는 건 배선 전체가 실제로 살아 있다는 뜻이다 — 네이티브 플러그인 로드 → `MobileAds.initialize` → 리워드 프리로드 → 표시 → `on_user_earned_reward` → 닫힘 → `_revive("ad_reward")`까지 한 줄로 이어졌다.

**②의 관측(2026-07-30, 에뮬레이터 + 기내 모드, 동의 배선 추가 후)** — 기기 안 `analytics.jsonl`을 그대로 뽑아 확인:

| 상태 | 발화 |
|---|---|
| 앱 진입 | `ad_consent_updated status=not_required can_request_ads=true unit_mode=test` |
| 오프라인 판 시작 | `ad_requested` → **`ad_no_fill reason=error latency_ms=351`** |
| 온라인 판 시작 | `ad_requested` → **`ad_filled latency_ms=5520`** |

이게 ②에서 **실제로 위험했던 절반**이다(실 SDK 실패 콜백 → 우리 상태 기계 번역). 그 뒤의 '공짜 부활'은
우리 코드 로직이고 `ad_probe`·`ad_mock_probe`가 이미 덮는다. 동의 흐름도 이 관측으로 **실기기 첫 검증**을 통과했다
(한국 = 동의 불필요 → 광고 요청 허용 → SDK 초기화 → 프리로드까지 한 줄).

⚠**이 관측이 계측 구멍 하나를 잡아냈다**: 오프라인 실패의 실제 사유는 `no_fill`이 아니라 **`error`(네트워크)**였는데,
`ad_no_fill` 이벤트에 사유가 안 실려 있어 둘이 한 덩이로 보였다. 그대로 뒀으면 소프트런치에서 **접속 문제를
'광고 재고 부족'으로 오독**했을 것이다 → 모든 `ad_no_fill`에 `reason`을 싣도록 수정.

⚠**로드 지연 5.5초 관측** — `LOAD_TIMEOUT_MS`(10초) 안이지만 여유가 크지 않다. 느린 실기기·나쁜 회선에서
타임아웃이 실제로 걸릴 수 있으니, 사람 플테에서 `ad_no_fill reason=no_fill`이 잦으면 이 값을 먼저 의심할 것.

⚠**에뮬레이터는 실기기가 아니다.** 광고 배선엔 충분하지만(GMA SDK가 실제로 네트워크를 탄다) 성능·터치감·기기별 화면비는 못 잰다 — 그건 W1 사람 플테 몫.

```bash
# 빌드 (툴체인·함정은 [[android-build-toolchain-setup]])
PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH" godot --headless --export-debug "Android" build/android/cascade.apk
ADB=/opt/homebrew/share/android-commandlinetools/platform-tools/adb
$ADB install -r build/android/cascade.apk
$ADB logcat -c && $ADB logcat | grep -iE "Ads|admob|AdService"
```
관측 절차: ①무한 모드로 죽는다 → '이어하기' → **테스트 광고**가 뜨고 끝까지 보면 부활(logcat에 `ad_rewarded`) ②기내 모드로 바꾸고 다시 죽는다 → 광고가 안 뜨고 **바로 공짜 부활**(= no-fill 폴백) ③광고를 중간에 끄면 부활 안 되고 팝업이 남아 **다시 누를 수 있다**.

### R3 — 출시 직전
실 유닛 ID · UMP 동의 흐름 · Play Console 데이터 안전성 신고 + 광고 ID 권한 · 미디에이션(데이터 후).

---

## 6. 검증 계획

`tools/ad_probe.gd` 6시나리오:
1. 광고 fill → 끝까지 시청 → 부활, `method=ad_reward`
2. 광고 fill → 중도 취소 → **부활 안 됨 + 기회 안 소진**(다시 누를 수 있음)
3. no-fill → 공짜 부활, `method=free_fallback`
4. 부활 1회 쓴 뒤 재실패 → **이어하기 버튼 안 뜸**(판당 1회)
5. 인터스티셜 캡 카운터 — 첫 2판 면제 + 3판/3분 규칙이 의도대로
6. 두 부활 경로 모두 **놓을 자리 보장**(소프트락 안전망 d6689ad 유지)

추가: **회귀 byte-identical** — 광고 서비스는 게임 RNG를 안 쓰고 헤드리스서 꺼지므로 기존 골든이 그대로 맞아야 한다.

**R2는 다른 걸 재야 한다.** 위 6시나리오는 *우리가 쓴 상태 기계*를 재는데, 실 SDK가 붙어도 그건 안 변한다. R2에서 새로 생긴 위험 구간은 **플러그인 콜백 → 우리 상태 기계 사이의 번역**뿐이고, 기기 없이 그걸 밟는 유일한 방법이 플러그인의 **에디터 목 광고**다 → `tools/ad_mock_probe.gd` (창 모드 + `--ad-mock`, 30검사):

- 로드→시청→보상→닫힘 = 부활 / 로드 실패(code 3) = 공짜 부활 / 보상 없이 닫힘 = 부활 안 됨+기회 유지 / 표시 실패 = 공짜 부활
- **타임아웃 2종**(불변식 ⑥) — 실 SDK는 콜백을 영영 안 줄 수 있고, 그게 "결과는 반드시 1회"가 깨지는 유일한 현실 경로다. 로드 10초·표시 시작 8초를 넘기면 no-fill/오류로 닫아 공짜 부활로 흘린다. 늦게 도착한 광고는 버리지 않고 다음 요청용으로 쥔다.
- 게임 통합 1건 — 결과 팝업의 '이어하기'가 실 콜백 경로를 지나 부활까지.

**이음매(§3.3)는 상태 기계가 아니라 '어디서 띄우나'라 별도 프로브가 필요하다** → `tools/ad_seam_probe.gd` (7검사):
노출 off면 즉시 다음 판(회귀 없음) / 노출 on+재고면 광고를 거쳐 다음 판 / no-fill·오류에도 진행 /
**콜백 침묵 시 안전밸브 6초** / 판 시작 지점에선 안 뜸(진입 연출 보호) / 실패 재도전엔 안 붙음 / 온보딩 면제.
케이던스는 `tools/ad_cadence_probe.gd`가 세션을 흉내내 **몇 번째 판에서 뜨는지** 숫자로 찍는다(§3.4).

```bash
godot --headless --path . --script tools/ad_probe.gd            # 29 — 정책·상태 기계(페이크)
godot --path . --script tools/ad_mock_probe.gd -- --ad-mock     # 30 — 실 SDK 콜백 번역(목 광고)
godot --headless --path . --script tools/ad_seam_probe.gd       #  7 — 이음매(어디서 띄우나·안전밸브)
godot --headless --path . --script tools/ad_cadence_probe.gd    #     실효 케이던스 리포트(합/불 아님)
```

---

## 7. 코드 밖 (유저 몫) + 비용

| 항목 | 비용 | 시점 |
|---|---|---|
| AdMob 계정 개설·앱 등록·**실** 유닛 ID | **무료** (정산받을 때만 주소·세금 정보 + 첫 $10에 PIN 우편 확인) | ~~R2 시작 전~~ → **R3**. R2는 구글 공개 테스트 유닛으로 계정 없이 끝났다 |
| Firebase 프로젝트 (W1 잔여) | **무료 티어로 충분** | 같은 구글 계정으로 묶기 |
| **Google Play 개발자 계정** | **$25 1회** (환불 불가, 평생) | W4 클로즈드 트랙 업로드 필수 |
| 소액 유료 UA | 수백 달러 | W4 (로드맵 기존 항목) |

**⚠일정 리스크 발견 — 12 테스터 규칙**: 개인 명의 Play 개발자 계정은 프로덕션 출시 전에 **테스터 12명이 14일간 연속으로 클로즈드 테스트에 참여**해야 한다. W1 플테 목표가 5~10명인데 **처음부터 12명 이상으로 섭외하면** 플테와 이 요건이 한 번에 해결된다. 나중에 "12명 못 채워 출시 2주 밀림"이 이 규정의 흔한 사고다.

---

## 8. 기각·보류

- **배너·앱오픈 광고** — 코지 코어와 세로 보드를 해쳐 제외(C55). 재론 금지.
- **점수-리깅 리워드**("광고 보고 점수 2배") — 리더보드 공정성 파괴. featured 시드엔 영구 금지, 프리 무한도 소프트런치 미포함.
- **미디에이션** — 실데이터 전엔 붙일 이유 없음(fill률·eCPM을 봐야 어댑터 선택이 근거를 가진다).
- **IAP(광고 제거·스킨)** — 소프트런치 미포함, 데이터 후.
