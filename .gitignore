# Godot 4 import cache / editor state
.godot/
*.uid

# macOS
.DS_Store

# Godot export output
export/
build/

# 병렬 작업 worktree(트렁크 안에 두되 git은 무시 — 스테이지/무한 트랙, C63)
worktrees/
android/

# 팀 내부 스프린트 보드(심사 계정 초대 시 노출 방지)
docs/local/

# 홍보 소재(트레일러·모집용 클립). 원본 영상이 수 메가바이트라 저장소에 안 올린다 —
#   저장소는 게임을 만드는 것만 갖고, 파는 데 쓰는 물건은 여기 둔다. build/와 달리 export가
#   지우지 않는 자리라 마스터 영상의 집이 된다.
promo/
__pycache__/

# 계측 수집 주소 — 저장소가 공개라 소스에 안 박는다. export에는 실린다(analytics.gd 상단 주석 참조).
analytics_endpoint.txt
