# 도파민 연출·시스템 보강 설계 (C90~)

> 스코프: **1(자기기록 순간을 판 전체 폭발로) + 3(오디오)**, 그 전제로 **클리어 리듬(계단 시차) + 점수 롤업**, 곁들임 **산개 불꽃**.
> 근거: 블록블라스트 원본 화면녹화 2편을 프레임 단위로 분석(요약 아님, `verify-reference-claims-on-raw-frames`).

---

## 0. 레퍼런스 근거 (프레임 실측)

두 영상은 우리 두 축과 1:1 대응한다.
- **영상2 = Endless(점수)** ↔ 우리 무한모드
- **영상1 = Adventure(수집 퀘스트)** ↔ 우리 Gem Rush

### 0.1 블블이 잘하는 것 (관측)
1. **자기기록 갱신 = 게임 내 최대 폭발** — 실시간 점수가 크라운(=자기 최고점)을 넘는 *그 순간*, 판 전체가 터짐: 청록 네온 보드 테두리 + 금 왕관+월계관+갓레이 + 컨페티 + 가로로 쓸고 지나가는 빨간 리본(새 점수) + 하단 "Combo N". **결과화면 아니라 플레이 중간에.**
2. **한 클리어에 신호 겹겹이** — 네온 테두리(등급색) + 불꽃 **다발(랜덤 위치 3+개 동시)** + 판 전역 앰비언트 스파클 + 단어별 색칠 배너 + 콤보 스택.
3. **콤보 숫자 말고 '종류'를 칭찬** — "Strong!", "Legendary!", 마일스톤 "50% Done" 등.
4. **클리어 직전 무지개 예열** — 완성 줄이 클리어 전에 무지개로 점등·맥동.

### 0.2 핵심 발견 — 타이밍의 '계단식 시차' (20fps 실측, 프레임당 50ms)
한 번의 클리어가 **5개 미세 비트**로 쪼개져 순차 등장. 이 시차가 '리듬'이다.

| 비트 | ~시점 | 내용 |
|---|---|---|
| ① | t=0 | 조각 착지, 완성 줄 무지개 점등(예열) |
| ② | +100ms | 네온 테두리 번쩍 + 성취 단어가 **쓸며** 들어옴 |
| ③ | +200ms | 그 아래 **콤보 숫자가 두 번째 비트로 따로 팝인** (단어와 별개 2행) |
| ④ | +250ms | 블록 소멸 + **불꽃 다발**(위치별 시차)로 피어남 |
| ⑤ | +300~800ms | 점수가 **또르르 굴러 오름**(스냅 아님) |

### 0.3 우리 현황 (코드 확인) — 격차
- **리듬**: `_burst_lines`(Main.gd:1583~)에서 `flash_timer`·`praise_t`가 파괴 순간 **동시** 발화 = 한 박. 단어와 콤보가 한 덩어리. → 블블의 계단 시차 없음. 주석은 "겹치지 않게(Block Blast 원칙)"인데 *겹침만 피하고 한 박으로 뭉쳐* 리듬을 잃음(과교정).
- **점수**: 즉시 스냅. rolling 변수 없음(grep 0건).
- **PB 순간**: `_draw_pb_burst`(Main.gd:2746~)는 점수 카드(293,56)에 조준한 작은 링 2개+방사광, 옆 카드 안 넘게 반경 캡. → 게임 최대 순간을 **코너에서 속삭임**.
- **불꽃**: `_fire_climax`(Main.gd:1620~)는 **중앙 3중 충격파** 하나. → 블블은 랜덤 위치 다발+앰비언트.
- **오디오**: **0.** `sound_on`은 저장만("오디오 미구현"), `Main.tscn`에 AudioStreamPlayer 노드 자체 없음.

---

## 1. 설계 원칙 (기존 결정 존중 — 위반 금지)

- **기본 UI 무간섭(occlude-don't-mutate)** — 라벨 스왑·크기 바운스로 기본 HUD를 바꾸지 않음. 축하는 전용 오버레이 레이어로 *가릴 뿐*. [`transient-celebration-overlay-not-base-ui`]
- **색은 시각 전용** — 색-회로 기계 축 부활 금지. "2 Colors Gone" 같은 색-매칭 콜아웃은 **매핑 안 함**(우리엔 색 회로 없음). [`color-is-cosmetic-final`]
- **콤보=스펙터클, 난이도 동결** — 연출만 키움. 콤보를 추적/난이도 레버로 만들지 않음. [`combo-is-spectacle-bonus`]
- **텍스트 아닌 색으로 신호** — 상태는 기존 요소의 색·테두리로. [`hud-signal-by-color-not-text`]
- **결정성/회귀 byte-identical** — `tools/regress.gd` 시드 재현이 깨지면 안 됨. 연출 `randf()`는 로직 RNG 스트림을 소비하면 안 된다(⚠ 3.6 리스크 참조). [`gamemode-director-seam`]
- **검증은 창 모드 확대 렌더** — `--headless`는 렌더텍스처 null로 크래시. [`godot-pixel-verify-needs-window`, `zoom-renders-to-judge-ui`]

---

## 2. 워크스트림

### A. 클리어 리듬 재설계 — 계단 시차 (전제, 최우선)

**목표**: 파괴→단어→콤보→불꽃→점수롤업을 각 50~100ms 시차로 순차 등장. 블블 0.2 표를 우리 자산에 매핑.

**현 타임라인 유지** (`_begin_resolve` Main.gd:1487~): 충전홀드(콤보비례)→순차파괴→로켓→링물결. 이건 블블 ①(예열)보다 오히려 풍부하니 **유지**. 바꾸는 건 파괴 *이후* 텍스트/불꽃/점수의 비트 배치.

**변경**:
1. `_burst_lines`에서 지금 동시에 켜는 것들을 오프셋 큐로 분리. `praise_t`(단어)와 새 `combo_num_t`(콤보 숫자)를 **~120ms 시차**로 팝인 → 블블 ②③의 2박 스택 재현.
2. 콤보 숫자를 단어 아래 별행으로(단어=등급색, 숫자=금). 지금은 단어 하나뿐 → 숫자 비트 신설.
3. 불꽃(축포/impacts)은 파괴 직후가 아니라 단어 착지 직후(~+250ms)에 다발로(워크스트림 D).
4. 점수 롤업(B)이 마지막 비트로 흐름.

**타이밍 테이블(초안, 튜닝 대상)**:
| 비트 | 오프셋 | 트리거 |
|---|---|---|
| 파괴 | 0 | `_burst_lines`(기존) |
| 단어 팝인 | +0.06 | `praise_t` 시작 지연 |
| 콤보숫자 팝인 | +0.16 | 신설 `combo_num_t` |
| 불꽃 다발 | +0.22 | D의 산개 스폰 |
| 점수 롤업 시작 | +0.10 | B, ~0.5s에 걸쳐 도착 |

> 상수는 `CHARGE_*`/`FLASH_DUR`/`PRAISE_DUR`(Main.gd:35,36,54~56) 옆에 리듬 오프셋 블록으로 신설.

### B. 점수 롤업 카운터 (전제 — A의 마지막 비트이자 C의 조건)

**목표**: 표시 점수가 실제 점수로 또르르 기어오름.

**구현**:
- `var endless_score_shown: float`(신설) → 매 프레임 `endless_score`로 이징(속도: 남은 차이의 ~12/s 또는 고정 pts/s, 큰 점수도 ~0.6s 내 도착하게 clamp).
- HUD 점수 그리기는 `endless_score`(정수) 대신 `roundi(endless_score_shown)` 사용.
- ⚠ **PB 돌파 발화 이전** 문제: 현재 `_add_endless_score`(Main.gd:668)는 *실제* `endless_score`가 `endless_best`를 넘는 순간 `pb_pop_t`를 켬. 롤업이 붙으면 **표시 숫자가 아직 안 넘었는데 폭발**한다. → PB 폭발 트리거를 `endless_score_shown`이 `endless_best`를 넘는 순간으로 **이동**(엣지 플래그 `endless_beat_best`는 유지). 그래야 "숫자가 크라운으로 기어오르다 넘는 순간 팡"이 성립.

### C. 자기기록 순간을 판 전체 이벤트로 (스코프 #1)

**목표**: 코너 속삭임 → 판 전체 크레셴도. **단, HUD 카드는 안 건드림**(오버레이만).

**트리거**: B의 이동된 조건(표시 점수가 `endless_best` 돌파). 기존 `pb_pop_t`(Main.gd:465, PB_POP_DUR=1.15)를 **승격**해 재사용.

**레이어(전부 오버레이, 최상단)**:
1. **보드 테두리 네온 플래시** — 보드 사각 외곽선이 등급색으로 확 밝아졌다 사그라듦. (블블의 청록/마젠타 테두리. `hud-signal-by-color-not-text` 결.) 신규 draw, HUD 무관.
2. **왕관 + 월계관 + 갓레이** — 보드 중앙~상단에 금 왕관, 뒤로 방사 광선. 기존 `_draw_pb_burst`의 방사광을 판 스케일로 확대.
3. **리본 배너(새 점수)** — 가로로 쓸고 지나가는 띠 + 돌파 점수. 블블의 빨간 리본. 짧게 등장→체류→페이드.
4. **산개 컨페티** — 기존 `_spawn_confetti`(Main.gd:1948) 재활용, 보드 상단에서 쏟아짐.

**무간섭 확인**: 위 4개 전부 게임 위 오버레이. `_draw_pb_sticker`(Main.gd:2771, "👑 신기록!" 상주 스티커)는 **유지** — 순간 폭발은 원샷, 스티커는 이후 계속 갱신 중 표식으로 상주(현 설계 그대로).

**규모 가드**: 반경 캡(옆 카드 회피)은 **폭발엔 해제**(오버레이라 카드를 *덮어도* 안 바꿈), 단 스티커/상주 요소는 기존 제약 유지.

### D. 산개 불꽃 (스코프 #2 곁들임)

**목표**: 중앙 단일 → 랜덤 다발 + 앰비언트.

**변경**:
- `_fire_climax`(Main.gd:1620): 중앙 3중 충격파 유지하되, **보드 랜덤 위치 3~5곳에 시차(각 40~80ms) 불꽃 버스트** 추가 + 판 전역 앰비언트 스파클(작은 별, 저알파, 드문드문). 전멸 순간용.
- `_burst_lines` 축포(Main.gd:1593~): 콤보↑에서 파편을 완성 줄 *중심 하나*가 아니라 여러 셀에서 튀게(현재 셀별로 이미 튀나, 앰비언트 스파클 레이어 추가).
- ⚠ RNG: 산개 위치는 연출 전용 RNG(3.6).

### E. 오디오 (스코프 #3)

**목표**: 위 각 비트에 소리. 무음 → 리듬이 소리로도 들림.

**아키텍처**:
- `AudioStreamPlayer` **풀**(동시발음용 N개, ~8) 또는 절차적 `AudioStreamGenerator`. 초기엔 **짧은 샘플 세트**가 빠름(절차적 톤은 콤보 음정 램프엔 좋지만 타격감 샘플이 우선).
- 재생 진입점 하나(`_sfx(name, pitch=1.0)`)로 모아 `sound_on` 소비(Main.gd:377). 회귀/헤드리스에선 무음이어야(노드 없거나 `sound_on` 게이트).

**비트별 매핑(초안)**:
| 순간 | 소리 |
|---|---|
| 조각 착지 | 짧은 "톡" |
| 순차 파괴(셀) | 스타카토 "틱"(링 물결이 리듬) |
| 콤보 단어 팝인 | 등급별 **음정 상승**(GOOD 낮게 → UNREAL 한 옥타브 위) |
| 로켓 발사 | 저역 "슉→쿵" |
| 피격/처치 | 타격 "팍" + 처치 시 히트스톱에 맞춘 순간 무음+베이스 |
| 점수 롤업 | 구르는 "티티티틱" |
| PB 돌파 | 크레셴도 "팡" + 상승 아르페지오 |
| 전멸 climax | 큰 폭발 |

**에셋**: CC0/자작 짧은 샘플 or 절차적. 라이선스·용량(모바일) 주의. 커스텀 draw 게임이라 UI 사운드도 코드 트리거.

---

## 3. 리스크 / 열린 질문

1. **회귀 결정성** — 연출 `randf()`가 게임플레이 RNG 시드를 소비하면 `regress.gd` byte-identical이 깨진다. **연출 전용 RNG(`RandomNumberGenerator` 별 인스턴스)** 를 도입하거나, 기존 debris/confetti가 이미 어떻게 처리하는지 먼저 확인(이미 `randf()` 쓰는데 회귀 통과 중이라면 로직-무관 경로일 것 — 재확인 후 답습).
2. **오디오 에셋 소싱** — 샘플 라이선스/용량 vs 절차적 생성. 첫 스파이크에서 결정.
3. **리듬 튜닝은 실측 의존** — 오프셋 값(2.A 테이블)은 창 모드 고프레임 녹화로 블블 옆에 붙여 조정. 숫자는 초안.
4. **모바일 세로 반응형** — 리본/왕관 오버레이가 세로 짧은 창·노치에서 안 잘리게 `board_y` 파생 좌표 사용([`mobile-plumbing-frame-and-touch`] 결).
5. **PB 폭발 트리거 이동(2.B)** 이 `endless_beat_best` 엣지 1회성과 충돌 없는지(롤업 중 여러 프레임 걸쳐 넘어도 1회만).

---

## 4. 순서 / 마일스톤

1. **A + B (리듬 + 롤업)** — 리듬이 살아야 나머지가 올라탐. 롤업은 C의 전제.
2. **C (PB 판 전체 폭발)** — 스코프 #1의 본체.
3. **D (산개 불꽃)** — 저비용 곁들임, C와 자산 공유.
4. **E (오디오)** — 리듬이 잡힌 뒤 각 비트에 매핑. 별도 스파이크(에셋 결정 포함).

각 단계 끝에 **창 모드 확대 렌더**로 검증, **블블 나란히 녹화** 대조, **`regress.gd` byte-identical** 통과 확인.

---

## 5. 손대는 지점 (코드 인덱스)

| 워크스트림 | 함수/변수 | 위치 |
|---|---|---|
| A 리듬 | `_burst_lines`, `praise_t`, 신설 `combo_num_t` | Main.gd:1583, 441, 신설 |
| A 상수 | 리듬 오프셋 블록 | Main.gd:35~36,54~56 옆 |
| B 롤업 | 신설 `endless_score_shown`, HUD 점수 draw | Main.gd:345 근처, 점수 그리기 |
| B 트리거 이동 | `_add_endless_score` PB 조건 | Main.gd:668~670 |
| C PB 폭발 | `_draw_pb_burst` 승격, 신규 테두리/리본/왕관 draw | Main.gd:2746 |
| C 스티커 | `_draw_pb_sticker`(유지) | Main.gd:2771 |
| D 불꽃 | `_fire_climax`, `_burst_lines` 축포 | Main.gd:1620, 1593 |
| E 오디오 | 신설 `_sfx()` + AudioStreamPlayer 풀, `sound_on` 소비 | Main.tscn(노드 신설), Main.gd:377 |

---

## 진행 로그

### ✅ A(리듬·텍스트) + B(점수 롤업) — 커밋됨
- **A 리듬**: 파괴 순간 동시발화하던 칭찬 단어를 `PRAISE_LEAD=0.09` 지연(파괴→단어 한 박). 등장은 **오버슛 스냅(0.55→~1.1→1.0) + 흰빛 펀치**(첫 ~0.07s 흰색→등급색). 위치는 402 고정 폐기 → **보드 세로 정중앙(board_y 파생, 반응형)**. BB는 텍스트를 보드 중심에 띄움(원본 프레임 확인).
- **텍스트 렌더**: 공유 `_draw_text_outlined`(얇은 검정) 대신 칭찬 단어 전용 **이중 아웃라인 스티커**(어두운 겹 + 흰 겹 + 색 채움) — 어두운 판/밝은 링 양쪽 다 대비 확보.
- **폰트**: 축하 전용 `_font_display` = **Baloo2 ExtraBold**(통통 라운드, BB 캔디감). `_ready`서 FontVariation wght=800 로드, 미로드 시 폴백.
- **등급색 수정**: GOOD 밝은노랑→**리치골드(1.0,0.72,0.06)**, UNREAL 청백→**일렉트릭블루(0.24,0.68,1.0)** — 흰 아웃라인에 묻히던 밝은 두 등급만 밝기↓(hue 유지). 중간 등급은 원래 잘 읽혀 유지.
- **B 롤업**: `endless_score_shown`이 실제 점수로 이징(rate 9.0/min 260). HUD·크라운이 롤업 값. **PB 돌파 발화를 실제가산→표시숫자가 크라운 넘는 순간으로 이동**(차오름→돌파). 검증: 롤업 0→5000, 크라운(3200) 넘는 순간 PB 발화 확인.
- 검증: 회귀 `regress.golden.txt` **byte-identical**(연출은 RNG 무소비·로직 무영향), 창모드 확대 렌더로 리듬·색·중심·폰트 확인.

### 백로그
- ⚠**폰트 용량**: `fonts/Baloo2.ttf`가 683KB **가변 폰트 풀셋**. 출시 전 **라틴 + ExtraBold 단일 웨이트로 서브셋**(~100KB대). 지금은 미착수.

### 다음 = C(PB 판전체 폭발)
- 현 `_draw_pb_burst`는 점수 카드에 조준한 작은 링+방사광(코너 속삭임). C88 병합서 카드가 중앙 이동하며 좌표(293,56) 어긋남도 있음.
- 목표: 판 전체 오버레이 폭발(보드 테두리 네온 + 왕관/월계관/갓레이 + 리본[돌파 점수] + 산개 컨페티). HUD 카드 무간섭.
