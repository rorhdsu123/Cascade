# Cascade / BlockCastle — 작업 규약

이 파일은 **여러 세션이 동시에 이 저장소를 만질 때 어긋나는 것**만 적는다.
설계 판단·과거 결정은 여기 말고 각 트랙 문서(`AUDIO_PLAN.md`, `docs/UI_ART_PLAN.md` 등)에 있다.

## 병렬 작업 구조

트렁크는 `main`(저장소 루트), 병렬 트랙은 `worktrees/<이름>`에 워크트리로 붙어 있다.
`audio · juice · stage · endless · plumbing · haptic · color` — 각각 `track/<이름>` 브랜치다.

워크트리들은 **저장소 하나를 공유한다.** 그래서 어느 워크트리에서든 `git log --all`을 쓰면
다른 트랙의 커밋이 전부 보인다. 플래그 없는 `git log`가 자기 브랜치만 보여줄 뿐이다 —
"다른 트랙이 뭘 했는지 알 수 없다"는 말은 틀렸다. 확인할 땐 `--all`을 붙일 것.

## 결정 ID — 트랙마다 다른 접두사

커밋 제목 끝에 결정 ID를 괄호로 단다: `fix: 죽은 배경음 토글 제거 … (C127)`.
메모리·보드·문서가 이 ID로 결정을 가리키므로 **유일해야 한다.**

**번호를 눈으로 세지 말고 스크립트를 돌린다:**

```
python3 tools/next_id.py           # 현재 워크트리의 다음 ID
python3 tools/next_id.py --list    # 이 트랙이 지금까지 쓴 ID 전부
```

수열은 **트랙마다 따로** 간다. 트렁크가 `C`(C127까지 사용), 나머지는
`A`(audio) · `J`(juice) · `S`(stage) · `E`(endless) · `P`(plumbing) · `H`(haptic) · `L`(color).

- ID는 **병합해도 안 바뀐다.** 손잡이는 영구적이고, 접두사가 그 결정이 어디서 나왔는지 말해준다.
- 새 트랙을 만들면 `tools/next_id.py`의 `PREFIX` 표에 안 겹치는 글자를 추가한다.

**왜 전역 카운터를 버렸나.** 2026-08-03 하루에 세 번 겹쳤다(C124·C125가 audio와 juice에서
각각 다른 결정에 붙었다). 원인은 정보 부족이 아니다 — `--all`이면 다 보였다. 원인은
**확인과 커밋 사이의 틈**이다: 두 세션이 같은 분에 각자 최대값을 읽으면 둘 다 같은 다음 번호를
집는다. 긴 세션은 번호를 잡고 두 시간 뒤에 커밋한다. 규율로 못 메우는 종류의 문제라 수열을 쪼갰다.
빽빽하게 이어지는 하나의 수열은 **아무 쓸모가 없었다** — 필요한 건 유일성뿐이다.

⚠**레거시**: `C1`~`C127`은 트렁크와 트랙이 뒤섞여 쓴 구간이고 중복이 남아 있다
(`C100`·`C101`·`C102`·`C104`~`C114`·`C124`·`C125` 등). 옛 ID를 인용할 땐 날짜나 트랙을 같이
적어 어느 쪽인지 밝힐 것. `C128`부터는 트렁크 전용이다.

## 세션을 끝낼 때

"미병합 0"은 **그 순간의 사실일 뿐**이다 — 병렬 세션이 몇 분 뒤에 깬다.
마무리 전에 `git log --all`과 각 워크트리의 `git status`를 한 번 더 훑을 것.
