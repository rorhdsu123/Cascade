# Cascade — 설계 정본 (프로토 v1)

> 폴리오미노 팩킹 × 복리 연쇄 폭발 디펜스. 2026-07-09 아이디에이션에서 확정.
> 코드네임 `Cascade`(가제). 단일 진실 = 이 문서.

## 1. 개요
- **장르**: 로그라이트 디펜스 (턴제 심사숙고형)
- **플랫폼**: **모바일 무료 앱 (F2P, 리워드 광고 수익화)** — Godot 4.6 / GDScript, 세로 지향 화면. ⚠**C47에서 PC/Steam 프리미엄 → 모바일 F2P로 피벗**(근거·영향은 결정 로그 C47).
- **타깃**: 저아트·고시스템 / 폭발형 바이럴 (입소문 발견성). 수익 = **광고 우선(리워드 부활 + 인터스티셜) → IAP 후속(코스메틱)**. 후반 서지의 아까운 실패 → 리워드 부활이 광고 수익의 심장. 상세 §8·C50.
- **한 줄**: **폴리오미노로 방어 회로를 짜고, 한 방에 연쇄 폭발시켜 화면을 숫자 폭풍으로 청소하는 디펜스.**
  = 테트리스의 공간 퍼즐 × Balatro의 복리 폭발 × 디펜스의 스테이크.

## 2. 확정된 축 (모든 판단의 기준)
- **운 ↔ 숙련 축에서 "숙련" 확정.** 주사위·운기반 폐기, 무작위 최소화. (원래 근거: PC 프리미엄 구매자는 숙련을 사러 옴. ⚠**C47 F2P 피벗 후 근거 갱신** — 숙련 축은 유지하되, 이제 그 이유는 "난이도 벽이 RNG가 아니라 실력이 바닥나는 지점이라야 광고 부활이 정당하고 숙련과 안 싸운다"이다. 상세 C47.)
- **폭발형 바이럴 확정** (조용한 컬트 ITB형 아님). 도파민 = **시스템적 복리 폭발**(아트/juice 아님) = Balatro가 저아트로 도파민 터뜨린 방식 = 사용자 강점(설계·수치).
- **도파민 ↔ 숙련 화해**: `숙련으로 쌓아서 → 결정적으로 터뜨린다`. 폭발이 잘 쌓은 것에 대한 보상이라 둘이 같은 방향.
- **차별**: Random Dice/Lucky Defense(운·병합)와 다르고, ITB(조용함)보다 시끄럽고 바이럴. 명제 = **"디펜스인데 Balatro식 복리 폭발"**.

## 3. 핵심 재미
> **"조각을 끼워 회로를 키우고, 언제 터뜨릴지 판단하고, 연쇄로 청소하는" 루프.**
> 특히 **"한 개 더 놓아 회로 키울까 vs 지금 터뜨릴까"** 긴장 (Balatro의 "핸드 언제 낼까"에 대응. 도박 아닌 판단).

## 4. 코어 루프
```
① 웨이브 진행   적 전진, 회로가 아직 방어 안 함 (조용한 압박·누수 리스크)
② 조각 배치     회로 충전 — 숙련: 같은 색 사슬을 길게 잇기 + 자원(적 전진) 관리
③ 발동!(수동)   모든 회로가 사슬로 터짐, 크기 복리 → 숫자 폭발 → 화면 청소  ★도파민
④ 보상          클리어→(v2: 조커/개조)→다음엔 더 큰 연쇄 (로그라이트 복리)
```

## 5. 프로토 v1 규칙 (1차 수치 = 가안, 전부 튜닝 대상)
| 항목 | 값 |
|---|---|
| 보드 | 8열 × 10행. 하단 = 거점 |
| 거점 HP | 20 |
| 템포 | 준-턴제 (트위치 배제) |
| 자원 모델 | **골드 없음. 조각 1개 놓을 때마다 전원 아래로 1칸 전진** = 무한 패킹 봉쇄 |
| 적 스폰 | 매 스텝 상단 랜덤 열에 웨이브 잔여 풀에서 1마리 (row 0). 웨이브당 총량 소진까지 |
| 웨이브 | W1 적 6 → 웨이브당 +2, HP 복리 ×1.25. **5웨이브 클리어=승. 거점 HP 0=패** |
| 적 처치 | 하단(row 10) 닿으면 거점 −1·소멸 |
| 조각 | 3색(R/B/Y) 테트로미노 중심, "다음 큐"에서 1개씩 무한, 회전 허용, 빈 칸 배치 |
| 회로 | 직교 인접 + **같은 색** = 한 회로 (색 잇기 = 패킹 퍼즐) |
| 발동 | **수동**(버튼/SPACE, 무료 액션·적 안 전진). 모든 회로 동시 폭발, 발동 칸 전부 비워짐(공간 회수) |
| 딜 공식 | 칸당 충전 10, **사슬배수 = 회로 크기 n → 회로딜 = 10n × n = 10n²** |
| 예시 딜 | 4칸=160 · 8칸=640 · 12칸=1,440 (지수 폭발감, 곡선 튜닝) |
| 광역 | 발동 총합(Σ 회로딜)을 **전 적에게** 광역딜 |

### UX 필수
- **현재 잠재딜(Potential) 상시 표시**: 지금 발동 시 총 딜 = Σ10n². 숫자 커지는 게 보여야 도파민 + "언제 터뜨릴까" 판단 근거.
- 발동 시 **떠오르는 숫자 + 간단 연쇄 이펙트**(라이트닝/플래시). 아트 아님, 구조 확인용.
- 다음 조각 미리보기, 웨이브/거점HP 표시.

## 6. 계측 포인트 (플테 판정 기준)
1. **[1급] "참을까/터뜨릴까" 긴장이 서는가** — 안 서면 게임 없음.
2. **연쇄 폭발이 "우와"인가** — ⚠**거짓부정 경계**: 프로토는 이펙트 0이라 밋밋해도, 구조(숫자 폭발+청소)가 성립하면 juice는 나중. **재미판정 아니라 루프성립 판정.**
3. **색 잇기가 퍼즐인가 노가다인가.**
4. **주의분산** — 조각 배치 ↔ 적 레인 핑퐁(MVP3서 1급 경계했던 것). 준-턴제라 완화되나 관찰.

## 7. 범위 (프로토)
- **IN**: 격자·3색 조각 배치·색연결 회로·수동 발동·복리 연쇄·에스컬레이팅 웨이브·거점 HP
- **OUT(나중)**: 조커/개조·아트/이펙트·위치기반 방어·상점/골드·위치 상성

## 8. 로드맵 (출시 정본 — C50 확정)

> ⚠ 구 로드맵("루프 성립 → 조커/개조 → juice → 위치기반 방어 → 메타/런")은 **폐기**. 조커/개조·로그라이트 런은 C24·[[no-meta-build]]에서 기각됨. 아래가 정본.

### 8.0 출시 전략
- **라이브서비스 듀얼코어**: **캠페인(스테이지)과 무한 모드가 둘 다 코어.** 캠페인 = 숙련·진행·authored 챌린지("다음이 보고 싶다"), 무한 = 표현·경쟁·하이스코어("내 최고를 깬다"). 서로 다른 동기를 맡고 **교차 공급**한다 — 공유 소프트 재화, 코스메틱 양쪽 적용, 하드 로스터(장갑·점착·분열, [[enemy-roster-and-readfree-tells]])는 후반 캠페인 + 랭크 무한 양쪽 등장.
- **데이터-먼저 소프트런치**: 두 기둥을 얇게-좋게 내고, 어느 쪽을 플레이어가 사랑하는지 실측 → 라이브옵스 예산을 그쪽에 집중. 콘텐츠 예산을 미리 못박지 않는 게 소프트런치의 존재 이유.
- **셸 = 리더보드-먼저**: 소프트런치 셸은 무한 점수 **리더보드**(경쟁 천장, Play Games/Game Center = 백엔드 0, 아트 트레드밀 0). 코스메틱 셸은 IAP 층으로 후속.

### 8.1 수익 모델 (광고 우선, IAP 나중)
소프트런치 = 광고 위주. IAP(코스메틱)는 데이터 보고 후속.

| 광고 포맷 | 설명 | Cascade 적합성 |
|---|---|---|
| **리워드 광고** | 자발 시청 → 보상 | ★ 코어. 부활(C48)이 이미 이 형태. 비강제라 코지 톤 유지 |
| **인터스티셜** | 자연 브레이크에 전면 광고 | ○ 스테이지 전환·판 종료. **빈도 캡 필수**(과하면 코지 파괴) |
| 리워드 인터스티셜 | 스킵 가능+보고 나면 보상, 인트로로 opt-out | △ 인터스티셜의 온건판, 테스트 후보 |
| 배너 | 상시 띠 광고 | ✗ 코지 훼손 + 세로 보드 가림. 회피 |
| 앱 오픈 광고 | 앱 실행/복귀 시 | ✗ 첫인상 저해·공격적. 회피 |
| 네이티브 | UI에 녹인 광고 | △ 홈/상점 메뉴에 한해 후속 검토 |
| 오퍼월/플레이어블 | 오퍼 완료 보상 / 인터랙티브 | — 오퍼월은 코지 부적합, 플레이어블은 주로 UA(유저확보) 도구 |

→ **소프트런치 = 리워드(부활) + 인터스티셜(빈도 캡).** 배너·앱오픈은 코지 코어([[core-fantasy-cozy-dopamine]])와 세로 화면을 해쳐 제외. 추가 포맷은 실데이터·현행 AdMob 정책 조사 후 도입. IAP = 코스메틱(광고 제거·스킨), 소프트런치 미포함.

**광고 게이팅 (C55 확정) — 세 슬롯:** 원칙 = 강제 광고는 세션 경계 밖으로 안 나감, 리워드는 비강제. "재시작마다 강제 광고"는 **수익 함정**(코지 이탈 → D7 리텐션 = LTV 실체가 깎임 → 순수익 하락)이라 배제.
- **A. 거점사 → 리워드 부활 (주력 수익, 자발).** "광고 보고 이어가기" opt-in → 같은 런 계속(더 깊이=더 높은 점수). **리워드로 무제한**(하드 캡 없음 — 자발 시청이라 '어려울수록 안 본다'는 **플레이어 의지 자체가 throttle**). C53 "정직한 그라인드 허용"과 일관. 막힘사는 C48대로 부분구제(하단 3줄)만. ⚠세션 늘어짐·광고 피로가 실데이터에 보이면 런당 캡(2~3회)으로 조일 여지(튜닝).
- **B. 재시작(새 런) → 인터스티셜, 빈도 캡.** 완전 종료 후 다음 런 = 자연 브레이크("판 종료" 슬롯). 캡 = **세션 첫 2판 광고 없음(첫인상 보호) + 이후 "3판마다 or 3분마다 중 늦은 쪽" 1회.**
- **C. 점수-리깅 리워드는 featured 시드엔 금지.** "광고 보고 점수 2배" 류는 리더보드 공정성 파괴 → 오늘의 시드엔 없음. 리워드 부스터는 프리·코스메틱 재화에만(후속, 소프트런치 미포함).
- **귀결:** 주력 엔진 = 리워드 부활 물량(자발·코지·정당·고eCPM, 죽음 횟수=인게이지먼트에 비례해 스케일). 보조 = 캡 인터스티셜(런 경계). 재시도는 마음껏 하되 부활 리워드로 자연 수익화, 리텐션은 안 깎임.

### 8.2 개발 로드맵 (대략 기간)
> 팀: **기획·개발 = 유저 + Claude / 아트 = 별도 디자이너(병렬 트랙)**. 기간은 코어가 이미 완성된 전제의 추정치. 아트 트랙은 개발과 동시 진행.

**개발 트랙 (유저+Claude), 순차:**
| Phase | 내용 | 대략 |
|---|---|---|
| **0. 모드 아키텍처** | `modes/game_mode.gd` director를 Main.gd에 통합, `StageMode`(현 STAGES 래핑)+`EndlessMode`, `modes/` 커밋 | ~3~5일 |
| **1. 무한 코어 + 리더보드** | EndlessMode(승리 없이 램프, 레버=전진속도 [[cascade-difficulty-lever-is-advance-speed]]), **점수=폭발마다 처치×콤보(C52)**, `game_rng` 분리 + **오늘의 featured 시드**(데일리/프리 통합 C53), 로컬 베스트 → 플랫폼 리더보드 | ~1.5주 |
| **2. 모바일 배관** | 터치 입력(드래그 C32→`InputEventScreenDrag`)·세로 실기기 사이징·세이브/영속·KR 폰트(C22③)·**광고 SDK**(리워드+인터스티셜, AdMob+미디에이션) | ~2주 |
| **3. 캠페인 확장 + 소프트런치 셸** | 스테이지 5→**12~15**(데이터+`sim.gd` 곡선 검증)·홈/리더보드 UI/설정/통계 | ~1~1.5주 |
| **4. 런치 옵스** | 애널리틱스(D1/D7·광고 fill·부활율 C48ⓓ)·스토어 에셋·빌드 파이프라인(안드로이드 먼저)·1지역 소프트런치 | ~1~1.5주 |

**→ 소프트런치까지 대략 6~8주.**

**아트 트랙 (별도 디자이너, 병렬):** 스테이지 시각 변주·적/보드 폴리시·코스메틱 스킨 시드·**스토어 에셋(아이콘·스크린샷)**·UI 아트. 스토어 에셋은 Phase 4까지 필요.

**소프트런치 이후 (데이터 기반 라이브옵스):** 코스메틱 경제 + IAP · 하드 로스터 랭크 사다리 · 시즌 리셋·이벤트 시드(오늘의 featured 시드 리더보드는 소프트런치 셸에 이미 포함, C53) · 추가 광고 포맷 · **데이터가 가리키는 기둥(캠페인 or 무한) 집중 확장.**

## 결정 로그
- **C55** (2026-07-18, 광고 게이팅 확정 — 리워드 부활 주력 + 캡 인터스티셜, 재시작 강제광고 배제) **C53 통합모드의 수익 손잡이 확정.** 원칙: 강제 광고는 세션 경계 밖으로 안 나감, 리워드는 비강제([[core-fantasy-cozy-dopamine]]). **핵심 논거 = "재시작마다 강제 광고"는 수익 함정** — 코지 F2P의 LTV 실체는 D7 리텐션, 재시작 인터스티셜 남발은 코지 이탈→DAU 하락→순 광고수익 감소. 반면 리워드 부활은 플레이어가 원해서 봄(고완주율·고eCPM)+비강제(코지 유지)+물량이 죽음 횟수=인게이지먼트에 비례해 스케일 → **절제가 곧 수익 최대화.** **세 슬롯**: **A. 거점사→리워드 부활**(주력, 자발, 같은 런 이어가기, **무제한**=자발 시청이 스스로 throttle, 하드 캡 없음; 막힘사는 C48 부분구제만; 실데이터서 세션 늘어짐 보이면 런당 2~3회 캡 여지). **B. 재시작→인터스티셜 빈도 캡**(세션 첫 2판 광고없음+이후 3판/3분 중 늦은쪽 1회). **C. 점수-리깅 리워드(광고→점수 2배 류) featured 시드엔 금지**(리더보드 공정성; 부스터는 프리·코스메틱 재화만, 소프트런치 미포함). 유저 "재시도 계속 하게+수익" 요구를 부활 리워드 물량으로 충족하되 리텐션 무손상. SPEC §8.1에 정본화. [[endless-mode-design]] [[core-fantasy-cozy-dopamine]]
- **C54** (2026-07-18, 캠페인 6스테이지 아크 authoring — C51 ⑥ 실행, pool 다이얼로 단조 난이도 곡선) **C51이 확정한 '난이도=조각 풀' 방향(기전은 c965f10에서 `_pool_piece` 정식 승격)을 실제 아크로 authoring. 유저 결정 = 옵션 A(현 적 로스터 + pool 다이얼이 난이도 담당, 점착/분열은 후속 act-3 확장).** **① 프리셋 3종**: `POOL_RICH`/`POOL_STD`/`POOL_LEAN` — 공통 '변주 base'(테트로미노·직사각 포함 = 손맛)에 line-maker(I5) 가중만 20·11·4로 차등. 주 dial = I5 비율(C51 혼합 스윕: basic-only 5%→84% 단조). **② 6스테이지 = 기존 5 비트 + 신규 1**: 1 첫방어선(basic·RICH·hp7 온보딩) → 2 무리(swarm·RICH·hp3) → 3 속공(fast·STD·hp3) → 4 **줄 굶김[신규](basic/swarm·LEAN·hp3 = 퍼즐 축 고립: 새 적 없이 조각 분포만으로 압박)** → 5 장갑(tank·STD·hp2) → 6 총력전(all·STD·hp2). 커리큘럼: 디펜스 비트(적)와 퍼즐 압박(pool)을 겹쳐 authoring. **③ sim 검증**(`tools/sim.gd`, N=300, DDA OFF=숙련 기준): 승률 **90→69→54→37→26→23% 단조 하강** — pool+core_hp가 2D 면으로 매끄러운 램프. 줄 굶김 = **거점사 194 / 막힘사 8**(300판) → LEAN이 공격 처리량을 굶겨 **거점 쪽으로** 죽인다 = C51 ④ 교정 프레임("조각은 막힘문이 아니라 처리량 손잡이")을 실제 authored 스테이지에서 실증. 풀트레이즉사 0%·죽을때보드 ~35/64 = 건강한 실패 프로필(값싼 죽음 없음). **④ UI**: 6타일이 PLAY_BTN(y=742) 침범 → `SEL_H` 76→70·`SEL_GAP` 12→10(6번째 하단 702<742), 창 렌더로 확인([[godot-pixel-verify-needs-window]]). **⑤ ⚠캐비엇**: 봇=강한 stuck 회피 숙련봇 → 절대치가 아니라 **곡선 shape(단조)만 이관**. 초보 절대 난이도는 소프트런치 데이터로 캘리브레이트(ROADMAP 데이터-먼저). tail(s5·6 = 26·23% 숙련)이 코지 코어엔 셀 수 있음 → 데이터 후 s5·6 pool을 RICH 쪽으로 한 눈금 풀 여지(1파라미터+재sim). **⑥ 남은 것**: 점착/분열 구현 후 act-3(스테이지 7·8) 확장 = 랭크 무한 로스터 겸(C51 ⑤ 점착=키스톤) · 12~15는 실데이터 후(C50 ⑤) · 사람 플테. [[stage-difficulty-via-piece-pool]] [[core-fantasy-cozy-dopamine]] [[cascade-difficulty-lever-is-advance-speed]] [[verify-reference-claims-on-raw-frames]]
- **C54** (2026-07-18, 캠페인 6스테이지 아크 authoring — C51 ⑥ 실행, pool 다이얼로 단조 난이도 곡선) **C51이 확정한 '난이도=조각 풀' 방향(기전은 c965f10에서 `_pool_piece` 정식 승격)을 실제 아크로 authoring. 유저 결정 = 옵션 A(현 적 로스터 + pool 다이얼이 난이도 담당, 점착/분열은 후속 act-3 확장).** **① 프리셋 3종**: `POOL_RICH`/`POOL_STD`/`POOL_LEAN` — 공통 '변주 base'(테트로미노·직사각 포함 = 손맛)에 line-maker(I5) 가중만 20·11·4로 차등. 주 dial = I5 비율(C51 혼합 스윕: basic-only 5%→84% 단조). **② 6스테이지 = 기존 5 비트 + 신규 1**: 1 첫방어선(basic·RICH·hp7 온보딩) → 2 무리(swarm·RICH·hp3) → 3 속공(fast·STD·hp3) → 4 **줄 굶김[신규](basic/swarm·LEAN·hp3 = 퍼즐 축 고립: 새 적 없이 조각 분포만으로 압박)** → 5 장갑(tank·STD·hp2) → 6 총력전(all·STD·hp2). 커리큘럼: 디펜스 비트(적)와 퍼즐 압박(pool)을 겹쳐 authoring. **③ sim 검증**(`tools/sim.gd`, N=300, DDA OFF=숙련 기준): 승률 **90→69→54→37→26→23% 단조 하강** — pool+core_hp가 2D 면으로 매끄러운 램프. 줄 굶김 = **거점사 194 / 막힘사 8**(300판) → LEAN이 공격 처리량을 굶겨 **거점 쪽으로** 죽인다 = C51 ④ 교정 프레임("조각은 막힘문이 아니라 처리량 손잡이")을 실제 authored 스테이지에서 실증. 풀트레이즉사 0%·죽을때보드 ~35/64 = 건강한 실패 프로필(값싼 죽음 없음). **④ UI**: 6타일이 PLAY_BTN(y=742) 침범 → `SEL_H` 76→70·`SEL_GAP` 12→10(6번째 하단 702<742), 창 렌더로 확인([[godot-pixel-verify-needs-window]]). **⑤ ⚠캐비엇**: 봇=강한 stuck 회피 숙련봇 → 절대치가 아니라 **곡선 shape(단조)만 이관**. 초보 절대 난이도는 소프트런치 데이터로 캘리브레이트(ROADMAP 데이터-먼저). tail(s5·6 = 26·23% 숙련)이 코지 코어엔 셀 수 있음 → 데이터 후 s5·6 pool을 RICH 쪽으로 한 눈금 풀 여지(1파라미터+재sim). **⑥ 남은 것**: 점착/분열 구현 후 act-3(스테이지 7·8) 확장 = 랭크 무한 로스터 겸(C51 ⑤ 점착=키스톤) · 12~15는 실데이터 후(C50 ⑤) · 사람 플테. [[stage-difficulty-via-piece-pool]] [[core-fantasy-cozy-dopamine]] [[cascade-difficulty-lever-is-advance-speed]] [[verify-reference-claims-on-raw-frames]]
- **C53** (2026-07-18, 데일리/프리 2갈래 → 무한 하나로 통합 + '시드=동일 판' 배관 발견) **C52 ⑦의 '2갈래(데일리 랭크 / 프리 캐주얼)'를 유저 수익 우선 요구로 재설계 — 갈래를 합친다.** **① 계기**: 유저 "데일리 1회 = 광고 수익 부족, 계속 재시도하게 하고 싶다". **② 상충 명시(못 피함)**: 리더보드 하나는 [콜드리드 실력(1회·부활없음 — 명예·바이럴·저수익) XOR 최적화/그라인드(무제한 재시도·부활 — 고수익·'순수실력' 아님)] 중 **하나만** 측정. 무제한 광고 재시도와 순수 콜드리드는 같은 판에서 양립 불가(무제한 열면 상위권=제일 많이 간 사람). 단 이건 rig(게임이 몰래 조작=배신)와 **다른**, 모두에게 열린 정직한 그라인드 → 배신감 없음. **③ 결정 = 통합**: ⓐ 무한 하나로 합침 — 무제한 재시도 + 거점사 광고부활(C48) = 수익 엔진, 어디서나 열림. ⓑ 데일리 = 별도 모드 아님, 그 위 **'오늘의 featured 시드 + 리더보드' 스킨** — 원하는 만큼 플레이, **당일 최고점 제출**. ⓒ 프리(랜덤 시드) = 연습터·순수 코지로 잔존(역시 광고 수익화). → 콜드리드 순수성 포기, 리더보드 = "오늘 판을 누가 제일 잘 갈았나"(최적화 경쟁 = 성공 모바일 데일리 다수의 형태). 수익 볼륨은 애초에 데일리가 아니라 **하루종일 도는 무한/프리가 캐리** → 데일리가 1회여도 수익 타격 작음(걱정은 과장). **④ ★기술 발견 — '시드만 박으면 전원 동일 판'은 지금 배관으론 거짓**(Main.gd 코드 검증, [[verify-reference-claims-on-raw-frames]]): 세 원인 — ⓐ 조각이 **보드-적응형**(`_make_piece`가 `_free_cells()`로 티어 정하고 `_piece_fits_at_least`로 지금 보드에 놓을 수 있는 조각만 후보) → 뽑는 조각이 내 보드에 반응, 보드 갈리면 같은 RNG위치서 다른 조각. ⓑ **코스메틱 `randf`(죽음파편 처치당 7회+)가 게임플레이 `randi`와 같은 전역 스트림** → 처치수(=실력) 갈리면 이후 조각큐·스폰 통째 desync. ⓒ 바닥유지 스폰이 `enemy_count`(내 처치=보드상태)에 반응. `tools/regress.gd`의 byte-identical은 **봇이 시드마다 동일 수를 두기 때문** — 인간 둘은 절대 같은 스트림 소비 안 함. **⑤ 배관 결정(남은 일 '시드 배관'의 정체)**: `game_rng: RandomNumberGenerator` **분리** = 조각 shape + 스폰(타입·열·swarm)에만 사용, 파티클/컨페티/셰이크는 전역 `randf` 유지(색은 게임플레이 무관 [[color-is-cosmetic-final]]). featured 시드는 **배치 인덱스 i만의 함수로 `piece[i]`/`spawn[i]`가 확정되는 결정적 트랙**(보드·적수 무반응, 바닥유지 훅 off). 반응형(적응 조각·바닥유지)은 프리/캠페인 몫. 부작용 둘: 고정 트랙이 가끔 꽉 찬 보드에 큰 조각 던짐 → **막힘사가 실력 실패로 부활**([[failure-face-skill-dependent]]의 숙련축=패킹과 정합, Free의 fit-필터가 막던 걸 데일리는 시험으로 되살림) / 트랙이 알려져 move-list 재생으로 **사후 점수 검증**(치트 방어) 쉬움. **⑥ 리더보드·UI**: 당일 최고점 제출. **'첫 시도 점수' 배지**(콜드리드 순수파용, 별도 보드 X = UI 비용 0). featured엔 DDA off(공정), **gentle 구제-DDA는 프리만**(랭크 절대 X 유지 — C52 ⑦ 계승). 공유 카드(깊이+점수+콤보 스파크라인) 유지. **⑦ 남은 것**: `game_rng` 분리 배관(`tools/regress.gd` 정합 유지) + featured 트랙 생성기 + 공유 카드 + 사람 플테. [[endless-mode-design]] [[core-fantasy-cozy-dopamine]] [[failure-face-skill-dependent]] [[gamemode-director-seam]]
- **C52** (2026-07-18, 무한모드 코지 코어 설계 — sim으로 램프·실패얼굴·점수 확정, DDA 대신 데일리 시드) **Phase 1 무한 착수, 산문 아이데이션([[design-ideation-prefers-prose]]) + 10년차 디펜스·퍼즐 리뷰 → sim 실측 연쇄. `modes/endless_mode.gd`(실코드) + `tools/endless_sim.gd`(봇 실력 사다리) 신설.** **① 램프 = 깊이(place_count) 결정적, 3구간**: A.코지 플래토(d<D1=40, 전진3·HP완만·basic+fast·서지없음) → B.물기(D1~D2, 서지 주기 등장·간격 깊이 따라 수축·로스터 다양화) → C.종말(d≥D2=90, 전진2 바닥·HP램프가 주압력→한방컷 깨짐→콤보 강제). 스테이지 dict 대신 함수가 스케줄. 전진 하한2·fast/swarm 면제(C47) 상속. 시계=깊이(시드 결정적 = 리더보드·데일리 공정성), 벽시계 아님. **② ★점수 = 폭발마다 (처치수 × 콤보) — 곱셈, 생존 덧셈 아님**: 생존만 보상하면 거북이가 최적 → 콤보 판타지 배신. 처치×콤보면 거북이는 점수 안 붙고 적을 쌓았다 콤보로 대청소해야 튄다 = **SPEC §3 "참을까/터뜨릴까" 긴장을 점수가 되살림**. 깊이는 덧셈 아니라 '폭발 기회 수'로 들어옴(적분 구간). 리더보드 정렬 = 이 점수(계수는 튜닝). C9/C23(SCORE 격하)은 캠페인 목표가독성 얘기 — 무한은 점수가 제자리. **③ CORE_HP=14 = '닳는 여유 게이지'(나이프 아님)**: 유저가 3=나이프 기각·25=너무 관대. 스윕({8,12,16,20,25}, 中봇 거점%: 73/67/62/55/48) → **14 확정**(中 63% 거점사·중앙깊이~160·밴드1.9×). 넉넉함↔거점지배↔판길이가 CORE_HP 한 축에서 충돌 → 75:25 과욕, 실현 과녁 = "engaged(中+)가 거점 과반". 낮을수록 거점↑·판짧음(광고 카덴스↑)·밴드↓. **④ ★실패 얼굴이 실력 의존적 — 숙련 아크(C51과 교차 확증)**: 어떤 HP에서도 약봇=막힘사(스스로 묻힘), 中봇=거점사(밀물에 뚫림). 초보는 막힘으로 죽으며 패킹을 배우고→마스터하면 디펜스(거점사)로 변함. **막힘문은 숙련에 bot-resistant**(C51 캠페인 실측과 동일 — 조각/HP 어찌 줘도 숙련 막힘사는 안 움직이고 거점 수렴). ⚠봇은 강한 stuck 회피자라 숙련 기준; 초보 실측은 다를 수 있음(C51 캐비엇 공유). 귀결: 진짜 막힘 위협은 점착(sticky)만 만든다([[enemy-roster-and-readfree-tells]], C51 ⑤와 합류). **⑤ 숙련 축 = 보드 패킹, 적 조준 아님**: 강봇(적 레인 조준)이 中봇(순수 패킹)과 동률·오히려 막힘 많음(55% vs 63% 거점) — AoE 밴드가 콤보폭만큼 알아서 훑어 조준 무의미, 조준에 클리어 쓰면 패킹만 나빠짐. 표현 밴드(강÷약 죽는깊이)=1.9×(1.4× 안전선 위) = 실력이 깊이·점수를 벌린다 = 리더보드 압축 없음. **⑥ 거점사 지배 = 두 기둥 공유 코어 문법(막힘 아님)**: 실패의 의미는 코어 속성 → 모드마다 뒤집으면 정체성 쪼개짐 + 부활(C48, 거점에 묶임)이 무한에서 안 먹혀 수익 역전. 캠페인은 이미 거점 지배(C28, DDA로 막힘 억제). 분기는 실패얼굴 아니라 **막힘의 역할**: 캠페인=억눌린 배경(DDA on), 무한=살아있는 숙련엔진(DDA off, 경쟁이 패킹 깊이로 변별). 같은 영혼, 다른 그릇. **⑦ ★DDA 대신 데일리 시드 — 공정성은 랜덤 아니라 평등에서**: 랭크 무한 DDA off(per-player DDA=점수 비교 불가=리더보드 오염). 대신 조각·스폰 RNG를 **데일리 시드**(그날 전원 동일 판)로 → 남는 변수는 실력뿐=순수 숙련 랭킹 + "오늘 보드에서 남들 이기기"(Wordle 모델, 매일 새 판=리텐션·공유). 램프가 이미 깊이-결정적이라 거의 공짜. **무한 2갈래: 데일리(시드·랭크·DDA off) vs 프리(랜덤시드·캐주얼·구제-DDA 가능)**, 데일리를 셸 1급으로 당김(ROADMAP §4 '소프트런치 이후'에서 승격). **BB 레퍼런스 실측**(웹서치, [[verify-reference-claims-on-raw-frames]]): BB는 적응형 DDA(구제+최고점 근처 rig) 쓰나 미공개·커뮤니티 최대불만 "rigged"=숙련 약속 배신. BB는 공정 글로벌 랭크가 아니라 나-vs-과거점수라 DDA가 공정성 문제 안 됨=우리완 다름. 우리는 rig 없이 서지→거점사→부활(C47/C48)로 더 공정한 "한 판 더"를 이미 가짐. 구제-DDA는 캠페인/프리에만, 랭크엔 절대. **⑧ 남은 것**: 시드 배관(조각·스폰 randi 시드 파라미터화, `tools/regress.gd` 정합) + 데일리 구현 · C48 부활이 막힘사엔 부분구제(하단3줄)뿐이라 거점사 지배가 부활 정합에도 유리 · 사람 플테(봇 캐비엇). 코드 `modes/endless_mode.gd`(튜닝 var: CORE_HP·BASE_HP·HP_RAMP·D1·D2·서지주기)·`tools/endless_sim.gd`. [[gamemode-director-seam]] [[cascade-difficulty-lever-is-advance-speed]] [[cascade-damage-mult-is-meaningless]] [[core-fantasy-cozy-dopamine]] [[no-meta-build]]
- **C51** (2026-07-18, 스테이지 설계 방향 — 난이도를 조각 풀로 옮긴다(퍼즐 축 전환), sim 실측으로 가설 1개 반증) **Phase 3 캠페인 12~15 설계 착수, 산문 아이데이션([[design-ideation-prefers-prose]]). 10년차 디펜스·퍼즐 기획자 관점 리뷰 → 조각 풀 실험 → 프레임 교정.** **① 진단**: 난이도 손잡이가 `core_hp`+`total` 둘뿐(기준 ⑤: step_every=절벽·spawn_every=비단조)이고 4아키타입(배움/무리/속공/장갑/총력)이 이미 소진 → 12~15개를 이 축으로 채우면 재탕. **재미는 퍼즐 축**("한 개 더 vs 터뜨릴까", SPEC §3)인데 난이도는 전부 **디펜스 축**(적·core_hp)에 authored = 어긋남. **② 결정 = 조각 분포를 스테이지별 난이도 주력 손잡이로.** 지금 `_random_piece`는 보드 여유만 보고 **스테이지를 안 봄**(전 스테이지 동일 분포) → 퍼즐 장르 1번 레버가 통째로 놀고 있었다. 명명된 풀 프리셋(clean/small-tempo/no-straights/awkward/blocky)으로 스키마에 `pool` 필드 1개만 추가하는 방향. **③ 실험**(`tools/pool_probe.gd`+`pool_game.gd` = Main **무수정** 서브클래스가 `_random_piece`만 실험 풀로 제한, 적 전부 basic 고정 → 조각만 변수, N=120×2난이도): **조각만으로 승률 5%~76% 벌어짐** = 손잡이 성립, step_every 절벽 없음. `core_hp` 조이기(3→2)가 그 위에 단조로 얹힘(깔끔76→69·삐뚤47→38·덩어리72→61). ⓐ ✅ **small=템포**(거점사 98%·누수 4×·막힘 0%) = '빠른 적' 없이 fast 사망 프로필 재현. 단 5%=벽(작은 조각은 줄도 굶어 콤보 2.5 이중불리) → 손잡이로 쓰려면 line-maker와 **섞어야**. ⓑ ✅ **no-straights=줄굶김**(55%), 가장 부드러운 dial. ⓒ ❌ **반증: awkward=막힘사 예측이 틀렸다.** S·Z를 주면 '자리가 막혀 죽는다' 예측했으나, **막힘사 12%(깔끔과 동일)·거점사 45%로 감**. 봇(=괜찮은 플레이어)이 삐뚤 조각을 **라우팅해 안 막히고**, 대신 공격이 약해져 거점이 뚫린다. **④ 교정된 프레임(핵심)**: 조각은 "퍼즐 사망문(막힘)을 여는" 게 아니라 **"공격 처리량을 조절해 거점 쪽으로 죽이는"** 손잡이다. **막힘문은 숙련 플레이에 bot-resistant** — 조각을 어떻게 주든 막힘사는 10~24%에서 안 움직이고 전부 거점사로 수렴. ⚠캐비엇: 봇은 강한 stuck 회피자(_score −900 stranding)라 이건 **숙련 기준**; 초보 실측은 다를 수 있음. **⑤ 귀결 — 점착(sticky) 적 재검증**: 조각으론 못 여는 '막힘 위협'을, **지울 수 없는 방해 블록을 강제**하는 점착만이 만든다(플레이어가 못 피함). 하드 로스터 점착([[enemy-roster-and-readfree-tells]])의 존재 이유가 데이터로 또렷해짐 — 분열보다 점착이 키스톤. **⑥ 다음 세션 착수**: 혼합 풀 실험(small + line-maker 비율로 5~76% 사이 착지 = 바쁨을 벽 아닌 dial로) → 프리셋 확정 → 스키마 `pool` 필드 → **8스테이지 소프트런치 아크 authoring**(데이터-먼저, 15는 실데이터 후). 커리큘럼 프레임: 1막(요구 고립)→2막(2개 결합)→3막(점착/분열 도입=랭크 무한 로스터 겸). 도구 `tools/pool_probe.gd`·`pool_game.gd`(재사용). [[cascade-difficulty-lever-is-advance-speed]] [[verify-reference-claims-on-raw-frames]]
- **C50** (2026-07-18, 출시 로드맵 정본화 — 라이브서비스 듀얼코어 + 데이터-먼저 소프트런치) **§8 구 로드맵(조커/개조→juice→위치방어→메타/런) 폐기하고 출시 전략 확정** — 산문 아이데이션([[design-ideation-prefers-prose]])으로 진행, 유저 결정 연쇄. **① 빠른 데이터-먼저 소프트런치**: 캠페인+무한만으로 실데이터 먼저 받고 셸 깊이는 라이브옵스로. **② 듀얼코어(유저: "둘 다 중요")**: 캠페인·무한 둘 다 코어. 스테이지-코어 피벗도 무한-부차도 아님. 규율 = 두 기둥이 **다른 동기**(캠페인=숙련·진행·authored / 무한=표현·경쟁·하이스코어)를 맡고 **교차 공급**(공유 소프트 재화·코스메틱 양쪽 적용·하드 로스터 양쪽 등장) → 안 그러면 서로 플레이시간 잠식. **③ 셸=리더보드-먼저**: 무한이 이미 점수를 뱉으니 플랫폼 리더보드(Play Games/Game Center)가 최저비용 경쟁 천장(백엔드·아트 트레드밀 0). 코스메틱은 IAP 층으로 후속, 파워 진행 아님([[no-meta-build]]). **④ 수익 = 광고 우선/IAP 나중**: 리워드(부활 C48) + 인터스티셜(빈도 캡) 확정. 배너·앱오픈은 코지([[core-fantasy-cozy-dopamine]])+세로화면 훼손으로 제외. 추가 포맷은 현행 AdMob 정책 조사 후. IAP=코스메틱, 소프트런치 미포함. **⑤ 캠페인 12~15**: "7일 분량을 스테이지로"는 잘못된 프레임 — 무한이 정의상 무한 일수 콘텐츠라 7일 리텐션은 무한+리더보드의 일. 스테이지는 깔때기 겸 1주차 기둥, 데이터 보고 확장(STAGES 데이터+`sim.gd` 튜닝이라 저비용). **⑥ 팀**: 기획·개발 = 유저+Claude / 아트 = 별도 디자이너(병렬 트랙). **⑦ 개발 기간 추정**: Phase 0(모드 아키텍처 ~3~5일, [[gamemode-director-seam]]) → 1(무한+리더보드 ~1.5주) → 2(모바일 배관·광고 SDK ~2주) → 3(캠페인 확장+셸 ~1~1.5주) → 4(런치 옵스 ~1~1.5주) = **소프트런치 ~6~8주**. **⑧ 다음 착수 = Phase 0**(director 통합, modes/ 이미 시드됨). 상세 §8.
- **C49** (2026-07-17, 클리어 축하 색종이 — 경축은 공격과 방향이 반대다) **C48이 남긴 '역전 클리어 juice' 착수 → 설계 중 단순화**. **① 역전 분기 폐기(유저)**: 부활한 판(revive_used)만 특별 연출하려 했으나 "역전을 분기로 나누는 건 과하다" → **클리어냐 아니냐로만**, 모든 스테이지 클리어에 동일 축하. 팝업 헤드라인 차별화(②안: "역전 클리어!")도 "과하다" → **"스테이지 클리어!" 그대로**. **② 골드 충격파(전멸 연출 `_fire_climax`) 재사용 기각(유저)**: "공격 느낌이 난다". 정확한 진단 — **중앙에서 바깥으로 터지는 건 타격/공격**이고 축하가 아니다. 캡처로 확인 후 유저가 반려(`tools/climax_shot.gd`). **③ 처방 = 색종이(컨페티)**: 화면 위에서 3색(R/B/Y = 플레이어가 쓴 조각 색) 작은 직사각형이 **완만한 중력 + 좌우 나풀거림(sin sway) + 회전**으로 떨어진다. **방향(위→아래)이 충격파(중앙→바깥)와 반대라 '경축'으로 읽힌다** = 공격감 제거의 핵심. 색이 조각 색이라 '내가 놓은 색들의 축제'(C30 색 통일의 연장). 저아트 톤이라 56개로 절제, 화면 안 덮음. 팝업 '위'(최상단)에 그려 흩날리되 작은 조각이라 텍스트 안 가림. **④ 트리거**: `_check_win`에서 `game_clear=true` 시 `_spawn_confetti()`(실패엔 없음). `_process` 파티클 섹션에서 업데이트(클리어 후에도 도는 구간), `_draw`는 `_draw_result` 다음(팝업 위). **⑤ 검증** `tools/confetti_shot.gd`(창 모드, early=상단 쏟아짐 / mid=중단까지 내려와 팝업 위 흩날림 확인). 코드 `Main.gd`(`_spawn_confetti`·`confetti`·`_check_win`·`_process`·`_draw`). **⑥ 남은 판정(플테)**: ⓐ 개수·속도·수명이 적정한지(과하면 절제, 밋밋하면 ↑). ⓑ **클리어 순간 타이밍** — 지금은 팝업이 즉시 뜨고 색종이가 그 위로 쏟아지는데, 팝업을 아주 잠깐(0.3~0.5s) 늦춰 색종이가 먼저 터지고 팝업이 뜨는 2단이 더 극적일 수 있음(C48 ①의 '승리 순간' 아이디어의 온건 버전 — 골드 충격파 없이). ⓒ 부활로 이긴 판에 뭔가 더 얹을지는 보류(역전 분기 폐기했으니 당분간 없음).
- **C48** (2026-07-16, 광고 부활 — 아까운 실패를 이어받는다) **C47 ⑥ⓑ광고 부활 UI + ⓒ버튼 뒤집기 실현. F2P 수익 모델의 심장** — 서지(C47)가 만든 후반 '아까운 실패'를 리워드 광고 한 편으로 이어받는다. **① 설계값(전부 MVP, 플테로 조정)**: ⓐ **판당 1회**(`revive_used` — 표준 F2P, 이미 썼으면 그 판엔 광고 버튼 없이 재도전만). ⓑ **모든 실패에서 부활**(거점 파괴 + 막힘). ⚠**정정** — 처음엔 막힘을 MVP 제외("드묾" surge_probe 봇 실측: 막힘 10~30 vs 거점 100~250건)했으나, **유저 플테에서 곧장 막힘으로 지고 "이어하기 버튼이 안 뜬다"** → 봇은 막힘이 드물어도 사람은 다르고, 실패 경로 하나만 부활 가능하면 **나머지 실패는 그냥 새서** F2P 수익 구멍이 된다. ⓒ **세컨드 윈드 = 하단 3줄만 손대는 부분 개입, 두 경로가 대칭**(진행도 유지, 콤보만 초기화 — 부활은 새 국면이라 스트릭 안 이어줌): **거점 파괴** → 거점에 임박한 **하단 3줄 적만 제거**(`REVIVE_CLEAR_ROWS`=3, row≥ROWS−3) + HP 풀 복구, **위쪽 적·보드는 유지**(전멸은 너무 관대+새 판 느낌). **막힘** → **하단 3줄 보드만 비운다**(공간 확보), **위 구조·적은 유지**(보드 때문에 죽었으니 적까지 지우면 비일관). ⚠**설계 변천(플테 2회)**: ① 처음 거점=전멸/막힘=전체 초기화 → 유저 "이어하는 느낌이 안 난다". **Block Blast 확인**: 부활 시 초기화 않고 **일부만 제거**(검색상 "2~3줄/밀집 영역 빈틈", ⚠2차 소스·원본 미확인 — 확실한 건 '부분 제거' 방향 [[verify-reference-claims-on-raw-frames.md]]). ② 막힘 전체→하단 3줄 부분 클리어 + 적 유지. ③ **거점 전멸 → 하단 3줄 적만 제거**(유저 아이디어) — '적을 공짜로 지움'(구제)에서 '즉사 위협만 걷고 나머지는 실력으로'(기회)로. 검토했던 대안 '적 유지 + 줄완성 조각 트레이 지급'(코어 루프·숙련 정합·역전 juice — C44 조준 프리뷰가 빛날 자리)은 계산 복잡+못 만회 시 좌절 위험이라 **관대한 확장으로 후속 보류**(유저: 1번은 관대한 밸런싱). ⚠단순 HP 복구만이면 서지 구간 즉사 재발이라 하단 정리가 핵심(C47 "부활 후 또 죽으면 사기"). ⓓ **광고는 프로토 스텁** — 버튼 누르면 즉시 부활, 실제 리워드 광고 SDK 연동은 나중. **② 버튼 위계 뒤집기(ⓒ)**: 기존 실패 팝업은 재도전이 주(C31/C39). F2P는 "광고 이어하기"가 주여야 → **부활 가능 시 3버튼**: 금색 3D "▶ 이어하기"(주, 재생 삼각형 + 우상단 **AD 배지**로 광고임을 숨기지 않음 = 정직) > 초록 부차 "재도전" > 회색 고스트 "홈". **부활 소진/막힘이면 2버튼**(재도전이 초록 3D 주로 승격, 현행 복귀). 금색은 팝업 안에서 '보상·특별'(클리어 헤드라인도 금색)이라 보드의 콤보/조준 골드(C30/C44)와 공간 분리돼 안 싸움. **③ 좌표 단일화**: 버튼이 상황따라 2~3개라 그리기·입력이 좌표를 따로 적으면 어긋남(C31) → **`_result_layout()` 한 함수가 panel/cont/retry/home Rect를 반환**, `_draw_result`·`_input` 공유. 기존 const(RESULT_PANEL·RETRY_BTN·HOME_BTN) 제거. **④ 검증**: `tools/result_input.gd`(로직·입력 5케이스, 헤드리스: 부활가능 판정 / 광고클릭→game_over=false·core_hp 7복구·적0·revive_used / 재사망시 부활불가+재도전 / 막힘 부활불가 / SPACE 주동작=부활 / 홈·빈곳 모달), `tools/result_shot.gd`(창 모드 렌더: revive 3버튼 / used 2버튼 확대 확인). 밸런스 회귀 불필요 — 부활은 `_input`에서만 호출, 봇 시뮬은 안 씀. 코드 `Main.gd`(`_result_layout`·`_revive`·`_draw_result`·`_draw_play_icon`·`_input`·`revive_used`). **⑤ 남은 판정(플테)**: ⓐ **세컨드 윈드 강도** — HP 풀복구+적 전멸이 너무 관대한가/부족한가(부활 후 실제로 클리어까지 가는지). ⓑ 막힘 부활의 **하단 3줄이 적정한지**(2줄=덜 관대·더 빡빡, 4줄=여유 — 플테로. 또는 하단 고정 말고 '가장 꽉 찬 줄' 제거가 공간 효율은 나으나 예측성↓). ⓒ **실패 헤드라인이 부활 맥락과 맞는지** — 지금은 남은 적 비율로 갈리는데(C40) 부활 버튼 옆이면 "거의 다 왔어요!"가 더 부합. ⓓ 실제 광고 SDK 연동 + 부활 빈도가 수익에 맞는지. **⑥ 이로써 C47 경험 아크의 실패→부활 고리가 닫힘**(위기 서지 → 아까운 실패 → 광고 이어하기 → 세컨드 윈드). 남은 아크 조각: 서지 텔레그래프(C47 ⓐ, 유저 스킵), 부활 후 '역전 클리어'의 연출적 보상(juice).
- **C47** (2026-07-16, F2P 피벗 + 한 판 경험 아크의 첫 조각: 후반 서지) **유저 결정 2건: ① 수익 모델을 PC/Steam 프리미엄 → 모바일 무료 앱(F2P, 리워드 광고)으로 피벗. ② 그래서 잘 하다가 후반에 실패할 수 있게 난이도를 올려, 아까운 실패 지점에서 광고 보고 이어하게.** 아크의 나머지(광고 부활 UI 등)는 다음, 이번엔 후반 서지부터. **① 피벗이 SPEC 토대를 건드림**: §1 플랫폼(프리미엄→F2P)·§2 축 근거("프리미엄 구매자라 숙련")를 갱신. **숙련 축 자체는 유지** — 오히려 F2P에 필요하다: 난이도 벽이 RNG가 아니라 **실력이 바닥나는 지점**이라야 부활이 정당하고("운 나빠서"가 아니라 "내가 못 버텨서") 숙련과 이어하기가 안 싸운다. 살아남는 축: 폭발형 바이럴·복리 폭발 도파민은 모바일 F2P에 더 잘 맞음. **② 왜 지금 판은 F2P와 안 맞았나(진단)**: 현행은 "적 N 처치=승리"인 독립 스테이지(C25) → 이기면 이어할 이유가 없고, 재도전을 C31·C39가 매끄럽게 해둬 재시작 마찰이 낮아 광고 볼 순간이 안 생긴다. 이어하기가 돈이 되려면 실패가 **아까워야** 하고 실패 뒤 **"조금만 더"**가 있어야 한다 → 경험 아크가 필요. 목표 곡선: **유능감(초반 잘함=투자) → 텔레그래프된 후반 서지 → 아까운 실패 → 광고 부활(세컨드 윈드) → 역전 클리어**. (무한 상승 생존 모드는 좋은 대안이나 별도 프로젝트로 후속 검토 — 유저 판단.) **③ ★서지 레버 = 전진 속도지 스폰 주기가 아니다(반직관, 실측 재확인)**: 처음엔 spawn_every 가속을 제안했으나 STAGES 주석의 기존 실측이 경고 — **spawn_every를 조이면 적이 뭉쳐 들어와 한 줄로 더 쓸려나가 오히려 쉬워진다**(비단조, C46 floor와 같은 결). 실제로 거점을 터뜨리는 유일한 축은 **전진 속도**(C25). → 서지 = 진행도(`spawned/total`)가 `surge_at` 넘으면 **모든 적이 한 단계 빨리 내려온다**(밀물이 빨라진다). [[verify-reference-claims-on-raw-frames]] 원칙이 또 걸림 — 직관(더 빨리 스폰=더 어렵다)이 이 게임에선 틀림. **④ 강도 튜닝(실측 3회)**: (a) `step_every−1` 단순 적용은 과함 — 봇 승률 곡선이 96/54/38/29/10으로 무너짐(S2 온보딩 다음인데 54%, S5 10%). 원인: step 2인 이미 빠른 적(fast·swarm)이 서지 때 step 1=**매 배치 전진(2배 점프)**, 특히 swarm 스테이지 붕괴. (b) `surge_at`을 뒤로(0.78~0.85, 판 마지막 15~22%로 좁힘)만으론 부족 — 이 게임은 원래 실패가 판 끝에 몰려(후반실패% OFF도 95%) 서지가 그 구간을 정통으로 때린다. (c) **처방 = 서지 전진을 최소 step 2로 클램프**(`maxi(2, step_every−1)`): 느린 적(basic·tank, 3→2)만 가속, **빠른 적(fast·swarm, step 2)은 서지 면제** → 2배 점프가 구조적으로 불가능, 타입 믹스에 비단조로 안 튐. **⑤ 결과(surge_probe A/B, 봇 300판/스테이지, DDA off)**: 승률 OFF **93/81/71/54/41** → ON **94/75/63/42/38**(곡선 유지하며 후반만 조임, S2·S3 −6~−8 완만, S4 −12). **핵심 지표 = S4 후반실패% 33→58%**(실패를 판 후반으로 밀어냄 = 아까운 실패). 평균 누수 전 스테이지 상승(전진 가속 작동), 실패 사유 '막힘'→'거점 파괴' 이동(디펜스다운 실패). **⑥ 미구현(다음)**: ⓐ ~~**서지 텔레그래프**~~ — **유저 스킵(2026-07-16)**: 현재 신호로 충분하다 판단. 계획했던 3층(서지 진입 시 보드 훑는 붉은 밀물 파동 / "적 이동" 카드 격화 / 보드 테두리 위협 맥동)은 미구현. `surge_active` 플래그는 노출돼 있어 나중에 필요하면 바로 얹을 수 있음. (원안: "몰려온다"를 미리 보여 실패를 억울함 아닌 아까움으로.) ⓑ **광고 부활 UI**(→ **C48에서 구현**) — 거점 HP 복구 + 보드 적 밀어내기(세컨드 윈드, 단순 HP 회복만이면 즉사 재발). ⓒ **실패 팝업 주/부 버튼 뒤집기**(→ **C48에서 구현**) — 지금은 재도전이 주(C31/C39), F2P는 "광고 이어하기"가 주여야. **⑦ ⚠남은 판정**: 봇은 서지를 '느끼는' 게 제한적(C28: 봇 실력 일정) + 봇>사람이라, **곡선의 절대치가 아니라 형태를 맞춘 것**. 사람 플테로 "후반이 아까운 위기로 느껴지는지" 판정. 검증 `tools/surge_probe.gd`(surge_enabled A/B, 실패 진행도 분포). 코드 `Main.gd`(`STAGES` `surge_at`·`advance_step` 서지 블록·`surge_active`·`surge_enabled`).
- **C46** (2026-07-16, 밀도 하한 `floor` — 무표적 발동을 스폰 타이밍으로 줄인다) **C43 Direction C의 ⒝'자주' 축(유저 지정)**. C44(⒜인지)·C45(배치 손맛)에 이어 마지막 남은 밀도 축. **① 문제(C43 실측)**: 보드가 희박해 **평균 동시 적 1.7~4.9마리**뿐 → 줄을 완성해도 절반 가까이가 아무도 못 잡는다(**무표적 발동 29~49%, 빈 레인 52~69%**). C43은 '빈 레인 로켓 억제'를 유저가 기각했으니, 로켓(콤보 인지)을 안 건드리고 **표적 쪽을 촘촘히** 하는 방향. **② 처방 = floor**: 스테이지마다 `floor`(보드에 유지할 적 최소 수)를 두고, `advance_step`에서 `enemies.size() < floor`면 스폰 스로틀(`spawn_every`)을 무시하고 매 스텝 +1 채운다 — 시작 버스트 없이 서서히. 처치·클리어로 보드가 비어도 곧 표적이 다시 생겨 무표적 발동을 줄인다. 값: S1=4 / S2~S4=5 / S5=6. **③ 총량은 안 늘린다 — 코드로 확증**: floor 스폰도 `_spawn_one`이 `spawned++`로 집계(1257행) → **적 총량(`total`)은 불변, 스폰 타이밍만 앞당긴다**. 설계 의도는 "난이도(core_hp·total)와 밀도(floor)를 분리". **④ 검증 = probe A/B**(`tools/density_probe.gd`, `floor_enabled` 토글로 off/on 2패스, 봇 200판/스테이지, DDA off): 평균 동시 적이 희박 스테이지에서 배증(S1 1.74→3.63, S3 3.18→5.21), **무표적 발동 S1 48.9→33.8% / S3 35.8→24.9% / S5 30.7→20.8%**(전 스테이지 하락), **빈 레인 S1 69.4→57.7% 등 전부 하락**, 발동당 실명중 0.67~1.47 → 1.35~2.28로 상승 = ⒝가 노린 것 그대로. **⑤ ⚠밸런스 중립이 아니다 — floor는 초반을 쉽게 만든다**: sim 승률(`tools/sim.gd` DDA off) **86/75/70/52/41(floor off, C44 기준선) → 96/84/70/54/39(floor on)**. C44 노이즈 밴드(±8%p) 기준 **S3~S5는 중립이지만 S1·S2는 +9~10%p로 유의미하게 쉬워졌다**. 인과: floor가 총량을 안 늘려도 보드에 표적이 늘 있어 같은 총량을 더 효율적으로 잡는다(밴드명중 상승과 같은 원리) → ③의 "순수 밀도 손잡이" 주장은 **후반엔 맞고 희박한 초반엔 틀리다**(밀도와 난이도가 초반에선 결합). 부수로 클라이맥스/판도 감소(S1 2.50→0.82, 봇 기준). **⑥ 남은 판정**: 유저 플테로 "좋아" 판정 완료(체감은 OK). 단 봇 기준 S1·S2가 96/84%로 헐거워졌으니, 사람에게도 너무 쉬우면 그 두 스테이지 `core_hp`/`total`을 재조여 floor가 벌어준 만큼 되돌린다(밀도는 유지, 난이도만 보정). 코드 `Main.gd`(`STAGES` `floor`·`advance_step` 밀도 하한 블록·`floor_enabled`), `tools/density_probe.gd`(floor A/B). 이로써 **Direction C 세 축(⒜인지 C44·⒝자주 C46·배치손맛 C45) 완료 → 다음 = ⒞짜릿(연출)**.
- **C45** (2026-07-15, 블록 착지 팝 — 빈손인 수에도 손맛) **C43 Direction C의 배치 축(유저 지정)**. **① 문제**: 실측상 우리 수의 **절반 이상이 헛수**(줄 완성 못 함, C30 노트 d — "BB는 헛수에도 +60 플로터라 빈손인 수가 없다"). 완성 수엔 두꺼운 전투 연출이 있지만 **비완성 배치엔 피드백이 0** — 조각을 놓아도 아무 반응이 없어 "배치 순간의 보상"(C6 피벗의 핵심)이 화면에 없었다. **② 처방 = 착지 팝**: 놓은 칸마다 흰 테두리 사각형이 셀보다 크게 떴다가 **셀 크기로 수축**하며 페이드(0.17s "탁"). 놓은 조각 색 + 흰 테두리. **③ 방향으로 이벤트 구분**: 블록 소멸 팝(C30, 사각형이 **밖으로 부풂**)의 정반대로 **수축** → 같은 사각형 문법 안에서 '도착'과 '소멸'이 모션 방향으로 갈린다(원형은 여전히 적 사망 전용, C16). **④ 숫자 없음**: BB의 "+60"은 점수가 목표라 가능했지만 우리는 C9에서 SCORE를 격하, C23에서 목표를 "남은 적 N"으로 못박았다 → 점수 플로터를 되살리지 않고 **순수 촉각 신호**만. 완성/비완성 모두에 뜨지만 진짜 메우는 건 비완성 수의 빈손감. **⑤ 검증** `tools/place_pop.gd`(ㄱ자 4칸을 빈 자리에 배치 → 팝 4개가 각 셀에 뜨는 것 창 모드 확대로 확인). 코드 `Main.gd`(`place_pops`·`_place_piece`·`_draw_board` 팝 루프·`PLACE_POP_DUR`). **남은 판정**: 실전에서 이 팝이 헛수의 허전함을 실제로 메우는지 플테. 다음 = Direction C의 마지막 축 ⒝자주(적 밀도 — 무표적/빈 레인 줄이기, 유저 지정).
- **C44** (2026-07-15, 조준 프리뷰 — 배치 전에 '이 적이 죽는다'를 적 위에 표식) **C43 Direction C의 인지 축 착수(유저 지정)**. C43이 못박은 "배치→처치를 ⒜확실히 보이게" 중 ⒜를 실현. **① 문제**: 기존 프리뷰(C30 ④)는 "어느 **줄**이 터지나"만 조각색으로 물들여 보여줬을 뿐, "어느 **적**이 죽나"는 배치 전에 안 보였다 → C43 실측의 무표적 발동(29~47%)·오버슈트(빈 레인 55~75%)를 플레이어가 **놓기 전에 몰랐다**(플테 "적 없는 라인 터뜨리면 손해 보는 기분"). **② 2겹 회피(유저 우려)**: 콤보 밴드까지 셀 물들이기로 그리면 완성 줄 프리뷰와 같은 문법이라 겹치고, 밴드는 보드 대부분으로 번져 화면을 덮는다 → **라인은 안 그리고, 죽는 적한테만 표식**. 보드가 희박(동시 적 1.7~4.9)해 표식은 몇 개뿐 = 노이즈 낮음. 역할 분리: 완성 줄 프리뷰="내 수가 뭘 하나"(입력) / 적 표식="누가 죽나"(결과). **③ 표식 = 적 외곽 변조**(새 떠다니는 도형 X): 이미 화면에 있는 적 위에 **로켓과 같은 골드 2겹 글로우 링**(넓은 은은한 + 좁은 밝은, C30 로켓 렌더 문법 재사용)을 맥동시킴 → '이 적을 저 로켓이 친다'로 원인→결과를 색으로 잇는다. HP바 초록·사망 팝 원형·R블록 빨강 어느 것과도 안 겹침. **④ 어긋남 방지 = 계산 공유**: `_begin_resolve` 안에 인라인이던 밴드 확산 로직을 **`_blast_band(rows, cols, combo_val)` 헬퍼로 분리** → 실제 처치와 프리뷰가 **같은 함수**를 쓴다(C31 "다른 셈이면 죽은 지표" 원칙). 프리뷰는 배치 시 콤보가 오르므로 `combo+1`로 계산. 표식이 **하나도 안 뜨면 = 무표적**("이 수는 아무도 못 잡는다")이 배치 전에 그대로 보인다 = C43이 노린 것. **⑤ 가림 해결 = 링을 최상단 오버레이로**(플테: "놓는 위치 아래 적이 있으면 파동이 잘 안 보여"): 그리기 순서가 `_draw_board`(적·링) → **`_draw_held`(커서에 붙은 조각)**이라, 드롭 칸 아래 적의 링이 들고 있는 조각에 덮였다. → 링을 적 루프에서 직접 안 그리고 위치·반지름만 `aim_marks`(단일 출처)에 적재, **`_draw_held` 다음에 `_draw_aim_overlay`가 최상단에 그린다**. 조각 자체는 여전히 보드 위(손에 든 느낌 유지)지만 '누가 죽나' 신호만은 절대 안 가려진다. 검증 프로브에 드롭 칸(7,6) 위 적 추가 → 노란 조각 위로 링이 뜨는 것 확인. **⑥ 검증** `tools/aim_preview.gd`(같은 보드·같은 적, 콤보만 0/2/4로: **combo0→표식 0**[밴드=완성 줄만, 인접행 적 못 잡음] / **combo2→인접행 3마리+드롭칸 1 조준·먼 적 1 제외**[밴드 rows 5/6/7] / **climax(combo4→+1=5)→전멸, 5마리 전부**. 로직은 `_blast_band` 출력으로, 표식은 창 모드 렌더 확대로 이중 확인. drag_pos를 드롭 칸 중심에 둬 들고 있는 조각 가림도 재현). 리팩터링 회귀는 `tools/sim.gd` 승률(86/75/70/52/41%, C30 기준선 노이즈 내)로. 코드 `Main.gd`(`_blast_band`·`_draw_board` 프리뷰 블록·적 루프·`_draw_aim_overlay`·`aim_marks`). **남은 판정**: 실전에서 이 표식이 "지금 놓으면 쟤가 죽는다"로 읽혀 무표적/오버슈트 회피 판단에 실제로 쓰이는지 플테. 다음 = Direction C의 ⒝자주·⒞짜릿(밀도·연출 축).
- **C43** (2026-07-15, 콤보/커버리지 재정의 탐색 → "루프 유지, 배치→처치를 손맛·인지·밀도로 극대화") **플테: "콤보 오를 때 적 없는 라인을 터뜨리면 손해 보는 기분"**. 긴 설계 탐색 끝에 **메커니즘 변경이 아니라 연출·인지·밀도 강화(Direction C)로 결론** — 코드 변경 없음, 다음 세션 착수 대상. **① 실측(`tools/density_probe.gd`, 봇 200판/스테이지, DDA off)**: 보드는 희박 — 평균 동시 적 **1.7~4.9마리**, 발동 시점 2~5.5마리. 발동당 **실명중 0.7~1.8마리인데 로켓 2.4~2.6개 발사 → 빈 레인율 55~75%**, **무표적 발동(줄 완성해도 아무도 안 죽음) 29~47%**. 봇이 "완성 줄이 적을 잡는지"를 최우선 조준하고도 이 수치 = **오버슈트는 실력이 아니라 구조**(사람은 더 심할 것). 밴드(콤보 확산)가 완성 줄보다 적을 더 잡는 발동은 **18~30%뿐(70~81%가 헛방)**, 추가 처치 겨우 0.2~0.6마리. 완성 줄만으로도 처치의 65~75% 유지(밴드 제거 시 25~35%↓). **② 기각한 재정의와 사유(전부)**: 관통/데미지(C5 "블라인드 부재"·C24 데미지축 기각 — 원샷이라 탱크만 의미) / 은행·수동발동(유저 **자동발동·캐주얼 확정**) / 킬로 콤보 쌓기(킬이 "줄 완성 AND 적 존재"로 이중 게이팅돼 희박 → 스트릭 불가) / 연쇄 cascade(발동당 명중 ~1·적이 흩어져 튈 데 없음 + 짧은 연쇄는 인지 불가 C15·C16) / 자동조준(콤보 용량 > 희박한 적 수 = 너무 쉬움) / 스페셜 합체(무회전·랜덤이라 특정 모양 못 노림 — C19 벽) / 빈 레인 로켓 억제(로켓 수가 적 위치 따라 들쭉날쭉 = 콤보 인지 혼란, **유저 기각**) / 파동 시각화(D2, 미채택) / 밴드 제거(D1/③, 재튜닝 필요). **③ 핵심 통찰 — 왜 모든 재정의가 "지금과 같다"로 수렴하나**: 커버리지/콤보를 어떻게 바꿔도 킬 규칙("완성된 줄에 걸린 적이 죽는다")을 안 건드리면 전부 현재 루프다. 지금과 **진짜 다른 유일한 길 = 적을 보드 상태로 만드는 융합**(적 = 격자를 점유하는 블록, 그 행/열 완성 = 처치, 전진하며 퍼즐이 재편). 근데 그건 **적이 플레이어의 패킹을 제약**하는 걸 받아들여야 하고, 유저가 그 대가를 반복해서 회피(적을 "안 채우는·놓기 가능한 오버레이"로 두려 함) → **융합 보류**. **④ 결정(유저) = Direction C**: 루프는 현행 유지. "배치→처치"를 **⒜ 확실히 보이고 ⒝ 자주 일어나고 ⒞ 짜릿하게** 만드는 연출·인지·밀도 작업으로 극대화. 검증 데이터 `tools/density_probe.gd`.
- **C42** (2026-07-15, HP 바를 '피격 생존' 신호로 — 상시 표시 폐기) **플테 후 유저: "hp바가 거슬린다. 적 외형만 남기고, 피격당해 살아남은 적만 바를 띄우자. 숫자는 빼고."** C41이 (b)"다 남기되 HP임을 알아보게"로 갔던 걸, 실제로 플레이해 보고 뒤집은 결정. **① 왜 상시 바가 죽은 지표였나**: C41 ⑦의 원샷 구조 그대로 — 최소 일격(120)이 basic/fast/swarm 최대 HP(65/39/26)를 전 스테이지에서 넘어 대부분 원샷이라, 상시 게이지는 **한 번도 안 줄어든 채 등장→소멸**한다. 줄지 않는 바 + 이름표로 오독되는 숫자가 화면 노이즈만 됐다. **② 처방 = 바가 '뜨는 것'을 신호로**: `hp < maxhp`(피격당하고 살아남음)일 때만 바를 그린다. 별도 플래그 없이 hp/maxhp만으로 게이팅 — 죽은 적은 배열에서 제거되므로(`_apply_hit`) 이 조건이 곧 '데미지 입은 생존자'다. **바가 보이면 = "얘는 한 방에 안 죽었다"** → 탱크·후반 HP램프 등 버티는 적에게만 나타나 게이지가 비로소 정보가 된다(C41 갈림길의 (a)를 '숨김'이 아니라 '조건부 표시'로 실현). **③ 숫자 제거**: 오독의 근원. 남은 하트가 '이건 체력'을 글자 없이 말한다(C41이 세운 기호 유지). 숫자가 빠져 바 폭 축소(0.90→0.60 CELL), 채움은 어두운 초록(흰 숫자 대비용 0.16,0.47,0.20)→**밝은 초록(0.20,0.72,0.30)**으로 되살림(더는 글자를 이길 필요가 없다). **④ 몸통 복원**: C41 ⑥이 상시 게이지 자리를 만드느라 몸통을 아래로(`E_BODY_DY`=10) 내리고 줄였는데(basic 0.33→0.27, tank 0.42→0.32), 게이지가 상시가 아니게 됐으니 되돌림(`E_BODY_DY`=**0**, 크기 복원) → 평소 '외형만' 모습이 셀 중앙에 꽉 참. `_enemy_pos`는 이제 `_cell_center`와 같다(상수는 유지 — 이펙트 좌표 참조가 그대로 붙어 있으므로). 드물게 뜨는 바가 머리를 조금 덮는 건 '유닛 위 체력바'의 흔한 문법이라 모양 인지를 안 해친다. **⑤ 검증** `tools/hpbar_probe.gd`(4타입 × 풀피/반피 렌더 + 실측: **풀피=바 없음 / 반피=바 있음** 전부 확인, 창 모드). 코드 `Main.gd`(`_draw_board` 적 루프·`E_BODY_DY`). **남은 판정**: 실전에서 바가 '뜨는 것' 자체가 "안 죽었다"로 읽히는지 플테.
- **C41** (2026-07-14, 적 HP 게이지 — 숫자가 체력으로 읽히게) **플테 제보(지인): "적에 써있는 숫자가 뭔지 모르겠다"**. **① 진단 — 숫자가 '안 읽히는' 게 아니라 '살아있지 않다'**: 실측 계산 결과 플레이어의 **최소 일격(120 = 줄 하나·콤보 없음)이 basic/fast/swarm의 최대 HP(65/39/26)를 다섯 스테이지 전부에서 넘는다 = 전원 원샷**. 탱크마저 S1(75)·S2(110)에선 원샷. 즉 **그 숫자는 한 번도 줄어드는 걸 볼 수 없는 숫자**다 — 등장했다 그대로 사라지니 "저게 체력이구나"를 배울 경로가 없다. 초록 HP 바도 마찬가지로 절대 안 줄어든다(같은 죽은 값을 두 방식으로 그리고 있었다). 지인이 플레이한 S1은 **탱크까지 원샷이라 숫자가 단 한 번도 안 움직이는 판**이었다. **② 갈림길**: (a) 견디는 적(탱크)에게만 HP를 남겨 '숫자의 존재 = 한 방에 안 죽는다'를 정보로 만들기, (b) 다 남기되 HP임을 알아보게, (c) 밸런스를 고쳐 HP를 살리기(⚠C25가 검토 후 기각한 축 — 난이도는 커버리지에서 온다). **유저 판단 = (b)**. **③ 처방 = 숫자를 바 안으로**: 예전엔 숫자가 **몸통 한가운데**, HP 바는 **머리 위**에 따로 있어 둘이 서로 다른 물건으로 보였다(몸통 위의 숫자 = 체력이 아니라 이름표·ID로 읽힘). → **`♥ + 게이지 + 숫자`를 한 덩어리로 묶고** 하트로 '이건 체력이다'를 글자 없이 말한다. 탱크는 게이지도 크게(C14의 "버티는 게 보임" 텔레그래프 유지). **④ 가독성 — 흰 글자를 밝은 초록 위에 얹은 게 문제였다**(유저: "HP바 가독성이 너무 안 좋아"). 배경만 밝게 바꾸고 글자색은 그대로 뒀던 것. **채움을 어두운 초록(0.16,0.47,0.20)으로 낮추니** 게이지가 차 있든 비어 있든 흰 숫자가 항상 이긴다(글자색 하나가 두 배경을 다 감당해야 한다). 숫자는 **바 전체의 중앙 정렬**(하트를 뺀 나머지 폭의 가운데에 두면 오른쪽으로 밀려 보인다. 3자리 탱크는 하트와 부딪힐 때만 최소로 비켜남). **⑤ 게이지는 '셀 안'에 못 박는다 — 실제 버그 2건**: 머리 위에 띄웠더니 (i) **적은 항상 row 0에 스폰되므로 게이지가 보드 밖으로 나갔고**, 아래로 뒤집었더니 (ii) **세로로 붙어선 적끼리 게이지가 포개졌다**. 셀 고정으로 둘 다 구조적으로 불가능해짐. **⑥ 몸통을 게이지 아래로 내려앉힘**: 셀 위쪽을 게이지가 쓰니 몸통이 셀 한가운데면 **머리가 잘려 원이 돔이 되고 탱크 사각형이 직사각형이 된다 = 모양으로 타입을 읽는 문법이 무너진다**. → `E_BODY_DY`=10으로 몸통을 내리고 크기를 게이지 아래 남은 높이에 맞춤(basic 0.33→0.27, tank 0.42→0.32 — **탱크가 기본보다 확실히 크다는 위계는 유지**). ⚠**사망 팝·파편·임팩트·데미지 숫자가 전부 `_cell_center`를 쓰고 있어** 몸통만 내리면 이펙트가 머리 위에서 터진다 → 적 전용 좌표 **`_enemy_pos`**(= 셀 중심 + `E_BODY_DY`)를 만들어 묶음(블록 소멸 팝은 셀의 일이라 `_cell_center` 유지). **⑦ 남은 판정**: 이 게이지는 **실전에서 줄어드는 걸 볼 일이 거의 없다**(①의 원샷 구조 그대로). 하트+게이지 '형태'가 학습을 대신해야 하므로, **같은 지인에게 다시 보여줘 '체력'으로 읽히는지가 진짜 판정**. 안 읽히면 (a)나 (c)로 간다. 검증 `tools/color_probe.gd`(적 4타입 × R/B/Y/빈칸, HP는 실제 스테이지 값 — 전부 30으로 두면 3자리 탱크의 자릿수 충돌을 못 본다). 코드 `Main.gd`(`_draw_board` 적 루프·`_draw_heart`·`_enemy_pos`).
- **C40** (2026-07-14, 실패 헤드라인 — 심판이 아니라 초대) **"실패"는 판정을 내리고 대화를 닫는 말**이라, 팝업의 일("이만큼 남았다 → 다시", C31)과 정반대로 민다. **① 레퍼런스 — 절반만 확인**: 영어권 매치3는 심판형 헤드라인을 안 쓴다 — Royal Match는 **사유를 말하고**(`Out of Moves!`), Toon Blast는 **재구성한다**(`So Close!`, 유저 인용). ⚠**한국어 로컬라이즈 원문은 확인 실패**(Game UI Database 403, 웹 검색은 전언뿐) → 아래 문구는 **레퍼런스 인용이 아니라 우리 팝업 구조에서 나온 설계값**이다. **② 남은 적 수에 따라 갈린다**(유저 판단): 헤드라인 **바로 아래에 '남은 적 N'이 찍히기 때문**이다. 20마리 중 13마리를 남기고 지고서 "아쉬워요"라고 하면 **헤드라인과 숫자가 서로를 반박한다** — 아까운 말은 아까울 때만 해야 힘을 갖는다. 크게 졌을 땐 위로 대신 **초대**로 넘긴다. **③ 문구**(전부 해요체): 남은 적이 총량의 **15% 이하 → "거의 다 왔어요!"** / **40% 이하 → "아쉬워요!"** / **그 이상 → "다시 해볼까요?"** (`FAIL_CLOSE`/`FAIL_NEAR`). **④ 남은 판정**: 임계값과 문구 모두 사람 플테로 확인. 검증 `tools/fail_headline.gd`(3단계 렌더 + 실제 분기 확인). 코드 `Main.gd`(`_fail_headline`).
- **C39** (2026-07-14, 결과 팝업 폴리싱 — 문자열 8 → 4) **"텍스트가 너무 많아 안 읽힌다"(유저)**. **① 진단 = 세어보니 맞았다**: 실패 팝업이 문자열 **8개 + 아이콘 2개**, 글자 크기 **6종(17~56px)**. 그중 **셋이 같은 두 동작을 반복**하고 있었다 — 초록 버튼 `재도전` + 고스트 버튼 `홈으로` + 맨 아래 힌트 `SPACE 재도전 · ESC 홈`(힌트가 버튼 라벨을 코드로 그대로 끌어다 씀: `"SPACE %s · ESC 홈" % label`). **② 처방 = 덜어내기**: (a) **키 힌트 제거** — 버튼이 이미 같은 말을 하고 있고, 마우스로 하는 게임이다. **글자만 뺐고 SPACE·ESC 입력은 그대로 받는다**. (b) **스테이지 이름(`1. 첫 방어선`) 제거** — 기본 동작이 재도전(=같은 스테이지)이라 방금까지 보던 이름을 다시 확인할 이유가 없다. **③ 위계 = 크기 격차**: 남은 넷이 **56·20·24·18·46으로 다 엇비슷해 무엇이 헤드라인인지 눈이 못 정했다** → **판정 64 / 사유 20 / 캡션 18 / 숫자 52**로 벌림. 판정과 숫자만 크고 나머지는 확실히 작다. C31이 세운 시선 순서(판정 → 사유 → 남은 적 → 버튼)는 그대로. **④ 헤드라인은 글자 수가 아니라 패널이 크기를 정한다**: "실패"(2자)는 64px가 넉넉하지만 "스테이지 클리어!"(8자)는 같은 크기면 패널을 끝까지 밀어낸다 → 폭 초과 시 자동 축소(하한 40px). **⑤ 패널 축소**: 두 줄이 빠져 생긴 빈 공간만큼 조임 — 패널 490 → **430**, 버튼 좌표도 함께 이동(`RESULT_PANEL`/`RETRY_BTN`/`HOME_BTN`). **⑥ 검증** `tools/result_input.gd` — 힌트를 지우고 버튼을 옮긴 뒤에도 **재도전 클릭·SPACE·ESC·홈 클릭 4종이 새 좌표에서 그대로 작동**하고 **빈 곳 클릭은 여전히 무시**(C31의 모달 유지)됨을 실제 `_input` 주입으로 확인. 렌더 `tools/result_shot.gd`. 코드 `Main.gd`(`_draw_result`).
- **C38** (2026-07-14, 적 팔레트가 빨강에서 빠짐) **조각 색과 적 색이 같은 팔레트를 놓고 충돌하던 문제**. 유저 제보: "빨간 블록과 빨간 적이 겹치면 인지가 안 된다". **① 진단 — 비유가 아니라 사실이었음**: `C_RED`(조각)과 `C_E_BASIC`(기본 적)이 **헥스까지 같았다**(`#e5484d`). 두 색 체계가 각자 팔레트를 고르다 부딪힌 것. 픽셀 실측(`tools/color_probe.gd`, 적 4타입 × R/B/Y/빈칸 16조합)에서 빨강 블록 위 기본 적의 몸통↔배경 거리 = **0.00** = 원이 통째로 사라지고 HP 숫자·초록 바만 공중에 뜸. **탱크의 마룬(`#7a1f3d`)도 같은 버그의 조용한 버전**(거리 0.45). **② 방향 = 적이 양보(유저 판단)**. 조각 3색(R/B/Y)은 유지. ⚠단 이 선택은 "기본 적 = 빨강"이라는 가장 강한 위협 관습을 버리는 대가를 치른 것 — 대신 빨강은 조각과 위협 신호(피격 플래시·누수 −1·실패 테두리)가 나눠 갖는다. **③ 처방**: basic `#e5484d` → **바이올렛 `#a855f7`**, tank `#7a1f3d` → **딥 바이올렛 `#6d28d9`**(둘 다 옮김 — 하나만 고치면 절반만 고치는 것). **밝기 관계(밝은 기본 ↔ 어두운 무거운 변주)를 마룬-빨강 시절 그대로 보존**해 플레이어가 배운 "탱크 = 육중한 기본"이 안 깨지게. 저HP 명암도 어두운 빨강 → 어두운 바이올렛. **④ 색이 아니라 구조로 분리 — 적 전원에 어두운 외곽선**(`C_E_RIM`): 색으로만 분리를 보장하면 팔레트가 바뀔 때마다 같은 버그가 재발한다(두더지 잡기). **탱크가 이미 쓰던 검은 외곽선을 전 타입으로 일반화**(C31의 "같은 문법 재사용, 새 시각 언어 안 만듦"). **⑤ 결과(실측)**: 빨강×기본 **0.00 → 0.71**, 빨강×탱크 **0.45 → 0.73**. **⑥ 남은 스레드**: **시안 고속 적 × 파란 블록이 이제 최약 조합(0.33)** — 이번 변경 전에도 같은 값인 **기존 문제**이고 테두리가 윤곽은 세워주나 색 자체는 여전히 가깝다. 빨강만큼 극단적이지 않아 임의로 안 건드림 → **사람 플테로 판정**. 코드 `Main.gd`(`C_E_*`/`_etype_fx_color`/`_draw_board` 적 루프).
- **C31** (2026-07-14, 결과 팝업 + 재도전 버튼) **실패의 출구가 없던 문제**. 기존 결과 화면은 화면 전체를 덮는 텍스트 나열이었고, **재시도 수단이 SPACE 키뿐**(마우스로 하는 게임인데 재도전만 키보드). 게다가 **아무 데나 클릭하면 홈으로 튕겨서**, 실패 직후 습관적으로 누르면 스테이지 밖으로 밀려남 = "한 판 더"의 마찰이 최대. **처방(레퍼런스: Toon Blast식 "So Close!" 팝업)**: ① 전체화면 나열 → **어두워진 게임 위에 뜨는 카드**(모달). 시선 순서 = 판정 → 사유 → **남은 적** → 버튼(착지점). ② **재도전 버튼**(회전 화살표 + 초록 3D — 홈 화면 시작 버튼과 **같은 문법 재사용**, 새 시각 언어를 만들지 않음) + 부차 동작인 홈은 고스트 버튼으로 위계 분리. ③ **버튼 위에 남은 적 N**(💀 + 숫자) — 실패의 크기를 숫자로 보여줘 "이만큼 남았다 → 다시"로 잇는 것. **정의는 HUD 목표 카드와 동일한 `total − killed − leaked`를 재사용**(다른 셈이면 게임 중 내내 보던 숫자와 어긋남 — 검증 스크린샷에서 상단 카드 13 = 팝업 13 일치 확인). ④ 패널 테두리 색으로 판정 분기(실패=빨강 / 클리어=골드) = **글자 아니라 색으로 신호**. ⑤ **모달화 — 빈 곳 클릭 무시**(오조작으로 홈 튕김 제거). SPACE·ESC는 유지. ⑥ 클리어도 같은 패널 재사용하되 버튼은 "다음 스테이지"(회전 화살표는 '다시'의 뜻이라 실패에만), 숫자는 처치 수. 검증 `tools/shoot_result.gd`(실패 팝업 렌더 + 실제 `_input` 주입으로 빈 곳 클릭 무시·재도전 클릭 시 재시작 확인). 코드 `Main.gd`(`_draw_result`/`_result_advance`/`RESULT_PANEL`·`RETRY_BTN`·`HOME_BTN`).
- **C30** (2026-07-13, 연출 1탄 — 줄 폭발) **"플레이어 행동의 보상"이 화면에 없던 구멍을 메움 + 레퍼런스 재검증으로 C26 오독 정정**. **① 진단 — 연출이 없는 게 아니라 엉뚱한 데만 있었음**: 전투 층(로켓·파편·히트스톱·전멸 충격파)은 C14~C24로 두꺼웠는데, 정작 `_begin_resolve`가 완성 줄 셀을 **배치와 같은 프레임에 즉시 삭제**하고 있었음 → 조각을 놓는 순간 줄이 증발하고, 이후 0.3~3.2초 연출이 **텅 빈 보드 위에서** 재생. C6에서 "도파민을 발동이 아니라 **배치 순간**에"라고 피벗해놓고 그 보상이 화면에 없던 것. (+ `line_flash_rows/cols/timer`·`sweep_timer`는 세팅·감소만 하고 **아무도 안 그리는 죽은 상태**였음 — 제거.) **② 처방 = 셀 삭제를 타임라인에 태움**: `충전 홀드 → 전 셀 동시 소멸 → 테두리 잔상 → 로켓 → 순차 피격`. 모든 전투 연출이 소멸 뒤로 밀림. **③ 색 통일(유저 제안)**: 완성 줄이 `원래 색 → 방금 놓은 조각 색`으로 물들었다가(`CHARGE_TINT`=0.45 지점까지) 흰색으로 달아오르며 부풂. 파편 색도 그 색 → **알록달록한 보드에서 완성 줄만 도드라지고, 폭발이 '내 조각의 결과'임이 색으로 이어짐**. 시각용일 뿐이던 3색(C24에서 색 시너지 기각)에 처음으로 역할이 생김. 로켓부터는 골드→핫오렌지(콤보 신호)로 두어 두 신호가 안 싸우게 분리. **④ 드래그 프리뷰**: 고스트를 dim(α0.42) → **활성 색**(실제 놓인 것과 동일, 흰 테두리로 미리보기 표시)으로. + **줄 완성 프리뷰**(BB식) — 지금 놓으면 터질 줄을 조각 색으로 물들여 맥동 = 폭발 색 통일의 예고편. 무효 자리는 **불투명** 빨강(반투명이면 파란 블록 위에서 자홍색이 되어 '금지'로 안 읽힘). **⑤ ★C26 오독 정정 — BB는 블록을 순차 파괴하지 않는다**: 줄터짐 영상(60fps)을 프레임 단위 재검증 → **완성 줄 8칸이 단 1프레임(16.7ms)에 전부 동시 증발**. C26이 "한쪽 끝부터 차례로 부서진다"고 적었고 그대로 구현했더니 **유저가 즉시 "느리다"**(스태거 0.05→0.03→0.015로 줄여도 근본이 안 고쳐짐 — 원인이 속도가 아니라 **구조**였으니까). 실제 BB 구조: `색 통일+홀드 0.55s(점수 얼어붙음) → 블록 동시 증발 0.017s → 빈 줄 0.07s → 빛 바 스윕 0.10s → 파티클 한꺼번에 터짐 0.22s → 카운트업(바와 동기) → Combo 텍스트(파괴 후 0.35s, 스프링)`. **"순차"의 정체는 블록이 아니라 빛 바였다 — 시간이 걸리는 연출은 전부 블록이 사라진 '뒤'에 온다.** → 스태거 전면 제거(동시 소멸), 파편도 동시, 소멸 순간 줄 자리에 **색 테두리 잔상**(0.06s). 총 재생 1.95 → **1.63초**. 유저 확인 "지금 좋아". **⑥ 색 통일 = 조각 색 확증**: 같은 영상 두 클리어가 각각 **금색·초록색**으로 물듦 = 그때 놓은 조각 색(빛 바·파티클 색도 따라감). ③의 방향이 맞았음. **⑦ 콤보 비례 홀드(근거 약함, 유지 보류)**: `CHARGE_BASE`0.20 + `CHARGE_PER_COMBO`0.12(상한 0.60) — 셰이크 금지(C24⑦) 후 비어 있던 에스컬레이션 축을 **시간**이 메움("더 크게" 아니라 "더 오래 참는다"). ⚠단 C26의 "콤보2≈0.27s / 콤보4≈0.55s"는 이번 영상(클리어 4회)으론 재현 못 함 — **콤보 비례 홀드는 아직 검증 안 된 가설**. 사람 플테로 판정. **⑧ 밸런스 무변화 검증**: 시뮬 기준선 대비 승률차 전부 노이즈(±8%p CI 내), **클리어율 43.1/42.0/43.1/43.7/43.1 → 43.3/42.3/43.5/43.6/43.3**(표본 큰 지표라 민감) = 순수 연출 변경 확인. **⑨ 부수 수정**: resolve 재생 내내 보드에 떠 있던 '못 놓음' 빨간 고스트 제거(충전 중 셀 때문에 `_can_place`가 false가 됨), 블록 소멸 팝을 **사각형**으로(적 사망의 **원형** 팝과 시각 문법 분리 — C16이 세운 "원=적이 죽었다"를 흐리지 않게). **남은 스레드**: (a) **전체화면 섬광 — ★계획이 실행에서 뒤집힘**. BB는 전체화면 플래시를 한 번도 안 씀(빛이 전부 **보드 안**에 머묾 = 반응 주체가 '화면'이 아니라 '**보드라는 객체**'). 당초 계획은 "일반 콤보는 보드 내부 광원으로, **전멸만 전면 섬광 유지**"(화면이 반응하는 건 전멸뿐이라는 위계)였으나, **유저 판단으로 정확히 반대**를 택함 → **전멸 골드 섬광(`climax_flash`) 제거**(상수·변수·틱·`draw_rect` 전부. 충격파 3겹·히트스톱·일제 로켓은 유지 = 전멸의 무게는 **보드 안 광원**이 짊어짐), **`flash_timer`(줄 클리어 전면 섬광)와 `red_flash`(피격)는 존치**. 검증: 보드·파티클이 안 닿는 구석 픽셀(4,995)을 시간축으로 훑어 클라이맥스 발화(t=0.87) 직후의 골드 점프(이탈 0.557→**1.239**, 색 0.58/0.50/0.36)가 사라지고 단조 감쇠만 남음을 확인(`tools/probe_climax.gd`). **남은 판정**: `flash_timer`는 콤보5에서 α0.39로 구석까지 물들임(측정 0.42/0.35/0.29) = BB 원칙에 걸리는 건 오히려 이쪽 → **사람 플테로 존폐 판정**(문서 논리가 아니라 손맛으로). (b) 빛 바 스윕 — 우리는 그 자리에 로켓이 있어 겸할지 별도로 깔지 미결. (c) **카운트업 — 그대로 이식하면 안 됨**: BB는 점수가 유일한 목표지만 우리는 C9에서 SCORE를 구석으로 격하했고 C23에서 목표를 "남은 적 N"으로 못 박음 → 우리 버전 카운트업의 주인공은 점수가 아니라 **"남은 적" 숫자**(로켓이 적을 죽일 때마다 툭툭 줄어드는 게 순차 피격과 동기). (d) 배치 순간의 보상(BB는 헛수에도 "+60" 플로터 = **빈손인 수가 없음**. 우리 수의 절반 이상이 헛수). (e) 연출 중 입력 허용(BB는 재생 중에도 다음 조각 드래그 가능. 우리는 최대 2.5초 완전 잠금). (f) 하단 영문 튜토리얼 3줄 미한글화(C22 누락).
- **C29** (2026-07-13, 커밋 `1530116`) ⚠**SPEC 항목이 누락된 채 커밋됨** — 커밋 메시지는 "큰 조각 빈도 정상화 + 난이도 곡선 복원"이나 결정 로그에 기록이 없어 **의도·근거를 복원할 수 없음**. C30 시점 실측(봇 300판/스테이지, DDA off) 승률은 **86 / 76 / 70 / 45 / 35%** 로, C28이 적어둔 목표 곡선(96/77/57/44/28)과 크게 다름 — 특히 **S3가 57% 의도인데 70%**, **S5가 28% 의도인데 35%**로 전반적으로 헐거움. **이 괴리가 C29의 의도인지 이탈인지 미확인 = 다음 세션 확인 대상.**
- **C28** (2026-07-13, 보드 8×8 + 조각 풀 확장 + DDA) **보드 6×6 → 8×8, 조각 풀에 직선 장척 추가, DDA는 '줄 완성 구제'로만**. **① 8×8 = 성격의 전환**: 줄이 8칸이라 조각 유입 대비 수지가 조여짐 → 클리어율 **46% → 34%**(Block Blast 실측치와 동일). 그 결과 **막힘 사망이 거의 사라지고 죽음이 전부 '거점 파괴'로 이동** = 디펜스 장르에 맞는 실패 경로로 복귀("놓을 곳이 없어 죽는다"는 6×6의 지배적 사인이었고 장르와 어긋났음). ⚠대가: 줄 완성이 어려워져 **최대콤보 5.9 → 4.0**, 커버리지 사다리가 짧아짐. **② 처방 = 조각 풀 확장**(BB 실측 반영: 최다 조각이 **I5(17.7%)**): `I5h/I5v`(직선 5칸) + `R32/R23`(직사각 6칸) + `R33`(3×3) 추가, 3티어(SMALL/MID/BIG)로 재편하고 **보드 여유에 따라 티어 확률**을 정함(C27의 board-aware 유지). 결과 최대콤보 **4.0 → 4.6**, 클리어율 **37%**(BB 30%와 구 46%의 중간)로 착지. `CLIMAX_COMBO`=5. **③ DDA — 채택하되 '구제 전용', 그리고 결정적 교훈**: 유저 제보(BB에 DDA 있음)로 구현. ⚠**첫 구현이 게임을 오히려 어렵게 만들었음**(S5 승률 28%→16%): '친절도 = 놓을 자리가 많은 조각'으로 점수를 매겼는데 그건 곧 **작은 조각**이고, 작은 조각은 막힘은 피하지만 **줄을 못 만들어 공격을 굶긴다** = 디펜스에선 무장해제. **BB는 죽음의 원인이 막힘 하나뿐이라 '잘 맞는 조각=구원'이 성립하지만, 우리는 실패 경로가 둘(막힘/거점)이라 '친절'의 정의가 다르다.** → 구제를 **"지금 줄을 완성할 수 있는 조각"으로만** 한정(못 찾으면 무개입). '압도 중 → 까다로운 조각' 절반은 폐기(온보딩 막힘사만 3배로 늘림). 갓 모드(같은 스테이지 연속 2회 실패 → 강한 구제) 유지. **④ DDA 효과는 시뮬로 검증 불가**: 봇은 실력이 일정해 '고전' 상태에 잘 안 빠짐 → A/B 차이가 대부분 노이즈 범위(±3%p). DDA의 가치는 실제 사람이 무너질 때 나오므로 **사람 플테로만 판정 가능**. **⑤ 최종 승률(DDA off)**: **96 / 77 / 57 / 44 / 28%**. `step_every`=2는 8×8에서 절벽(누수시계 16배치)이라 전 스테이지 3으로 통일, 난이도는 물량·거점HP·타입 믹스로만 만듦. fast 전진도 `base/2`(→1, 과함) 대신 `base−1`로 완만화.
- **C27** (2026-07-13, 조각 딜링 로직) **조각 크기 = 보드 상태 연동 + 공정 리필 보장**. **① 레퍼런스 검증 결과 = 판정 불가(정직하게)**: "Block Blast는 보드 상태 따라 조각을 준다"를 검정 — 리필 시점(3개 새로 깔린 순간)의 보드와 받은 조각을 짝지어 **같은 풀에서 무작위로 뽑은 대조군**과 비교. 배치가능률 100% vs 99.6%, 3개 다 소화 100% vs 100%, 즉시 줄완성 가능 71% vs 75%, 평균 크기 4.48 vs 4.51칸 → **모든 지표에서 무작위와 구별 안 됨**. 단 이 영상의 플레이어가 보드를 계속 느슨하게(20~30/64칸) 유지해 **빡빡한 보드 표본이 없음 = 검정력 부족** → "적응형이 없다"가 아니라 **"이 영상으론 증명 불가"**. ⚠**앞서 본 '보드가 찰수록 작은 조각' 신호는 측정 오류**였음: 트레이 *중간* 상태를 세면 보드가 찬 시점엔 "플레이어가 안 고르고 남긴 조각"만 남아 작아 보임 — 인과가 반대(작아서 안 골라 남은 것). 리필 순간만 보면 사라짐. **② 우리 코드의 진짜 결함(실측)**: `_make_piece`의 `big_chance`가 **적 스폰 진행도(`spawned/total`)**에 묶여 있어 보드를 안 봄 → 파편화된 보드에도 후반이면 테트로미노를 50%까지 퍼부음. 실측: **막힘 사망 시 보드 점유가 평균 46%뿐** = 보드가 '차서'가 아니라 큰 조각이 '안 맞아서' 죽는다. + `_refill_tray`가 배치 가능 여부를 안 봐서 **막힘 사망의 5.6~11.2%가 '받자마자 셋 다 못 놓는' 딜 사고**(플레이어 실수 아님). **③ 처방**: (a) `big_chance`를 **보드 여유(`_free_cells`)** 연동으로 교체 — 여유 많으면 큰 조각, 빡빡하면 작은 조각(보드 10%참→평균 3.43칸, 85%참→2.67칸). (b) `_refill_tray`가 **최소 하나는 놓을 수 있는 트레이**가 나올 때까지 재추첨(최대 24회) → **풀트레이 즉사 0.0%**(전 스테이지). 막힘은 이제 '스스로 몰린 결과'로만 발생. **④ 연동 재조정**: 조각이 보드에 맞아 들어가니 클리어율 44→46%, 최대콤보↑ → `CLIMAX_COMBO` 6은 판당 2~3회로 잦아짐 → 스윕 후 **7** 확정(판당 0.6~2.0회). 스테이지 재조임 → 승률 **89 → 70 → 60 → 46 → 20%**.
- **C26** (2026-07-13, Block Blast 플레이 영상 분석 → 콤보 유예) **콤보 리셋 완화 = 스트릭이 실제로 자라게**. 레퍼런스(Block Blast 7분 실플레이) 프레임 분석에서 규칙 3가지 실측: **(a) 콤보 = 동시 줄 수가 아니라 스트릭** — 3개 열이 동시에 터졌는데 표시는 "Combo 4"(클리어 낸 배치마다 +1). **(b) 헛수 1회로는 스트릭이 안 끊김** — 클리어(Combo 3) → 아무것도 못 지운 3×3 배치 → 클리어(**Combo 4**)로 이어짐. 최대 Combo 7 관찰. **(c) 압박의 정체 = 산수** — 조각 평균 4.35칸 × 트레이 3개 = **13칸 유입** vs **한 줄 8칸** → 3수마다 1.6줄을 지워야 본전. 실측 클리어율 ~30%가 이 요구치와 거의 정확히 겹침(아슬아슬한 균형). 적도 타이머도 없이 조각 크기와 줄 길이의 비율만으로 긴장을 만듦. + 보드가 찰수록 **작은 조각을 주는 적응형 딜링** 신호(점유 0~9칸→평균 5.27칸, 40~49칸→3.75칸. 표본 작아 잠정). **처방**: 우리는 헛수 1회에 **즉시 combo=0**이라 스트릭이 봉쇄돼 있었음(봇 평균 최대콤보 2.6~3.0 = 커버리지 사다리가 1~2레인에서 끝남 = C25 ⑤에서 지적한 "사다리가 2칸뿐" 문제의 진짜 원인). → **`COMBO_GRACE`=1**(연속 2회 헛수에만 리셋). 결과: **최대콤보 2.7 → 5.2~5.7** = 커버리지 사다리(1→2→3→4레인→전멸)가 처음으로 실제 작동. HUD에 유예 상태 표시("콤보 x4 **위태!**" 경고색) — 유예가 판단 재료가 되게. **연동 재조정**: 스트릭이 자라니 `CLIMAX_COMBO` 3은 **판당 10회**가 터져 클라이맥스가 아니게 됨 → 임계 스윕(4/5/6/7) 후 **6** 확정(판당 1~2회 = 사다리 꼭대기, 도달 가능하되 드묾). 플레이어가 강해진 만큼 스테이지 재조임(총량↑·거점HP↓) → 승률 **85 → 75 → 60 → 50 → 33%** 단조 하강 유지. 우리 클리어율은 **~44%**로 Block Blast(~30%)보다 높음 = 조각/줄 수지가 더 헐거움(6×6이라 줄이 6칸으로 짧음) → **후속 검토**: 유입 대비 줄 길이 수지를 조여 "차오르는 압박"을 되살릴지. 연출 학습(색 통일·충전 홀드·점수 카운트업 동기화)은 미착수.
- **C25** (2026-07-13, 밸런싱 + 스테이지) **밸런스 기준 확립 + 독립 레벨 5종**. 헤드리스 시뮬레이터(`tools/sim.gd`, 그리디 패커 봇 500판/스테이지)로 감이 아니라 실측으로 튜닝. **① 측정 오염 버그 수정**: 누수(적이 거점 도달)가 `killed++`로 집계돼 **흘려보낸 적이 목표 진행도·승리조건에 카운트**됐음(구 거점25/총28 → 24마리 흘리고 4마리만 잡아도 승리). `killed`/`leaked` 분리, 클리어=`killed+leaked ≥ total`(누수분은 거점 HP로 값을 치름), **거점 HP = '허용 누수 횟수+1'로 재정의**해 total 대비 훨씬 작게(누수 봉쇄). 진행바는 처치=초록·누수=빨강 분리 표시. **② 기준 = "데미지는 손잡이가 아니다"**: 일격 최소값(콤보1·1줄)=120인데 basic HP는 어느 스테이지든 <120 = **항상 원샷**. HP를 올려도 안 변하다가 임계 넘으면 갑자기 안 죽는 '절벽' → C24(증폭축=커버리지)의 귀결로 **난이도도 커버리지 축에서 와야 함**. **③ 실측이 뒤집은 것 — 타입 정체성이 코드에 없었음**: (a) *swarm*은 인접 열에 **같은 row로 동시 스폰+동속** → 영원히 같은 행 → 가로줄 하나가 그 행 전체를 때리므로 **한 줄=클러스터 통째 전멸 = 가장 싸게 잡히는 적**(의도의 정반대. 무리 스테이지가 온보딩보다 쉬웠음 85%>83%). (b) *tank*는 콤보2(180) 구간에 앉혔는데 봇 평균 최대콤보 2.9라 그냥 뚫림. (c) 실제로 거점을 터뜨리는 건 **fast(누수 시계 6배치)뿐** = 작동하는 난이도 축이 '적 속도' 하나였음. **④ 처방(메커니즘 수정)**: swarm = 열을 보드 전체로 흩고 **전진 주기를 멤버마다 엇갈림(desync, 절반은 −1칸주기)** → 행·열 모두 벌어져 한 줄로 못 쓸어냄 = **범위(콤보 레인) 요구가 실제로 생김**. tank = HP를 **콤보3(240) 구간**으로(`tank_mult` 4.5, `hp_ramp`는 낮게 유지해 후반 콤보4 벽 방지) = **관통 요구 생김**. **⑤ `CLIMAX_COMBO` 4→3**: 콤보4 전멸이 판당 0.2회로 천장 밖이었음(C24가 6→4로 낮춘 것과 같은 논리) → 3에서 판당 ~1회. ⚠부작용: 6열 보드에서 커버리지 사다리가 **1레인 → 2레인 → 전체**로 2칸밖에 안 되고, 전멸이 흩어진 적의 답이 돼버림(후속 검토 대상). **⑥ 스테이지 = 독립 레벨 선택**(Block Blast식, 런 누적 아님) 5종: 첫 방어선(basic 온보딩) / 무리(swarm·범위) / 속공(fast·템포) / 장갑(tank·관통) / 총력전. 상수는 전부 `STAGES` 데이터로 이관(`total`/`core_hp`/`base_hp`/`hp_ramp`/`tank_mult`/`spawn_every`/`step_every`/`onboard`/`weights`). **⑦ 결과 승률(봇)**: 89 → 80 → 51 → 46 → 26% 단조 하강, 후반 패배 사유가 '막힘'에서 **'거점 파괴'로 전환**(디펜스가 실제 위협이 됨). ⚠봇 기준이라 사람 승률은 더 높을 것 — 절대치 아닌 **곡선의 형태**를 맞춘 것. 다음=사람 플테로 체감 검증.
- **C1** (2026-07-09) 새 프로젝트 아이디에이션. "디펜스 × 친숙게임" 11후보 → 주사위 디펜스 설계까지 갔다가 **운기반이라 폐기**(PC 프리미엄=숙련). 축 확정 = 운↔숙련.
- **C2** 도파민 논의 → **폭발형 바이럴 확정**. 해법 = 시스템적 복리 폭발(Balatro식, 저아트로 도파민). 팩킹계열 채택, 순수 ITB(조용함) 기각.
- **C3** 코어 확정 = 폴리오미노 팩킹 + **수동 발동 복리 연쇄**. 자원=조각당 적 1전진. 딜=10n².
- **C4** 프로토 v1 스펙 확정(§5), 새 폴더 `~/Desktop/Cascade` 신규 Godot 프로젝트로 구현 착수. Step A(코어루프)+legibility 패스 구현·플테. ①"참을까/터뜨릴까" 긴장 **통과**.
- **C24** (설계+플테) **조커/개조 1탄 = "콤보 = 청소 범위"**(증폭축 재정의): 로드맵 "복리 증폭" 착수. 현 증폭=콤보스트릭(선형 `1+0.5×(streak−1)`)뿐, 색(R/B/Y)은 `#시각용만`. **① 색 시너지 검토→기각**: 테트리스·Block Blast는 색 장식, 뿌요·컬럼스·퍼즐파이터는 색이 코어지만 "줄완성+같은색" 이중제약은 장르에 없음(서로 싸움 → C17 "실행 어려움" 재발). 소프트 보너스조차 "복잡"으로 유저 기각 → **색은 시각용 유지**. **② 핵심 통찰(유저)**: 데미지만 키우면 탱커·HP램프 외엔 오버킬 낭비(C5 "블라인드 부재" 재발). 디펜스 코어=밀물 청소(C6) → 증폭은 **데미지 아닌 커버리지(한 방에 몇 마리)**를 사야 함. 콤보 데미지배수(`_streak_mult`)는 '탱커 관통용 부증폭'으로만 유지. **③ 결정 = 콤보가 폭발 범위**: 완성 줄에서 **매 콤보 1줄씩 추가**(양옆 아닌 한 줄, 바깥 교대 +1/−1/+2…), 총 폭발 레인=콤보. 보드 셀 제거는 완성 줄만(**팩킹 보존** — 범위는 도달 데미지만, 공짜 공간 아님). **④ 순차 물결**(C15 순차연출): 링(추가 순서) 단위로 안→밖 순차 발사=심지처럼 번짐(`BLAST_RING_DELAY`, 플테 0.22 너무 빠름→**0.4**). **⑤ 전멸 클라이맥스**: 밴드가 전 열 커버 or **콤보≥`CLIMAX_COMBO`(=4)**(6×6 만콤보 6은 도달 드묾→낮춤)면 순차 대신 **한 방 광역** — 전 열 일제 로켓 + 보드중앙 대형 충격파 3겹 + 골드 전체화면 섬광 + 히트스톱, 세계 이동 전 **1.35s hold**로 breathe. **텍스트 없음**(유저 "전멸! 어색"→제거), 속도도 느리게(충격파 수명·섬광 늘림). **⑥ 에스컬레이션**(모든 콤보): 콤보↑ = 흰 섬광 밝기↑·**따뜻한 주황 물듦** + COMBO 라벨 골드→핫오렌지. **⑦ 셰이크 전면 제거**(유저 "피격 셰이크와 헷갈림"): 블라스트/콤보/전멸 셰이크 삭제 → 화면 흔들림은 **거점 누수·큰 적 사망(피격)에만**. 값 전부 느슨(튜닝 대상). 다음=종합 플테로 "복리 폭발감 + 커버리지 쾌감" 판정. 코드 `Main.gd`(`_begin_resolve`/`_fire_climax`).
- **C23** (플테) **목표 카드 의미 명확화**: C22 목표 아이콘(임시 붉은 토큰)이 "빨강만 잡나?"·"눈 있는데 보드 basic은 눈 없음"으로 인지 안 됨. 진단 핵심(유저): 이 게임은 매치3식 "특정 색 N개 모으기"가 아니라 **밀려오는 걸 무조건 다 없애야 거점 HP 안 깎이는** 구조 → 목표가 애초에 **타입 무관**. 처방: (a) 아이콘 = 특정 타입/혼합 대신 **타입 중립 "처치 대상" 기호 = 해골**(뼈색 두개골+아래턱+어두운 눈·코·이빨, 절차적). (b) "해골 28"만으론 "28마리 처치"라는 동사 의미가 안 실림 → 카드 문구 재편: 제목 **"목표"** 유지 + 내용 **"💀 남은 적 28"**('남은 적' 작게·숫자 크게, '마리' 단위는 뺌). C22 남은 스레드 ②(목표 아이콘) 해소. ①(보드 이동 예고)은 현 상단 카드로 충분 판정 → **스킵**. ③(폰트 번들) 미착수.
- **C22** (플테) **상단 정보 UI 전면 재설계 + 한글화**: C21 예고 미터(좌상단 작은 점)가 "적 언제 내려오나"를 못 살림. 반복 탐색 → 결론: (a) 구석 미터를 키우거나(B) 카운트다운 숫자로 바꿔도 **추상 숫자가 적과 분리돼 "이동"으로 인지 안 됨**, (b) 매 프레임 빨강 펄스는 과함(매치3 관습=평소 조용·임박만 정적 강조). **착지 = 상단 위계 재편**: 시선을 **두 카드로 분리**(Toon Blast식) — **목표 카드**(적 아이콘+남은 수=클리어 목표) + **적 이동 카드**(전진까지 N턴, 임박 시 붉은 정적 강조). 둘 아래 **처치 진행바**. **CORE HP 중복 제거**(상단 삭제→보드 하단 **거점 HP바**만: 빨강↔초록 그라데이션+외곽선 흰 글자). **전 라벨 한글화**(목표/적 이동/콤보/거점) + 한글 렌더용 `SystemFont` 로드. **시작 시 적 1마리 배치**(빈 보드서 "적 이동 N턴" 어색함 해소). 카드는 각진 `draw_rect`(라운드 코너 후속). **남은 스레드**: ①적 이동은 여전히 상단 숫자 — "움직임" 인지 마무리는 **보드 예고(A: 전진 임박 시 맨 앞 적 기울·착지칸 고스트)** ②목표 아이콘=임시 붉은 토큰 → 실제 적 스프라이트 교체 ③배포용 한글 폰트(Noto Sans KR 등) 번들.
- **C21** (플테) **적 이동 가시화**: 2배치마다 소리없이 툭 이동해 "언제 움직이나" 예측 불가. → **이동 예고 미터**(배치마다 채워지고 다 차면 적 이동+리셋, ENEMIES LEFT 근처) + **이동 애니**(순간이동 대신 한 칸 스르륵 미끄러짐, `vis_row` 이징). 주기(2)는 유지, 예측 가능+여유 유지.
- **C20** (플테 버그) **턴 순서 교체**: 기존 배치→적이동(+누수)→공격 이라 적이 먼저 움직여(거점 닿아 피해까지) 내 공격 전에 상황 종료="잡았어야 할 적한테 맞는" 억울함. → **배치 → 내 공격(현재 위치 적 타격) → 그 다음 적 이동/누수/스폰**. 조준한 적 먼저 죽이고 생존자만 전진 = 턴제 기본 문법. (적 이동주기=기본/탱크/무리 2배치·고속 1배치.)
- **C19** (플테) **교차 폭탄 제거 + 로켓 집중**: 교차(가로+세로 동시)는 6×6·랜덤·무회전에선 노릴 수 없어 "실행 불가" → 폭탄 삭제. **로켓 1종**으로, 기본 공격부터 손맛 확정. **로켓 연출 대폭 보강**: 머즐 플래시→굵고 밝은 로켓+긴 꼬리 질주→적 명중 시 임팩트 버스트+**히트스톱**(짧은 정지=타격감)+넉백+큰 데미지 숫자→라인 끝 소멸 폭발, 콤보 비례 강화. (변주는 후속: achievable한 콤보 업그레이드 등. 교차 트리거 재도입 금지.)
- **C18** (플테, 방향 전환) **매칭 모양 → 공격 종류**(Match-3 특수사탕 공식, 유저 아이디어). "줄 매칭이 왜 공격?" 해결 + 공격 변주·손맛·적별 대응 축. **1줄=로켓**(그 라인 관통), **교차(가로+세로)=폭탄**(교차점 3×3 광역·×1.5 보너스, 로켓과 별도로 추가). 로켓=한 레인(무리·고속)에, 폭탄=뭉침·탱크에 → 대응 생김. 스코프=로켓+폭탄 먼저(일제사격·초폭발 후속). + 중복 중앙 큰 데미지 숫자 제거(적 위 숫자만). 기존 방향성 타격을 "이름 붙은 공격"으로 승격.
- **C17** (플테, 근본 진단) **"규칙은 직관적이나 실행이 너무 어렵다"**: 줄완성=그 라인 적 죽이기는 직관적인데 **적이 움직이는 표적**이라 "그 적 있는 라인을 딱 완성"이 사실상 불가 → 킬이 우연·agency 없음 = 손맛 안 산 진짜 원인(연출 문제 아님). **처방 A(유저 선택)=규칙 유지, 실행 쉽게**: **보드 8×8→6×6**(짧은 줄=원하는 라인 빨리 완성), **적 느리게**(2배치마다 1칸·고속만 매배치), **적 수↓**(TOTAL 50→28) → "적 보고 그 줄 딱 완성해 잡기" 가능. (대안 B=근처 블록 데미지, 기각.)
- **C16** (플테) **적 사망 극화**: C15 순차연출로 거점피격(2)은 인지됐으나 "빔은 보여도 왜 적을 때리는지(사망) 안 보임"(1·3·4 여전). → **적 사망을 확실히**: 극적 사망(팝+파편+플래시+점수, 타입별 flavor)·빔 임팩트 플래시·**탱크 "막음" 연출**(역할 학습)·킬 시 ENEMIES LEFT 펄스(킬→목표 연결). ⚠이걸로도 손맛(4) 안 살면 잔손질 중단→구조(퍼즐 아래↔전장 위 시선분리) 재검토.
- **C15** (플테) **순차 연출**: C14가 "동시다발이라 안 보임"(1·2·3 다 인지 안 됨, "뭔가 빠짐") → 전투가 한 프레임에 몰림+시선은 아래 퍼즐에 있음. **줄 터질 때 입력 잠깐 정지하고 전투를 순서대로 재생**(충전→빔 훑음→적 거점쪽부터 팍팍 순차 처치→넉백→누수시 거점 쩍/−1/흔들). 스냅(~0.3~0.6s, 콤보 비례). 목적=이벤트 하나씩 보이게+시선을 전장으로. ⚠여전히 "빠짐"이면 구조 재검토(퍼즐 아래↔전장 위 시선 분리 근본문제).
- **C14** (플테) **전투 가시화 = 디펜스 손맛**(C13 적4종이 "색만 다름·역할 인지 안됨·손맛 없음"으로 판정 → 전투 자체를 보이게). **C=방향성 폭발**(전역 관통 폐기): 세로줄 완성→그 열 위로 관통 일격(그 레인 적 타격), 가로줄 완성→그 행 가로 스윕. 내 행동→어느 적 죽는지 가시화+몰린 곳 노리는 방어판단. **A=거점 피격 연출**(적 달려들어 때림·거점 쩍·흔들림·큰 −N). **B=빔 손맛**(관통 빔+적 순차 플래시·넉백·데미지숫자, 콤보 비례). **D=역할 텔레그래프**(탱크 HP노출+버티는게 보임, 고속 "!"긴급, 무리 쓸림, 첫등장 콜아웃). 적 종류는 그만 늘리고 전투 느낌부터. 밸런스 재튜닝.
- **C13** (플테) **디펜스 층 살리기**(퍼즐이 디펜스 재미를 굶긴다는 진단 → 인지 예산을 디펜스로): **적 4종**(기본/고속=2칸전진/탱크=고HP/무리=클러스터 스폰) 시각 확연 구분, 초반 기본만 후 섞임 + **폭발 넉백/스윕**(살아남은 적 위로 밀침, 콤보 비례) → 줄완성=방어무기 발사감. ⚠퍼즐은 더 안 조임(부담↓ 유지). 이후 튜닝.
- **C12** (플테) **조각 풀 확장**(Block Blast식): 4칸 테트로미노만이라 줄맞춤 뻣뻣→콤보 드묾. **작은 조각(1·2·3칸: 단일·도미노·트로미노·L3·2×2) 추가**, **초반 작은 조각 위주**(진행도 비례로 큰 조각 비중↑) → 배치 쉬움+초반 콤보 자주. 회전 없으니 비대칭 조각은 방향별로 풀에 포함. 난이도도 완화(C 흐름: BASE_HP40→28·HP_RAMP5→2·거점25·스폰밀도 완만).
- **C11** (플테) **회전 제거 + 3조각 트레이 도입**(Block Blast식). 회전 제거→공간제약↑("생각하게 됨"=좋은 숙련감) but 선택지 1개뿐=답답 → **트레이 3슬롯**(셋 중 골라 배치, 순서 자유, 3개 소진 시 리필)으로 agency 보상. 게임오버(NO SPACE)=**남은 트레이 조각 전부** 못 놓을 때. NEXT 프리뷰 제거(트레이가 곧 선택지). 적 전진=조각 배치당 1(트레이당 3전진).
- **C10** (플테) **공간부족 게임오버 추가**(Block Blast식): 현재 조각을 어느 위치·어느 회전으로도 못 놓으면 game_over("NO SPACE"). 패배조건 2개=거점HP0 or 공간부족.
- **C9** (플테) ①**웨이브 제거** → 한 판 = 총 M마리(가안50) 연속 스폰, 다 잡으면 승리. ②**헤드라인 = "ENEMIES LEFT n/m"**(SCORE 있던 상단중앙 큰 자리), SCORE는 구석 격하(유저 "Score는 최중요 정보 아님"). 웨이브 목표바→전체 진행바(killed/M), 배너/웨이브클리어팝 제거. ③**난이도 상향**(현재 ~100% 성공=너무 쉬움): ⚠HP만↑는 콤보스트릭이 딜 눈덩이로 무력화 → **HP↑+HP램프(스폰 인덱스 비례 급증)+스폰밀도↑** 병행. 가안=총50·기본HP40·+5/스폰·밀도1→2/배치·거점20. 튜닝 반복.
- **C8** (플테) ①**글자 가독**: 폭발 흰 플래시 위에 금색 팝 텍스트가 묻힘 → 진한 외곽선 추가 + 흰플래시 알파↓. ②**목표 가시화**(유저 "플레이 목표가 더 눈에 띄면"): 웨이브 목표가 화면에 안 드러남 → **웨이브 목표 바(상단, WAVE n/5 + 진행바 + "남은 적: N")** 적 처치마다 차오름·0=WAVE CLEAR 팝 + **웨이브 시작 배너**("WAVE n — 적 M"). 목표 단일화 = "이번 웨이브 적 다 잡기 → 5생존".
- **C7** (플테 튜닝, Block Blast 참고) C6가 "즉각 도파민(1)·테트리스 이유(3)"는 통과했으나 **"답답 + 인지 부족(2)"**: 가로줄만·10칸 세로보드라 완성 기회 가뭄. **Block Blast 로직 차용**(8×8·가로세로 양방향 클리어·3조각트레이·콤보스트릭·게임오버=공간부족). **적용: ①보드 8×8 ②가로줄 AND 세로줄 둘 다 클리어**(완성기회↑=답답해소) **③콤보 스트릭**(연속 클리어 수마다 COMBO×N 배수 폭증, 0클리어 수에서 리셋=인지 처방+숙련보상+도파민에스컬레) **④클리어/COMBO 연출 강화.** 폭발딜 = LINE_BASE(120) × 동시다줄배수 × 스트릭배수. **3조각 트레이(+회전 제거)는 보류**(다음, agency 여전히 얇으면). 회전은 유지.
- **C6** (플테 피벗) **코어 전환 = "줄 완성 즉발 폭발"**: C5의 QUOTA/수동발동/회로10n²는 ①"잘 놔도 이점 없다"(배치 얇음) ②"목표가 흩어짐"(블록·공격·방어 3분산) ③테트리스 미활용(모양=장식) 문제를 못 풀어서 폐기. 유저 판단: **도파민을 "발동"이 아니라 "배치 순간"에** + 테트리스 장점(공간=압박자원·줄완성보상·빈틈벌)을 정면 활용. **새 규칙: 가로줄을 꽉 채우는 조각을 놓는 순간 그 줄이 즉시 폭발**(데미지 120×콤보[1/2.5/4.5/7], 관통+셀제거+연출). 목표 단일화="줄 완성으로 밀려오는 적 청소". 웨이브=유한스폰[8/10/12/14/16]·전멸시 다음·5생존=승. 색은 시각용(매칭보너스=후속 depth). 제거: QUOTA·수동발동·10n²·potential·발동카운트업. **테트리스가 밥값을 하게 됨**(모양·회전·빈틈관리로 줄을 만들어야 폭발).
- **C5** (플테 발견) **핵심 결함 = "블라인드 부재"**: 작은 폭발로도 항상 적이 죽어 큰 회로를 만들 이유가 없음 → 숙련 보상·연쇄 손맛 둘 다 소멸("어떻게 놓든 죽고, 잘 놔도 이점 없다"). **처방 = 격파 목표(QUOTA) 시스템**: 웨이브마다 목표딜 Q=round(300×1.7^(w-1))[300/510/867/1474/2506], **한 발동 pool≥Q라야 웨이브 클리어**(누적 아님, 한 방의 큰 연쇄 보상), Q미달 발동=적만 솎고 보드비움(진행손실·긴급생존), 초과분=COMBO 점수. 적은 웨이브내내 계속 스폰(스폰풀 폐기). + **순차 연쇄 카운트업**(0→pool, 회로클수록 길게, Q넘기면 초록). 웨이브클리어 조건 기존(풀소진+보드빔)→ "pool≥Q"로 대체.
