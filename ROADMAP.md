# Cascade — 출시 로드맵

> 출시 계획 자립 문서. **상세 결정·근거는 `SPEC.md` §8 + 결정 로그 C50이 정본**이고, 이 문서는 그 요약·실행판이다.
> 확정일 2026-07-18 (C50). 팀: **기획·개발 = 유저 + Claude / 아트 = 별도 디자이너(병렬)**.

---

## 1. 출시 전략 (한눈에)

**라이브서비스 듀얼코어 + 데이터-먼저 소프트런치.**

- **듀얼코어**: 캠페인(스테이지)과 무한 모드가 **둘 다 코어**. 어느 하나가 부차가 아니다.
  - **캠페인** = 숙련·진행·authored 챌린지 → *"다음이 보고 싶다."*
  - **무한** = 표현·경쟁·하이스코어 → *"내 최고를 깬다."*
  - **교차 공급**(서로 잠식 안 하게): 공유 소프트 재화 · 코스메틱 양쪽 적용 · 하드 로스터(장갑·점착·분열)는 후반 캠페인 + 랭크 무한 양쪽 등장.
- **데이터-먼저 소프트런치**: 두 기둥을 얇게-좋게 내고, **어느 쪽을 플레이어가 사랑하는지 실측** → 라이브옵스 예산을 그쪽에 집중. 콘텐츠 예산을 미리 못박지 않는 게 소프트런치의 존재 이유.
- **셸 = 리더보드-먼저**: 소프트런치 셸은 무한 점수 **리더보드**(경쟁 천장). Play Games / Game Center = 백엔드 0, 아트 트레드밀 0. 코스메틱 셸은 IAP 층으로 후속.

> 왜 무한이 리텐션을 떠받치나: 무한은 정의상 "무한 일수 콘텐츠"라 7일+ 리텐션을 자동으로 먹는다. 스테이지는 유한하므로 **깔때기 겸 1주차 기둥**이지 7일을 채우는 물량이 아니다.

---

## 2. 수익 모델 (광고 우선 → IAP 나중)

소프트런치 = 광고 위주. IAP(코스메틱)는 데이터 보고 후속.

| 광고 포맷 | 설명 | Cascade 적합성 |
|---|---|---|
| **리워드 광고** | 자발 시청 → 보상 | ★ 코어. 부활(C48)이 이미 이 형태. 비강제라 코지 톤 유지 |
| **인터스티셜** | 자연 브레이크에 전면 광고 | ○ 스테이지 전환·판 종료. **빈도 캡 필수**(과하면 코지 파괴) |
| 리워드 인터스티셜 | 스킵 가능 + 보면 보상, 인트로로 opt-out | △ 인터스티셜의 온건판, 테스트 후보 |
| 배너 | 상시 띠 광고 | ✗ 코지 훼손 + 세로 보드 가림. 회피 |
| 앱 오픈 광고 | 앱 실행/복귀 시 | ✗ 첫인상 저해·공격적. 회피 |
| 네이티브 | UI에 녹인 광고 | △ 홈/상점 메뉴에 한해 후속 검토 |
| 오퍼월 / 플레이어블 | 오퍼 완료 보상 / 인터랙티브 | — 오퍼월은 코지 부적합, 플레이어블은 주로 UA(유저확보) 도구 |

- **소프트런치 확정 = 리워드(부활) + 인터스티셜(빈도 캡).**
- 배너·앱오픈은 코지 코어와 세로 화면을 해쳐 **제외**.
- 추가 포맷은 실데이터 + 현행 AdMob 정책 조사 후 도입.
- **IAP = 코스메틱**(광고 제거·스킨). 소프트런치 미포함, 데이터 보고.

---

## 3. 개발 로드맵

> 기간은 **코어가 이미 완성된 전제**의 추정치. 아트 트랙은 개발과 동시 진행.

### 개발 트랙 (유저 + Claude) — 순차

| Phase | 내용 | 대략 | 산출물 |
|---|---|---|---|
| **0. 모드 아키텍처** | `modes/game_mode.gd` director를 `Main.gd`에 통합 · `StageMode`(현 STAGES 래핑) + `EndlessMode` · `modes/` 커밋 | **~3~5일** | 코어가 모드-무관, 무한모드 형제로 꽂을 이음새 |
| **1. 무한 코어 + 리더보드** | EndlessMode(승리 없이 램프, 레버 = 전진 속도) · 점수 = 생존 깊이 · 로컬 베스트 → 플랫폼 리더보드 | **~1.5주** | 경쟁 코어 + 얇은 셸 |
| **2. 모바일 배관** | 터치 입력(드래그 C32 → `InputEventScreenDrag`) · 세로 실기기 사이징 · 세이브/영속 · KR 폰트(C22③) · **광고 SDK**(리워드 + 인터스티셜, AdMob + 미디에이션) | **~2주** | 실기기에서 돌고 돈이 되는 빌드 |
| **3. 캠페인 확장 + 소프트런치 셸** | 스테이지 5 → **12~15**(데이터 + `sim.gd` 곡선 검증) · 홈 / 리더보드 UI / 설정 / 통계 | **~1~1.5주** | 두 기둥 + 셸 완비 |
| **4. 런치 옵스** | 애널리틱스(D1/D7·광고 fill·부활율) · 스토어 에셋 · 빌드 파이프라인(안드로이드 먼저) · 1지역 소프트런치 | **~1~1.5주** | 스토어에 오른 소프트런치 |

**→ 소프트런치까지 대략 6~8주.**

### ⭐ Phase V. 검증-먼저 스프린트 (다음 4주) — 디렉터+기획자 감사(2026-07-22)로 삽입

> **왜 끼어드나**: Phase 0·1과 Phase 2 대부분이 끝났지만 **8스테이지·무한을 사람이 한 번도 안 해봤다**(봇 시뮬 튜닝만). 셸(밤하늘 존 C75·PB 연출 C73·리더보드 화면 C66)은 계속 깊어지는데 코어 60초가 사람을 붙잡는지는 0회 검증 → 검증보다 앞선 크래프트. 동시에 '데이터-먼저'인데 정작 데이터를 받을 배관(광고 SDK·실빌드·실 리더보드·애널리틱스)이 통째로 미완. **이 스프린트의 목표 = 폴리싱이 아니라, 진짜 빌드 + 진짜 광고 + 진짜 지표로 한 코어(무한)를 실사람에게 붙이는 것.**

> **진행 상태(2026-07-27)**: W1 **설계 산출물 2건만 완료**(`ANALYTICS_TAXONOMY.md`·`PLAYTEST_PROTOCOL.md`, 22f7f11) — **실행(플테·계정·배선)은 0**. W2~W4 전부 미착수. 그 사이 실제 작업은 폴리싱·목표 동사 저작(C82~C93)으로 흘렀다 = **⑦동결 조항 위반**. **2026-07-27 결정 = 여기서 폴리싱 동결하고 W1 실행으로 전환**(아래 §5 참조).
>
> | 항목 | 상태 |
> |---|---|
> | ① 사람 플테 5~10명 | 🔄 **빌드는 나옴**(2026-07-27): 안드로이드 디버그 APK 27MB·arm64·`com.yujin.cascade`. 남은 것 = **피험자 5~10명 섭외·실행**(유저 몫, `PLAYTEST_PROTOCOL.md`) |
> | └ 빌드 방법 | `PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH" godot --headless --export-debug "Android" build/android/cascade.apk` → 기기 연결 후 `adb install -r build/android/cascade.apk`. 툴체인·함정은 [[android-build-toolchain-setup]] |
> | ② 애널리틱스 택소노미 설계 | ✅ 완료 (P0 이벤트 10여 종 확정, Firebase 채택) / **Firebase 프로젝트 개설 ❌**(계정 작업, 유저 몫) |
> | ③ AdMob 연동 | 🔄 **R2 거의 완료** — R1=기획·이음새·페이크 백엔드·`ad_*` 계측(`ad_probe` 29/29) · R2=**AdMob 플러그인 v5.0.0 실배선 + 광고 포함 APK**(`ad_mock_probe` 30/30). 계정 없이 **구글 공개 테스트 유닛**으로 끝냈다. **2026-07-29 에뮬레이터 실관측: 테스트 광고 시청 → 부활 성사 ✅**(배선 전체가 살아 있음이 확인됨). 남은 관측 2건 = no-fill 폴백 · 중도 이탈(둘 다 같은 콜백의 분기, 프로브는 초록) → 그다음 R3(실 유닛·UMP·데이터 안전성) |
> | ④ 애널리틱스 실배선 | ✅ **P0 전량 배선 완료(2026-07-27)** — `analytics.gd` 이음새 + 판 경계 발화 + 로컬 JSONL 수집 + 판독기(`tools/analytics_report.gd`)·검증 프로브(`tools/analytics_probe.gd`). Firebase SDK 연결만 W2로 남음 |
> | ⑤ 실 리더보드 + 목업 제거 | ❌ `leaderboard.gd` `_platform_*` 전부 no-op, 친구보드·퍼센타일 목업 그대로 |
> | ⑥ 안드로이드 서명 파이프라인 | ❌ 미착수 |
> | ⑦ 폴리싱 동결 | ⚠ 위반 → **2026-07-27 발효** |
> | ⑧ 안전망-on 데일리 | ⏸️ 보류 유지 |
> | ⑨~⑫ (W4) | ❌ 미착수 |

| 주 | 할 일 | 왜 |
|---|---|---|
| **W1** | ① **실기기 빌드로 8스테이지+무한 사람 플테 5~10명**(광고 없이, 옆에서 관찰: 이탈 지점·"참을까/터뜨릴까" 긴장·죽음이 '아까움'인지 '억울함'(C60 재현)인지·부활 실제로 누르나) ② **애널리틱스 이벤트 택소노미 설계 + GA4/Firebase(무료) 개설** | 로드맵 내내 미뤄진 **가장 정보 많은 행동**. 지표는 빌드 전에 심어야 소급 가능 |
| **W2** | ③ **AdMob 리워드+인터스티셜 테스트 유닛 연동**(부활 스텁→실 리워드 배선, **no-fill 폴백** 정책, iOS면 ATT) ④ 애널리틱스 실배선 | 광고 SDK = 네이티브+미디에이션+서명 = **1인 개발 최대 지뢰(통상 2~4주)이자 모든 수익 데이터의 유일한 게이트.** 맨 뒤에 두면 출시 직전 터짐 → **당겨라** |
| **W3** | ⑤ **Play Games 실 리더보드 1개 배선 + 목업 친구/퍼센타일은 출시 빌드서 제거**(coming soon 게이트) ⑥ **안드로이드 서명 빌드 파이프라인** ⑦ **셸·캠페인 폴리싱 전면 동결**(밤하늘 존·PB 연출·캠페인 아트 정지) ⑧ 여유 시 **안전망-on 데일리**(C60 재설계, 엔진 있으니 1~2일 = D7 레버) | 가짜 소셜 증거는 안 붙이느니만 못함. 데일리는 장르 최강 D7 훅인데 '거의 다 된' 상태 |
| **W4** | ⑨ Play Console 클로즈드 트랙 업로드 ⑩ **소프트런치 지역 = Tier-1 영어권(캐나다/호주)** 확정 ⑪ **소액 유료 UA($수백, 크리에이티브 3~5종)** ⑫ **kill/promote 임계값 문서화** | 싼 인스톨(Tier-2)은 eCPM 왜곡 → 수익 신호 못 읽음. 순수 오가닉으론 코호트 안 참 → 데이터-먼저의 데이터 자체가 안 모임 |

**이 스프린트 이후에야 "데이터-먼저"가 말 그대로가 된다.** 기존 Phase 2 잔여(광고 SDK)·Phase 4 배관(애널리틱스·빌드)이 여기로 앞당겨져 흡수됨. Phase 3(캠페인 12~15)·점착·오늘의 판 심화는 **그대로 데이터 후**.

**감사에서 드러난 스코프 누락 3건(로드맵 밖이었음)**:
- ~~**튜토리얼/FTUE 부재** = 진짜 1순위.~~ → **✅ 해소(C77, fc0bfca)**: 스테이지1 온보딩 3박자(놓기→줄 완성→첫 누수 '손해 학습' 캡션) main 병합. 남은 건 계측뿐(`tutorial_beat_completed` 퍼널, W1 ②).
- **포지셔닝 미스매치**: "코지"라 부르지만 알맹이는 미드코어 전략퍼즐 → 코지 UA는 튕기고 전략 유저는 안 낚임. **가장 안 밝혀진 가정 = "이 코어 루프가 사람을 붙잡느냐" 자체**(어느 기둥이냐 다툼보다 앞섬).
- **캠페인 처방**: 이미 만들어짐(매몰비용 종료) → **지금 상태로 동결, 튜토리얼 겸 1주차 깔때기로만 얇게** 내고 무한으로 리드. 코드상 그렇게 낼 수 있음, 손만 떼면 됨.

**진단 정정(당초 디렉터 감사 4번)**: "코지라 죽음이 드물어 광고 볼 일 없음"은 이 게임엔 틀림 — 후반 서지(C47)로 죽음은 오히려 흔함(봇 100~250건/판). **진짜 리스크는 죽음 빈도가 아니라 죽음의 '질'과 부활 전환율이 미검증**이라는 것(C60서 '억울한 죽음' 신호 = 부활이 아니라 이탈 유발).

### 아트 트랙 (별도 디자이너) — 병렬

- 스테이지 시각 변주 · 적 / 보드 폴리시
- 코스메틱 스킨 시드(팔레트·모양 변주 — 저아트라 저비용)
- **스토어 에셋**(아이콘·스크린샷) — **Phase 4까지 필요**
- UI 아트

---

## 4. 소프트런치 이후 (데이터 기반 라이브옵스)

실데이터가 가리키는 방향으로 예산 집중:

- **코스메틱 경제 + IAP** (소프트 재화 earn + 스킨 sale)
- **하드 로스터 랭크 사다리** (장갑·점착·분열 — 코어는 chill, 얘들은 경쟁 천장에 예약)
- **데일리 챌린지** (고정 시드 비교)
- **시즌** 구조
- **추가 광고 포맷** (조사 후)
- **데이터가 가리키는 기둥(캠페인 or 무한) 집중 확장**

---

## 5. 상태 스냅샷 (2026-07-27)

- ✅ **완료**: 코어 루프 · 한 판 경험 아크(서지→부활→축하, C47~C49) · 광고 부활 스텁
- ✅ **Phase 3 캠페인 8스테이지 아크(C51→C57)**: 난이도를 **조각 풀**로(퍼즐 축) 확정·기전 승격(c965f10) → 프리셋 3종(RICH/STD/LEAN) + 6스테이지 authoring(신규 '줄 굶김' 포함), C57 act-3(7·8)에서 분열 도입해 **6→8스테이지**. sim 검증 승률 90→…→23% **단조**(`tools/sim.gd`). 점착·스테이지 12~15는 실데이터 후.
- ✅ **Phase 0 모드 아키텍처(C52·C56·C62·C63)**: director 이음새(StageMode+EndlessMode) + `game_rng` 분리(게임 결정성↔코스메틱, 회귀 byte-identical). 코어에서 모드 이름 분기 0(C63).
- ✅ **Phase 1 무한 코어 실플레이(C57)**: 셀렉트/허브 진입 + 무한 HUD(점수·깊이) + 곱셈 점수 + 로컬 베스트. 사람 플테 가능. (`tools/endless_ui_probe.gd` 창 검증)
- ✅ **featured 결정적 트랙 엔진(C59)**: '오늘의 시드 = 전원 동일 판' — 인덱스-주소 RNG로 `piece[i]`/`spawn[i]`를 보드 무반응 순수 함수화. 결정성 5시드 byte-identical + 캠페인 regress 불변. (`tools/featured_{probe,sweep,shot}.gd`)
- ✅ **출시 시퀀스 확정(2026-07-20)**: 8스테이지 트랙은 아크·감독 이음새·허브까지 완성 → **모바일 배관(Phase 2)을 점착·스테이지 확장보다 먼저.** 점착·12~15는 소프트런치 데이터 후(데이터-먼저와 일관).
- 🔄 **Phase 2 모바일 배관(C67→C70)**: 설정 모달+홈 내비 통일(C67) · 세로 반응형 프레임 + 터치=마우스 경로(C68, `tools/{portrait_shot,touch_probe}.gd`) · **영어 우선 i18n + Noto Sans 라틴 번들**(C69, 글로벌 출시) · **세이브 감사**(C70: 캠페인 진행도 영속 + 손상 내성, `tools/save_probe.gd` 12/12). **남은 것 = 광고 SDK**(리워드 부활 + 인터스티셜, AdMob+미디에이션 — 네이티브라 빌드 파이프라인 Phase 4와 맞물림).
- ✅ **Phase 1 무한 셸 심화(C64→C67·C73·C75)**: 리더보드 이음새 뒤로 점수 저장·제출(C64, `LeaderboardService`) · 거점사>클리어 우선순위 버그(C65) · **리더보드 화면**(C66: 친구 우선+퍼센타일·무부활 개인기록 — **백엔드는 로컬만, 친구 보드·퍼센타일은 목업**. `_platform_*` stub은 모바일 배관 때 실배선) · **PB 너머 발화 램프**(C67: 개인기록 넘으면 적 HP 복리↑, '평지' 해소) · **PB 돌파 연출**(C73: 크라운 락+원샷 버스트+인디고 배경) · **밤하늘 존**(C75: 절대점수 이산 연출 계단, 스펙터클↔난이도 분리 — [[endless-darkmode-record-visual]]).
- ✅ **캠페인 플로우 폴리싱 패스(C72→C74)**: 플로우 probe(`tools/campaign_flow.gd`, 13프레임 실측 감사)로 결함 도출 → **선택화면 힌트 오버플로**·**최종클리어 중복/오라벨 버튼** fix, **프런티어 유도**(마지막 스테이지=완주 아닌 '따라잡음'→무한 깔때기, [[stage-last-clear-is-frontier-not-finale]]), **장갑=강철 판금 실루엣**(도착 가독성, `tools/tank_readability.gd`), **콤보 카피 정직성**(피해→터지는 범위), **스테이지 인트로**(진입 목표 배너 중앙 등장→💀 수만 상단 목표 카드로 도킹, 탭 스킵, `tools/intro_shot.gd`, C74). 전부 회귀 byte-identical. **남은 펀치리스트 = #7 첫등장 콜아웃 보드 겹침 · #9 메뉴 세로 여백**(값싼 폴리시, 미착수).
- ✅ **보석 수집 스테이지 + 부활 버그수정(C81·C88·C89, track/stage 병합)**: 받기형 수집(Gem Rush) 목표 · 보석 전경화 튜닝 + Gem Rush S5 배치([[stage-goal-type-variety-future]]) · 거점사 부활 소프트락 수정([[wave-accounting-invariant]]).
- ✅ **튜토리얼/FTUE(C76·C77, fc0bfca)**: 스테이지1 온보딩 3박자 + 보드 폭 90% 확대(BB 원본 프레임 실측). **감사가 지목한 스코프 누락 1순위가 해소됨** — 남은 건 완주 퍼널 계측(W1 ②).
- ✅ **출시 차단 UX 패스(C78→C80·C82·C83·C85)**: 죽은 버튼·미번역 화면·노치·뒤로가기 4건 fix(C78) · 플레이 화면 상시안내 4줄 제거(C79, 가르치는 몫을 플레이로) · 허브 개편(신규=단일 경로/복귀=이어하기, C80~C83) · HUD 위계 정리(C85). 회귀 골든 재베이스(e461463).
- ✅ **인지 개선 패스(C87·C88)**: 무한 콤보 스펙터클(칭찬어 밴드, [[combo-is-spectacle-bonus]]) · **적 전진 텔레그래프 재설계**(글로벌 카운트다운 폐기 → 적별 2채널, [[advance-telegraph-per-enemy-not-global]]) + 목표 카드 확대.
- ✅ **도파민 폴리싱 패스(C90, track/juice)**: 칭찬 텍스트 리듬·점수 롤업·PB 판전체 폭발·산개 불꽃 튜닝 · **시커 종이비행기**(2줄+ → 거점 근접 적 호밍 처치, 자동발사 확정 — 탭-투-유즈는 시도 후 revert, [[tap-to-use-item-and-retiming]]) · 줄수 라벨(double/triple/quad) 폐기. 골든 재베이스(8f84a7b).
- ✅ **목표 동사 4종 완결(C81·C90·C91)**: Hold(거점 방어) · Collect(Gem Rush) · **Defuse**(폭탄 3-rung, 2cf9ccf) · **Protect**(도둑/금고 — 훔치기→막기/회수/영구손실, f7dc110). 동사 세트는 닫힘([[stage-goal-type-variety-future]]) — 남은 건 Protect R2/R3·이벤트 저작이며 **데이터 후**로 미룸.
- ✅ **스테이지 셸 확정(C92·C93)**: 스테이지 화면 = 비인터랙티브 **진행 리드아웃**(피커·재플레이 폐기, 프런티어 전용 버튼, [[stage-select-is-progress-readout]]) · 모두-클리어 전용 화면(버튼 없는 순수 안내) · **광고 부활 소프트락 fix**(놓을 자리 보장 = 트레이 리필+안전망, d6689ad).
- ✅ **블록 6색 카테고리컬 팔레트(2026-07-27 병합)**: 3색 → 색상환 6방향(빨·주·노·초·파·보-마젠타), CIELAB 상호 최소 ΔE≈39. 색은 순수 장식 유지([[color-is-cosmetic-final]]) · 회귀 byte-identical 확인 · `tools/candy_probe.gd`.
- 🧹 **브랜치 정리(2026-07-27)**: 병합 완료된 구 브랜치 4개 삭제(c22·c29·feat/stage1-tutorial·refactor/gamemode-director-seam) · `track/color` 병합 · juice·endless·color 워크트리를 main으로 동기화. `track/stage`만 미커밋 WIP(크로스 관통 빔 되돌리기) 보유로 동기화 보류.
- ⏸️ **오늘의 판(featured 데일리) 보류(C60)**: 첫 사람 플테서 **'억울한 막힘사'**(byte-동일 위해 안전망 제거한 대가) 발견 → 코지 톤 > 결정성 순수. **엔진은 보존, 플레이어 진입만 제거.** 재개 시 = 안전망 on 재설계('byte-동일' 아닌 '같은 도전'). **데이터-먼저: 프리 무한 먼저 제대로 → 데이터 후 데일리 재개.**
- 🧊 **폴리싱 동결 발효(2026-07-27)** — Phase V ⑦ 조항을 지금 적용. **동결 대상**: 캠페인·셸 폴리싱, 신규 목표 동사/이벤트 저작, 도파민 연출, 색·아트 변주. **동결 예외**: 출시 차단 버그 · 계측/광고/빌드 배관 · 플테에서 드러난 결함. 근거: C82~C93 두 주가 전부 폴리싱으로 흘렀고, 그 사이 "코어가 사람을 붙잡느냐"는 여전히 **0회 검증**. 스펙터클을 더 쌓기 전에 검증이 먼저다.
- 🔄 **Phase V W2 광고 배관 R1 완료(2026-07-28)**: 기획 확정 `AD_PLAN.md`(부활 캡 **판당 1회** 확정 — SPEC의 "무제한" 정정 / **no-fill이면 공짜로 이어줌** — 이어하기 소실 = C60 억울한 죽음 재발 / 광고 중도이탈은 기회 소진 아님 / **인터스티셜은 배관만, 노출 off** — 캡 수치가 플테 0회 근거라 W4 UA 코호트서 검증). 코드 = `ad_service.gd` 이음새(leaderboard·analytics와 동형, `_platform_*`만 R2에 채움) + 페이크 백엔드(fill/no-fill/취소/오류/지연 토글) + `ad_*` 6종 계측 + 결과 팝업 대기 잠금. 검증 = `tools/ad_probe.gd` 29/29 · 회귀 byte-identical · `analytics_probe` 28이벤트 PASS. **⚠12 테스터 규칙 발견**: 개인 Play 계정은 프로덕션 전 테스터 12명×14일 클로즈드 테스트 필수 → **플테 섭외를 5~10명이 아니라 12명+로** 잡으면 W1·W4가 한 번에 해결.
- ✅ **gradle 커스텀 빌드 전환(2026-07-28)**: 프리빌트 템플릿 → **로컬 gradle 컴파일**로 전환(`export_presets.cfg` `use_gradle_build=true`). **왜 = 네이티브 플러그인(AdMob·Firebase)의 유일한 전제조건**이고, 동시에 **AAB(클로즈드 트랙 업로드 형식)의 전제조건**이라 W2 ③·W3 ⑥이 둘 다 여기에 걸려 있었음. 선행으로 `platforms;android-35` + `build-tools;35.0.1` 설치(템플릿 요구치 = compileSdk 35). **동등성 정적 검증 = package/version/targetSdk 35/arm64 단독/권한(INTERNET+VIBRATE) 전부 프리빌트와 동일, 압축 전 총량 83.6MB≈83.8MB.** APK 파일 크기만 27MB→76MB(`compress_native_libraries=false` = 네이티브 lib 무압축 저장, 구글 권장·AAB 배포 시 무관. 사이드로드로 뿌릴 땐 압축 켜면 27MB 복귀). **⚠미검증 = 실기기 실행**(기기 미연결, `classes.dex` 5.7→6.1MB로 클래스 경로가 달라져 런타임에서만 드러날 수 있음 → `adb install -r` 1회 필요). 부수 발견 = ① `android/`가 .gitignore라 **빌드 템플릿이 저장소에 없음**(다른 머신/워크트리는 재설치 필요) ② **앱 아이콘 미설정**(`project.godot`에 `config/icon` 없음 → 기본 Godot 아이콘으로 출고, 아트 트랙 스토어 에셋 몫).
- 🔄 **Phase V W2 광고 배관 R2 — 코드·빌드 완료(2026-07-28)**: AdMob 플러그인 **v5.0.0**(poingstudios) 실배선 + **광고 포함 APK 산출**(88MB·arm64, 매니페스트에 `APPLICATION_ID`·플러그인 meta-data·`AD_ID` 권한 확인). **AdMob 계정은 안 필요했다** — 구글 공개 테스트 유닛으로 여기까지 온다(계정은 R3 몫). **밟은 지뢰 2개**: ① **compileSdk 35→36** — v5의 aar이 Google Mobile Ads *Next-Gen SDK v25*라 36을 요구하는데 Godot 4.6.2 템플릿은 35. compileSdk는 export preset이 노출 안 하는 값이라 템플릿 직접 수정 → `tools/android_template_patch.py`(⚠`android/`가 .gitignore라 저장소가 기억 못 함 = 템플릿 재설치 때마다 재실행) ② **플러그인 부재의 페이크 위장** — 기기에서 네이티브가 안 붙었는데 페이크로 흐르면 광고가 도는 것처럼 보이고 수익만 0이 된다 → 기기에선 플러그인 없으면 서비스를 끈다(공짜 부활로 흐르고 `ad_*` 지표가 비어 즉시 드러남). 검증 = **`tools/ad_mock_probe.gd` 30/30**(에디터 목 광고로 *플러그인 콜백 → 우리 상태 기계* 번역만 조준. 타임아웃 2종 포함 — 콜백이 영영 안 오는 게 "결과 1회" 불변식이 깨지는 유일한 현실 경로) · `ad_probe` 29/29 · 회귀 byte-identical · `analytics_probe` PASS. **✅실관측(2026-07-29, 에뮬레이터 Pixel 6/Android 15/arm64)**: 테스트 광고 시청 → **부활 성사**. 네이티브 플러그인 로드 → `MobileAds.initialize` → 프리로드 → 표시 → 보상 → 부활까지 한 줄로 이어짐을 눈으로 확인. 남은 관측 2건 = no-fill 폴백 · 중도 이탈(같은 콜백의 분기, 프로브는 초록). 에뮬 세팅·절차는 `AD_PLAN.md` §5 R2 / [[android-build-toolchain-setup]].
- 🐞 **가로 고정 버그 수정(2026-07-29, 출시 차단)**: 에뮬에 처음 올리자 앱이 **가로로 잠겨** 뜨고 세로 보드가 화면 왼쪽 띠에만 그려졌다. 원인 = `project.godot`의 `handheld/orientation`이 Godot 3 문자열 `"portrait"`(Godot 4는 정수 enum, 1=세로) → 매니페스트에 `screenOrientation=0`. **W1 프리빌트 APK부터 계속 그 상태**였는데 `handheld` 전용 값이라 에디터·데스크톱·창 모드 프로브 어디서도 안 드러났다. 교훈 = [[apk-must-actually-run-once]].
- 🔜 **다음(우선·진행 중) = ⭐ Phase V W1 실행**: ① **실기기(안드로이드) 플테 빌드 준비** — export preset이 `Web`뿐이라 안드로이드 preset·디버그 APK 경로부터 · ② **애널리틱스 배선**(`ANALYTICS_TAXONOMY.md` P0 이벤트를 `leaderboard.gd`식 서비스 이음새 뒤로, 플랫폼 SDK는 stub) + Firebase 프로젝트 개설(유저 몫) · ③ 피험자 5~10명 섭외(유저 몫, `PLAYTEST_PROTOCOL.md`) → 이후 W2 **광고 SDK**(최대 지뢰, 맨 뒤 금지) → W3 **실 리더보드 + 목업 소셜 제거** → W4 **클로즈드 트랙 + Tier-1 영어권 + 소액 UA + kill/promote 임계값**.
- 🔜 **후순위(데이터 후)**: 점착 구현(키스톤·막힘문 몫) · 스테이지 12~15 확장 · **Protect R2/R3 + 라이브 이벤트 저작** · 오늘의 판 재개(C60 ② 안전망 on 설계) · 공유 카드 · 캠페인 펀치리스트 #7(첫등장 콜아웃 겹침)·#9(메뉴 세로 여백)
- ⛔ **폐기**: 조커/개조 · 로그라이트 런 · 색-회로 기계 축 (근거: SPEC 결정 로그 C24·C50)
